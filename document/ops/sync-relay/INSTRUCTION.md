# 감리 → 개발팀장 지시서 (활성) — **현재 작업 없음**

- **상태**: **작업 없음 (idle)** · **S4-4 재개 보류**
- **갱신**: 2026-08-25 · 클로드감리
- **직전 라운드**: S4-15 — 결과 정본은 **[`INSTRUCTION-S415.md`](INSTRUCTION-S415.md)** 에 보존했다

---

## 지금 개발팀장이 할 일

**없다.** 이 파일에 새 지시번호가 적히기 전까지 착수하지 않는다.

`/지시확인`·`/싱크지시` 로 이 파일을 열었다면 **여기서 멈춘다.** 직전 라운드(S4-15)는 이미
`보고완료` 처리되어 `INSTRUCTION-S415.md` 로 보존됐다. 그 지시를 다시 수행하지 않는다.

---

## 현재 상태

| 항목 | 값 |
|---|---|
| S4-4 (동행 라이더 앞뒤 튐) | **미해결 일단락** — S4-15 까지 진행 후 재개 보류 |
| main2 | `66c9a5d` · 제품 코드는 **S4-1 · S4-2 · S4-2R · S4-3 까지만** 병합(`d879588`) |
| feature | `fix/multiplayer-read-amplification` @ **`0f5d35a`** (origin 동기) |
| S4-4 ~ S4-15 | **main2 병합 금지** — 계측기뿐 아니라 **미채택 제품 실험(S4-5 시간축 · S4-13 A/B)** 이 섞여 있다 |
| 현황 보고서 | [260825-동행-라이더-튐-S4-4-현황-보고서](../../archive/260825-동행-라이더-튐-S4-4-현황-보고서.md) |
| 진행 요약 | [REPORT.md](REPORT.md) |

---

## 재개할 때의 금지 사항 (그대로 유효)

```
S4-4~S4-15 의 feature 코드를 병합·수정·재시험하지 않는다 (별도 지시 전까지)
원인 파라미터가 확정되기 전에는 보간 상수·마커 로직·카메라 코드를 수정하지 않는다
캡처 로그는 「회귀 replay」가 아니라 「실패 trace」로 표기한다 (kind: fail-trace-not-replay)
새 노브·상수·예측 알고리즘을 먼저 지시하지 않는다
싱크 정확도(현재 수준)는 퇴보시키지 않는다 — 간격 확대 + 보간 단순 회귀는 옛 문제의 재발이다
```

---

## 재개 시 첫 착수 지점 (미확정 · 계획일 뿐)

S4-15 는 11.45 Hz trace 에서 `projected` 단계를 **최초 관측 지점**으로 기록했을 뿐,
원인은 확정되지 않았다. 재개하면 다음 둘을 함께 다룬다.

```
1. 카메라 4파라미터 분기      bearing · center · pitch · zoom — 어느 것이 투영을 흔드는지
                              ⑦ 로 이미 같은 프레임에 기록 중이라 새 배선이 거의 필요 없다
2. 60 fps 단일 브라우저 재검증  11.45 Hz 기록으로는 실제 진동 폭을 볼 수 없다 (W-1)
```

**둘 다 아직 아무것도 확정되지 않았다.** bearing 이 1순위라는 것도 자릿수 추정일 뿐이다.

---

## 지시서 보존 규칙

새 라운드를 시작할 때는 **이 파일을 덮어쓰기 전에** 직전 내용을 `INSTRUCTION-<번호>.md` 로
먼저 보존한다. `보고완료` 상태의 지시서를 그대로 덮어쓰면 결과 정본이 사라진다.
