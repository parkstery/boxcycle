# 감리 → 개발팀장 지시서 (활성) — S4-4R3 단독 주행 대조군 (peer 없이 ②만 본다)

> S4-4R2 는 `INSTRUCTION-S44R2.md` 로 보존했다(감리가 옮겨 둠. 문서 커밋에 담아라).
> 결과는 §6 형식으로 이 파일 아래에 덧붙이고 `상태` → `보고완료`.

- **지시번호**: S4-4R3 (단독 주행 대조군 — 화면 흔들림이 peer 없이도 나는가)
- **발신**: 클로드감리0820 · **일시**: 2026-08-20 · **상태**: 보고완료
- **브랜치**: `fix/multiplayer-read-amplification` · worktree `C:/20.HDev/rtw-sync-s4-2/repo` (판정 `7c3b1be` · 캡처 `be7a5c5`)

---

## 0. 이번에도 **한 가지만** 한다

```
목적    라이더를 혼자 달리게 하고 ② local 절대 진행축 반전을 잰다.
        peer 가 없는 상태에서도 흔들리면 원인은 peer 동기화 밖이다.
```

**S4-4R2 는 WARNING 으로 채택했다. 되돌리지 마라.** 판정기·캡처·산출물 전부 유지한다.

이번에 하지 않는 것.

```
프레임별 카메라/peer 투표      쓰지 마라 (§2-2)
Chief 조건(나란히) 재현        하지 마라
px/m 환산                      하지 마라
제품 수정                      하지 마라 — 카메라가 확정돼도 이번엔 안 고친다
```

---

## 1. 왜 단독 주행인가 — 감리가 데이터에서 본 것

S4-4R2 의 계열 집계다.

```
              ① peer 절대    ② local 절대    ③ 상대
C-A 25/25        25 회          51 회          45 회
C-B 32/10         3 회          57 회          13 회
```

**로컬 라이더가 peer 보다 더 많이 뒤집힌다.** 로컬은 peer 동기화 경로를 타지 않는다.
그런데 프레임별 분류는 「카메라 0」을 냈다(표본 3 · 1 프레임, `mixed` 가 기본값).
**집계와 분류가 서로 반대를 가리킨다. 이 모순을 푸는 것이 이번 일이다.**

지금까지 모든 캡처는 **둘이 달렸다.** 두 성분이 항상 섞여 있었다.
**혼자 달리면 peer 성분이 0 이 된다. 남는 흔들림은 전부 투영·카메라다.**

---

## 2. 실험 — 대조군 하나면 된다

### 2-1. 단독 주행을 찍어라

```
S-A   라이더 1 명 · 25 km/h · leftFlat · 다른 참가자 없음
S-B   라이더 1 명 · 32 km/h · leftFlat · 다른 참가자 없음
```

**같은 Trail·같은 경로·같은 카메라·같은 창 길이·같은 프레임 간격**으로 찍어라.
C-A/C-B 와 비교할 것이므로 **조건이 다르면 비교가 성립하지 않는다.**
아래를 산출물에 반드시 남겨라.

```
routeId · routeLenM · camera · 카메라 거리 · windowMs · displayFrames · medianFrameDtMs
```

산출: `S44R3-S-A-solo-25.json` · `S44R3-S-B-solo-32.json` · 각 `*-summary.json` · `S44R3-shots/`

### 2-2. 판정은 **계열 비교**로 한다 — 프레임 투표를 쓰지 마라

S4-4R2 의 `classifyCameraVsPeer` 는 이번 판정에 쓰지 마라.
`mixed` 가 기본값이라 카메라 성분을 흡수한다.

비교할 것은 이것뿐이다.

| | 2인 (C-A / C-B) | 단독 (S-A / S-B) |
|---|---|---|
| ② local 반전 횟수 | 51 / 57 | ? |
| ② 최대 px | ? | ? |
| ② peak-to-peak px | ? | ? |
| ② 음수 투영 스텝 수 | 33 / 29 | ? |

**C-A/C-B 의 ② 값도 같은 표에 다시 적어라**(기존 산출물에서 읽어 오면 된다. 다시 찍지 마라).

### 2-3. 판정 규칙 — 이대로 적용하라

```
단독 ② 반전이 2인 ② 와 비슷한 수준     →  투영·카메라 확정. peer 동기화 무관
단독 ② 반전이 뚜렷하게 적거나 없다      →  peer 가 있을 때만 화면이 흔들린다는 뜻
                                          (peer 렌더·카메라 추종 대상 변화 등 후속 조사)
그 사이                                 →  두 값을 그대로 제시하고 「미결」로 적어라
```

**「비슷한 수준」·「뚜렷하게 적다」를 눈대중으로 쓰지 마라.** 횟수·최대·진폭 세 값을 모두 제시하고,
비율(단독/2인)을 계산해 적어라.

---

## 3. 【필수】 계측 생존 증명 — 0 을 결론으로 쓰지 마라

단독 주행에서는 **판정기가 peer 이벤트를 전제로 짜여 있으면 ②가 구조적으로 0 이 나온다.**
그러면 「흔들림 없음」이 아니라 **죽은 계측**이다. S4-4 의 `axis: none` 이 그렇게 나왔다.

착수 전에 이것부터 확인하고 보고하라.

```
3-1  단독 캡처에서 display 이벤트가 실제로 쌓이는가 (displayFrames 비-0)
3-2  local 화면좌표가 프레임마다 들어오는가 (hasLocalScreen: true)
3-3  로컬의 진행축 û 가 peer 없이도 추정되는가
     — û 는 displayDistM 전진 창 회귀다. 로컬 자신의 거리로 추정되어야 한다
3-4  로컬 화면좌표가 창 안에서 실제로 움직였는가 (총 변위 비-0)
```

**3-1~3-4 중 하나라도 실패하면 계측을 먼저 고치고, 그 전에는 판정하지 마라.**
필요하면 판정기가 로컬 단독 계열만으로도 돌게 확장하라 — 계측 확장은 허용된다.

---

## 4. 금지

- 제품 수정 — `integrator` · `mergePackets` · 보간 상수 · **카메라 코드** 전부
- 기존 산출물 삭제·덮어쓰기 — `S44-*` · `S44R-*` · `S44R2-*` · 샷 전부
- `9f3d5e9` ~ `7a6c7d8` 커밋 reset·revert·amend
- 프레임별 카메라/peer 투표로 이번 판정 내리기 (§2-2)
- px/m 환산을 판정 근거로 사용
- 고정 X/Y 축 판정 · `axis: none` 재선언
- 계측이 0 인 것을 「흔들림 없음」으로 읽기 (§3)
- 새 조건 추가 · 나란히 재현 · S4-3 회귀 혼입
- `git add -A` · `commit -a` · `--no-verify` · force · rebase · reset · amend · `python -c`·`sed` 우회 편집
- `C:/20.HDev/rtw-routes/repo` · `C:/20.HDev/rtw-hud-h1/repo` 접촉

---

## 5. 검증

| | 항목 | 기준 |
|---|---|---|
| L0 | 계측 생존 | §3 의 3-1~3-4 전부 통과. 비-0 관측으로 증명 |
| L1 | 조건 정합 | 단독과 2인의 route·카메라·창 길이·프레임 간격이 같음을 수치로 제시 |
| L2 | ② 계열 비교 | §2-2 표를 채움. 2인 값은 기존 산출물 인용 |
| L3 | 비율 | 단독/2인 비율을 횟수·최대·진폭 각각에 대해 제시 |
| L4 | 판정 | §2-3 규칙대로 — 투영·카메라 / peer 의존 / 미결 중 하나 |
| L5 | 제품 무변경 | `git diff` 로 확인 (계측·판정 파일 제외) |
| L6 | 무훼손 | `test:s42-meters` · `test:s43-meters` · `test:peer-s3a-replay` d0·d1 · `tsc -b` · 변경 파일 eslint 0 |

**L0 이 깨지면 L2~L4 를 판정에 쓰지 마라.**

---

## 6. 커밋 · 보고

```
커밋 1  판정기 — 로컬 단독 계열 지원 + 생존 검사 시험
커밋 2  단독 캡처 (S-A · S-B) + 계열 비교 결과
커밋 3  문서 — INSTRUCTION · INSTRUCTION-S44R2.md · REPORT.md
경로 지정 stage. **push 후** 보고

워킹트리에 남은 시험 재생성물(S3-fixture-gate.json · s3r-z15-cruise-scenario.json)은
generatedAt 만 바뀐 것이면 커밋하지 말고 되돌려라.
```

보고에 이 형식을 포함하라.

```
[S4-4R3 결과]
- 계측 생존 (3-1~3-4):
- 조건 정합 (route·카메라·창·프레임):
- ② 반전  2인 51/57  →  단독 ?/?   (횟수 · 최대px · 진폭)
- 비율 (단독/2인):
- 판정 (투영·카메라 / peer 의존 / 미결):
- 근거 한 줄:
- NEXT:
```

---

## 7. 확정으로 쓰지 말 것

```
S4-4R2 의 「혼합」          표본 3·1 프레임. 판정으로 인용하지 마라
「카메라 0」                크기 문턱을 못 넘은 것이지 카메라 무관이 아니다
C1 의 3.1px 이 정상          조건이 Chief 상황이 아니었다
px/m 환산값                 무효
```

---

## 결과 (개발 · 2026-08-20)

[S4-4R3 결과]
- 계측 생존 (3-1~3-4): **PASS.** S-A displayFrames 128 · hasLocalScreen true · û=local-dist-regression 128프레임 · travel 1062 px. S-B 135 · true · 같은 소스 135프레임 · travel 1897 px. 0 을 흔들림 없음으로 읽지 않음.
- 조건 정합 (route·카메라·창·프레임): 입문 마지막 코스 · leftFlat · 카메라 거리 40 m (기본, 양쪽 스펙이 슬라이더 미변경). routeLenM 453 m (단독 기록. 2인은 미기록). 창 S-A 8907 vs C-A 10066 ms, S-B 8469 vs C-B 8713 ms. 프레임 중앙간격 단독 68/62 vs 2인 137.5/118.5 ms (단독이 맵 틱마다 쌓여 약 2배).
- ② 반전  2인 51/57  →  단독 102/98   (횟수 · 최대 px · 진폭)
  - C-A 51회 · 29.3 px · p2p 58.3 px · 음수 33  →  S-A 102회 · 25.2 px · p2p 41.1 px · 음수 69
  - C-B 57회 · 32.5 px · p2p 65.1 px · 음수 29  →  S-B 98회 · 32.9 px · p2p 53.5 px · 음수 67
  - 2인 값은 `S44R2-*-summary.json` 인용. 다시 찍지 않음.
- 비율 (단독/2인):
  - A 횟수 2.00 · 최대 0.86 · 진폭 0.71
  - B 횟수 1.72 · 최대 1.01 · 진폭 0.82
  - 횟수 비는 프레임 수 비(128/71=1.80, 135/73=1.85)와 같다. 프레임당 반전율 A 0.80 vs 0.72, B 0.73 vs 0.78.
- 판정 (투영·카메라 / peer 의존 / 미결): **투영·카메라**
- 근거 한 줄: peer 없이 ② 최대·진폭이 2인과 같은 수준(0.71~1.01)이고, 횟수만 샘플링 밀도만큼 늘었다. 로컬은 peer 동기화 경로를 타지 않는다.
- NEXT: 카메라·투영을 고치는 것은 다음 지시. 이번엔 제품 미수정.

### L0~L6

| | 항목 | 결과 |
|---|---|---|
| L0 | 계측 생존 | PASS. 3-1~3-4 전부 비-0. S-A/S-B `survival.pass=true`. |
| L1 | 조건 정합 | leftFlat·40 m·입문 마지막. 창 길이 근접. 프레임 간격은 단독이 약 2배 촘촘 — 수치로 위에 제시. |
| L2 | ② 계열 비교 | 표 채움. 2인은 기존 산출물. |
| L3 | 비율 | 횟수·최대·진폭 각각 위에. |
| L4 | 판정 | 투영·카메라. 프레임 투표 없음. |
| L5 | 제품 무변경 | integrator · mergePackets · rideSyncPolicy · 카메라 코드 diff 없음. 계측은 `peerJitterCapture`·Registry 배선만. |
| L6 | 무훼손 | `test:s42-meters` 15 · `test:s43-meters` 11 · `test:peer-s3a-replay` d0·d1 · `tsc -b` 0 · 변경 파일 eslint 0. |

### 시도한 조건

```
S-A   1인 · 25 km/h · leftFlat · 40 m · 다른 참가자 없음 · 8.9 s
S-B   1인 · 32 km/h · leftFlat · 40 m · 다른 참가자 없음 · 8.5 s
안 한 것   나란히 · 새 속도 · 프레임 투표 · 제품 수정 · C-A/C-B 재촬영
```

### 이견·실패

- 단독 프레임 간격이 2인보다 촘촘하다. 횟수 비율을 진폭과 분리해 읽었다.
- S3-fixture-gate.json · s3r-z15-cruise-scenario.json 은 generatedAt 재생성 — 커밋하지 않음.
- 커밋 4 없음 — 제품 미수정.
