# S3-DIAG-R2 REPORT — 체인 초과량 재판정

- **지시번호**: S3-DIAG-R2
- **일시**: 2026-08-11
- **브랜치**: `fix/multiplayer-position-sync`
- **e2e 실행 시간**: **1.1 분** (`S3R-summary.json` `elapsedMin` 1 · 프로세스 134 s)
- **보존**: `document/ops/sync-relay/S3R-chain-events.json` (pt1~pt7 **전량 4671건**) · `S3R-summary.json`
- 시계 보정: before B−A=**10 ms** · after=**144 ms** · 판정용 평균 **`clockSkewMs=77`**
- S3-DIAG 「①→② 최초 이탈」· 「③→④ 4초」· 「지배 링크 ②~④」는 **전제로 쓰지 않음**.

---

## UAG — 초과량 최대 링크

**④→⑤ (최초 수신 역행 폐기)**

| | |
|---|---|
| **실측 초과** | p50 **3.6 m** · p95 **19.2 m** · max **23.2 m** (n=77 unique seq) |
| **설계 예상** | 수용 시 **0 m** (전진 폐기는 0) |
| **성격** | 전진 폐기는 0. **최초로 본 (uid,seq) 의 distM 이 이미 newest 보다 작아** ingest 가 위치를 버림 |

### 링크별 (실측 − 예상) — 전부 m

| 링크 | 예상 | 초과 p50 | 초과 p95 | 초과 max | n | 환산 |
|---|---|---:|---:|---:|---:|---|
| ①→② | 200 ms × applied | −0.16 m | 0.66 m | **1.39 m** | 243 | m 직접 (§2-2 동기) |
| ②→③ | 0 + 반올림 0.05 m | 0 | 0 | **0** | 242 | m 직접 |
| ③→④ | 네트워크(이번에 측정) | 0.78 m | 1.33 m | **8.44 m** | 238 | endToEnd **93 / 160 / 1013 ms** · skew=77 |
| **④→⑤** | 0 (수용) | **3.6 m** | **19.2 m** | **23.2 m** | 77 | 폐기 거리 |
| ⑤→⑥ extrapolate | ahead 전체=초과 | 2.81 m | 10 m | **10 m** | 92 | 160+ahead · cap 1.2 s |
| ⑤→⑥ oldest | (해당 없음) | — | — | — | 0 | — |
| ⑤→⑥ interpolate | 160 ms+격자 | 0 | 0 | 0 | 64 | 계약 오차≈0 |
| ⑥→⑦ | 0 (동일 geo) | 0 | 0 | **0** | 156 | m 직접 |

`receiveVsAckMs` 는 참고만 (p50 −108 ms). **판정에 쓰지 않음.**

---

## 기술

### A 내부 (시계 불변)

| | p50 | p95 | max | 1 s 초과 | m 환산 (8.33 m/s) |
|---|---:|---:|---:|---:|---|
| `publishQueueMs` | **4520** | 9737 | **13904** | **69.4%** | 37.7 / 81.1 / **115.9 m** |
| `writeRttMs` | 206 | 268 | 286 | 0% | 1.7 / 2.2 / 2.4 m |

- **최대 동시 in-flight = 10.** `mergeTrailMotionSnapshot` 은 `begin` 후 **`finally` 에서 감소** (정상·예외 공통).
- 큐 적체는 번호 링크가 아니라 **② 스냅샷이 set() 전에 늙는 양**. 페이로드 `d` 자체는 ②=③ (초과 0).
- 늦게 끝나는 옛 쓰기가 더 새 패킷 뒤에 도착하면 **④→⑤ 최초 역행**으로 나타난다.

### 전송 `endToEndMs` (보정 명시)

`endToEndMs = firstSeenAt − clockSkewMs(77) − motionWriteStartAt`

| | p50 | p95 | max | 1 s 초과 |
|---|---:|---:|---:|---:|
| ms | 93 | 160 | 1013 | 0.4% |
| m | 0.78 | 1.33 | 8.44 | |

`deliveryMs` 미사용. `receiveVsAckMs` p50 음수 — ack 전에 수신된 표본이 많음 (참고).

### §2-2 ①→② (동기 레코드만)

- 스냅샷 생성 스택에서 `capturedAt` · `authDist` · `snapshotDist` 를 한 번에 기록.
- 측정 |①−②| p50 **0.83 m** vs 기대 200 ms×applied p50 **1.67 m** → 초과 p50 **음수**(설계 안).
- max 초과 **1.39 m**. 수십 m 증상을 설명하지 못함.

### §5 모드

| mode | 점유 | 비고 |
|---|---:|---|
| interpolate | 41.0% | 계약 오차 p50 ≈ 0 · max 0.009 m |
| **extrapolate** | **58.9%** | ahead raw p50 337 · p95 2678 · max 3356 ms · **cap 히트 22.8%** |
| oldest | 0% | — |
| paused | 0% | 판정 제외 |

- 계약 준수 오차 ≈ **0** (구현 결함 없음 — 고치지 않음).
- 외삽 vs A 실제 이동 오차: p50 0.15 m · p95 1.64 m · max 3.85 m.
- extrapolate 점유 59% + cap 23% → **버퍼가 DELAY 밖으로 자주 나감** (stall 증거).

### 역행 212건 A/B/C

| | 건수 | 비율 |
|---|---:|---:|
| A 최초 수신 역행 | **77** | 36% |
| B 동일 seq 재관측 (onValue) | **135** | 64% |
| C 그 외 | **0** | — |

C 원문: 없음. `repeatSeenCount` p50=4 · p95=17 · max=32.

전진 폐기 **0** (게이트 PASS). 동일거리 중복 461 (D-0, 합격 기준 아님).

### D-8 (범위 밖 · 원인 단정 금지)

pt2 표본: A `routeLen=1500` · `geoLen≈1029.6`. 이번 창 dist ≪ cap. 분리 기록만.

### 실패·미완

- 없음. 제품 동작·integrator 공식·구독 방식 미변경.
- 정확도 수정(S3) 보류.

### 이견

- 작업 전 이견 없음. §5 네 갈래는 `integrator.ts` 140–174 와 일치 (paused / oldest / extrapolate / interpolate, span≤0 → t=0).

### 커밋

- 이 보고·계측·전량 JSON 은 동일 커밋에 포함.
