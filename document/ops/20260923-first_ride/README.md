# Local First Ride 릴레이 — 20260923-first_ride

> **감리(클로드)와 개발팀장(커서)이 이 폴더의 파일로만 통신한다.**
> Chief 는 파일을 옮기지 않는다. 양쪽이 스스로 이 폴더를 읽고 쓴다.
> 규약은 미니맵 묶음(`20260923-minimap/README.md`) · 카메라 묶음(`20260922-new_camera/README.md`)을 그대로 승계한다.

## 1. 파일 이름 규약

```
20260923-지시01-<내용>.md              ← 감리가 쓴다
20260923-지시01수행결과-<내용>.md       ← 개발팀장이 쓴다
20260923-지시01수행결과02-<내용>.md     ← 같은 지시를 재작업했을 때
```

- 번호는 **지시에 종속**된다. 수행결과는 자기 일련번호를 갖지 않는다
- 날짜 접두어 `20260923-` 는 이 묶음 개시일로 **고정**(작성일이 아니다)

## 2. 누가 실행하는가 (2026-09-24 전환)

**개발팀장은 커서가 아니라 `impl-standard` 서브에이전트다.** 감리가 지시서를 쓰고 Agent 로 직접
발주한다 — 수신 대기가 필요 없다. 역할·발주 체크리스트: [ROLES.md](ROLES.md)

| 도구 | 누가 | 언제 |
|---|---|---|
| `scripts/ops-relay/watch-status.mjs` | **Chief** | 상황판 — 라운드·진행 로그·diff·캡처 |
| `scripts/ops-relay/poll-next.mjs` | 감리 | 열린 지시 1회 조회 |
| `scripts/ops-relay/check-amend.mjs` | 구현 | 라운드 중 정정 확인 |
| ~~`await-next.mjs`~~ | — | **폐지**(커서 전용 수신기) |

루프: `지시NN 작성 → Agent 발주 → 구현·검증·캡처 → 지시NN수행결과-*.md → 완료 알림 → 감리 판정`

## 3. 이 묶음의 고정 규칙

- `git commit` · `git push` **금지**. 커밋은 감리 승인 후 별도 지시한다
- **한 라운드 = 화면 변화 하나.** 문서만 도는 라운드를 만들지 않는다
- 감리는 수행결과를 읽기 **전에** `git diff --stat -- apps/web/src` 를 본다. 0줄이면 내용과 무관하게 반려
- 감리는 앱을 직접 띄우지 않는다 — **개발팀장의 캡처로만 판정**한다.
  그러므로 캡처 목록은 지시서가 미리 못 박고, 빠지면 반려한다
- 이미지는 **폴더 경로를 통째로** 준다(`.out` 은 gitignore 라 파일 링크가 안 열린다).
  한글 경로에서 headless 크롬이 hang 한 전례가 있으므로 ASCII 미러 경로를 함께 쓴다
- **진행 로그를 쓸 때마다 정정을 확인하라** — 라운드 중간에 Chief 결정이 뒤집히면 감리가
  진행 중인 지시서에 「⚠【정정】」 블록을 끼워 넣는다. `node scripts/ops-relay/check-amend.mjs document/ops/20260923-first_ride`
  가 새 정정만 뽑아 준다. `AMENDED` 면 하던 방향이 무효가 된 것이다
- **진행 로그를 남겨라 — `PROGRESS.md` 에 한 줄씩.** Chief 가 상황판으로 본다.
  라운드 착수 시 1줄 · 눈에 보이는 진전마다 1줄 · 막히면 즉시 1줄 · **최소 10분에 한 줄**.
  20분 넘게 조용하면 멈춘 것으로 간주한다(형식은 그 파일 머리말)
- 검증 예산: 한 주행 검증 5분 · 같은 수정 3회면 접근 전환 · 브라우저 5분 무진전이면 정적 하네스로 전환 ·
  e2e/에뮬레이터 프로세스를 남기지 않는다
- **하네스를 쓴다면 제품 CSS 파일을 그대로 불러와라.** 손으로 베낀 사본은 미디어쿼리 분기 버그를
  영영 재현하지 못한다(미니맵 묶음에서 실제로 당했다 — 항상 통과하는 게이트였다)

## 4. 이 묶음의 요구 기준 (SoT)

| 문서 | 역할 |
|---|---|
| [`document/260923-RTW-Local-First-Ride-실행계획.md`](../../260923-RTW-Local-First-Ride-실행계획.md) | **요구의 기준.** LF-0~LF-4 범위·진입 조건·하지 않을 것 |
| [`document/260714-RTW-Ontology.md`](../../260714-RTW-Ontology.md) | 용어. **Ready Ride 는 §2.2 등재된 사용자 노출 명칭**이다 |
| [`document/260707-RTW-결정-로그.md`](../../260707-RTW-결정-로그.md) | 2026-09-23 네 줄이 이 묶음의 결정 근거 |

## 5. 이 묶음의 전체 그림 (감리 계획)

| 라운드 | 대응 | 내용 | 화면 변화 |
|---|---|---|---|
| 지시01 | LF-1 | **Recognition 표면** — 첫 화면 「어디에서 첫 라이딩을 할까요?」 + `[지역 선택]`·`[현재 위치]`, 선택 지역으로 카메라 이동 | 좌하단 카드 + 카메라 |
| 지시02 | LF-2 | **Ready Ride 생성** — 방위 자동 선택 + 폐합(출발=도착) + 기본 3km | 경로가 생긴다 |
| 지시03 | LF-3 | **Claim 연동 스코어** — 안 달린 길 우선 · 자기 중복 감점 | 같은 자리에서 다른 경로 |
| 지시04+ | LF-4 | 관측 · 미세조정 | — |

지시01 이 지시02 의 **출발점(어디서 생성할 것인가)** 을 만든다. 순서를 바꾸지 마라.

## 6. 현재 파일

목록을 여기 고정하지 않는다 — **폴더를 `ls` 하라.**
대응하는 `수행결과` 가 없는 가장 높은 `지시NN` 이 지금 할 일이다.
