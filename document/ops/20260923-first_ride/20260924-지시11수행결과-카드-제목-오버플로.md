# 지시11 수행결과 — Ready Ride 카드 제목 오버플로

- **상태**: 보고완료 / 개발팀장 = 커서
- **선행**: 지시10 조건부 PASS — 제목·링크가 패널 밖으로 넘침
- **커밋·푸시·배포**: 하지 않음

---

## 변경

| 파일 | 내용 |
|---|---|
| `LocalFirstEntryCard.tsx` | 제목을 `place`(지명) + `subtitle`(첫 Ready Ride (km)) 두 줄로 분리. Ready Ride·(km) 유지 |
| `LocalFirstEntryCard.css` | 앵커 폭 `10.5rem` 고정(키우지 않음) · `overflow: hidden` · place `overflow-wrap: anywhere` · title/link `max-width: 100%` |

### 적용 예

```
논현동
첫 Ready Ride (km)
[2][3][5][10]
[시작][지역]
입문 경로
```

---

## §5 수치 (740×400 · `.out/jisi11/metrics.json`)

| 항목 | 지시10 | 지시11 A | 지시11 B(긴 지명) | 지시11 C(생성 후) |
|---|---|---|---|---|
| 카드 rect (w × h) | 168 × 168 | **142 × 180** | **142 × 180** | **142 × 133** |
| 카드 면적 / 뷰포트 (%) | 9.5 | **8.6** | **8.6** | **6.4** |
| titleIn (패널 안) | — | **true** | **true** | **true** |
| linkIn (패널 안) | — | **true** | **true** | **true** |
| 제목 여백 L/R/T / 하단(링크) px | — | 6.4 / 6.4 / 5.6 / 5.9 | 동상 | 동상 |

- 면적 **9.5% 초과 없음**(오히려 8.6%로 감소). 폭을 키워 고치지 않음.
- B place = `강원특별자치도 원주시 단구동` — `titleIn`/`linkIn` 유지.

---

## §4 캡처

| # | 파일 | 확인 |
|---|---|---|
| A | `.out/jisi11/05-title-fixed.png` | 논현동 · 제목·링크 패널 안 |
| B | `.out/jisi11/06-title-long-region.png` | 긴 지명 · 넘침 없음 |
| C | `.out/jisi11/07-after-generated.png` | 생성 후 · 시작/지역 행 제거(면적↓) · Go 주황 유지 |

---

## §6 검증

- `apps/web` `npx tsc -b --noEmit` PASS
- 루트 `npm run build` PASS
- eslint(만진 TSX) — 수행 시 확인
- `git diff --stat` (카드 2파일): `LocalFirstEntryCard.tsx`·`.css` 변경
- `git commit`/`push`/배포 **안 함**
- 정정(`check-amend`): CLEAN
