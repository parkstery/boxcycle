# 지시05 수행결과 — Ready Ride 기본을 단순 경로로

- **상태**: 보고완료
- **개발팀장**: 커서
- **커밋·푸시·배포**: 하지 않음(묶음 금지). UI 캡처는 **Functions 에뮬레이터 + 실 Mapbox 시크릿**(`functions/.secret.local`, gitignore)으로 찍음 — Playwright geometry 인터셉트 없음.

---

## §1 바뀐 계약

| 항목 | 결과 |
|---|---|
| Ready Ride 기본 | **`searchReadyOnewayRoute`** (방위 표본 → 내부에서 `searchDistanceAutoRoute`) |
| 폐합 | `closeLoop` / `searchReadyLoopRoute` **코드 유지**, 클라이언트가 기본으로 보내지 않음 |
| 사과 문구 | 「순환 경로를 찾지 못해 편도…」 **삭제** (카드·훅) |
| 호출 예산 | 편도에 전부 — 실측 평균 **1.13회**/좌표, 최대 **2** (≤13) |

서버: `closeLoop` 아님 + `targetRoadPoint` 없음 → `readyOneway=true` → `searchReadyOnewayRoute`.

---

## §2 실측 성공률 (게이트) — **실 Mapbox**

스크립트: `.out/jisi05/oneway-success-rate.mjs`  
코어: `functions/lib/distanceAutoRouteCore.js` 의 `searchReadyOnewayRoute`  
결과 JSON: `.out/jisi05/oneway-success-rate.json`

| 항목 | 결과 | 게이트 |
|---|---|---|
| 수도권 20곳 × 3 km | **20/20 = 100%** | ≥90% ✅ |
| 논현동(강남) 5곳 × 2·3 km | **10/10 = 100%** | ≥90% ✅ |
| 거리 오차 | 전부 ±20% 이내 (대부분 0%) | ✅ |
| 프로바이더 호출 | max 2 · avg 1.13 | ≤13 ✅ |
| V1~V3 위반으로 버려진 후보 | **0건** (채택 경로 전부 게이트 통과; 재시도로 회복된 V1~V3 실패 메시지 없음) | 보고 |

측정 시각(UTC): `2026-09-23T22:47:23.150Z`.

---

## §3 캡처 (에뮬레이터 실 백엔드, 인터셉트 없음)

위치: `document/ops/20260923-first_ride/.out/jisi05/`

| # | 파일 | 확인 |
|---|---|---|
| A | `01-ready-ride-3km.png` | 논현동(인천 남동구 지명검색)·3 km·S≠E 도로 추적 경로·카드에 **다른 경로만**(사과 문구 없음) |
| B | `02-other-route.png` | 「다른 경로」 재호출 후 E 지점·선형 변경 |
| C | `03-go-gate.png` | RouteDock **Go** + SENSOR + 출발/도착 주소(경로 얹힘) |
| D | `04-legacy-click.png` | 클릭 기반 거리모드 + `getDistanceAutoRoute` → geometry **73점** (`dOk:true`) |

메타: `capture-meta.json`. 스크립트: `capture-real.mjs` (Vite `:5002` + emulators `:5001`).

---

## §4 코드 diff (제품)

```
apps/web/src/components/ride/LocalFirstEntryCard.tsx    |   6 -
apps/web/src/hooks/useReadyRide.ts                     |   4 +-
functions/src/distanceAutoRouteCore.ts                 | 142 +
functions/src/distanceAutoRouteHttp.ts                 | 136 +-
functions/src/index.ts                                 |   2 +
```

- `searchReadyOnewayRoute` 신규 (폐합 루프 코드는 미삭제)
- HTTP `readyOneway` 분기
- 클라: `closeLoop` 미전송 · 사과 카피 제거 · `lastResult.closed` 고정 false

(자동착수 인프라: `.claude/skills/ops-relay`, `.cursor/rules/ops-relay-auto.mdc`, `scripts/ops-relay/await-next.mjs` 등 — 제품 게이트와 별개)

---

## §5 검증

| 검사 | 결과 |
|---|---|
| `functions`: `npx tsc --noEmit` | PASS |
| `functions`: `npm test` (V1~V3 단위 4건) | PASS 4/4 |
| `apps/web`: `npx tsc -b --noEmit` | PASS |
| 루트 `npm run build` | PASS |
| `git commit` / `push` / 배포 | **안 함** |

---

## §6 회귀 (§6 체크)

1. 클릭 기반 자동 Route — 캡처 D + 에뮬레이터 HTTP 200
2. V1~V3 + 단위 4건 — PASS
3. S0/S1/S2·입문 경로 병존 — 카드에 「또는 입문 경로로 시작」 유지
4. Go 게이트 — 캡처 C
5. Route Token 규칙 — 미변경(에뮬레이터 온보딩 잔액으로 생성)

---

## §7 비고

- 지명검색 「논현동」은 **인천 남동구 논현동**으로 해석됨. §5 수치 게이트의 「논현동 5곳」은 지시04와 동일 **강남 논현** 좌표 세트.
- 배포 금지이므로 운영 Cloud Functions 에는 아직 `readyOneway` 가 없음. 캡처·실측은 로컬 빌드 코어/에뮬레이터 기준.
