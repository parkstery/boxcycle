# RIDE-IN-RIDE-HIERARCHY-3 재작업 지시 — 시도 1/3

| 항목 | 내용 |
|---|---|
| 문서 유형 | **execution** — 수퍼바이저 → 개발팀장 재지시 |
| 작성 | 2026-09-13 |
| 상태 | **활성** |
| 원 지시 | [260913-도로망-주행중-3단계-작업지시서](260913-도로망-주행중-3단계-작업지시서.md) |
| PR | [#17](https://github.com/parkstery/boxcycle/pull/17) · 브랜치 `cursor/ride-in-ride-hierarchy-3` |
| 시도 | **1 / 3** (동일 작업 3회 FAIL 시 중단·Chief 보고) |

## 1. 검수 총평 — FAIL

제품 변경( HUD 진행 강조·RouteDock 접기·Publication ETA 제거) 방향은 지시 §1과 맞다. **금지 범위 위반 없음.**  
그러나 수용 기준 **H1·H2·H3 증거가 계획 기준을 통과하지 못함.**

| ID | 판정 | 사유 |
|---|---|---|
| H1 | FAIL | `test:next-ride`만으로는 센서→Go→running→종료→재개 흐름을 증명하지 못함. RouteDock Go 표면을 바꿨으므로 `npm run test:e2e:ride`(ride-entry) 또는 동등 e2e 필수 |
| H2 | FAIL (증거) | `route-dock-riding-contract.test.ts` 가 **제품 코드를 import하지 않고** 테스트 파일 안에 정책을 복제. 제품 `RouteDock.tsx` 와 드리프트 가능 → H2 증거로 불가 |
| H3 | FAIL (증거) | 좁은 가로에서 조작 가능 주장이 CSS 주석·자기보고뿐. 스크린샷 또는 e2e/정적 증거 없음 |
| H4 | PASS | `publicationDisplay.ts` + 목록 ETA 제거 + 계약 테스트가 제품 모듈을 import |
| H5 | 부분 | `test:next-ride` 197 pass 확인. H1용 e2e 추가 후 재집계 |
| H6 | 대기 | 머지 금지 |

## 2. 재작업 범위 (이 PR에만)

1. **H2 증거 고정**  
   - `routeDockUiPolicy`(또는 동등)를 `apps/web/src/lib/`(또는 RouteDock 옆 모듈)로 **추출**하고 `RouteDock.tsx` 가 그 함수를 사용하게 할 것.  
   - `route-dock-riding-contract.test.ts` 는 **그 제품 모듈만** import 해 단언. 테스트 내 정책 복제 금지.

2. **H1**  
   - `apps/web`에서 `npm run test:e2e:ride` 실행(또는 동일 ride-entry Playwright). fail 0.  
   - 결과(통과 수·시간)를 PR 본문/보고에 붙일 것.  
   - 5분 무진전이면 중단하고 로그·대체 경로를 보고(워크스페이스 자율 규칙).

3. **H3**  
   - 좁은 가로(예: 높이 ≤520px 또는 폭 ≤480px 가로)에서 riding 또는 ready-to-start 시 **Go 또는 종료/일시정지가 클릭 가능**함을 스크린샷 1장 이상 또는 Playwright 단언으로 제출.  
   - 경로: `document/archive/` 또는 PR 첨부. 「CSS로 완화」만으로 대체 불가.

4. **완료 보고**  
   - `document/archive/260913-RIDE-IN-RIDE-HIERARCHY-3-완료보고.md` 는 수퍼바이저 PASS 전 「완료」로 두지 말 것. 시도1 재작업 보고로 고치거나 `…-시도1-재작업.md` 로 분리.

**하지 말 것:** 범위 확대(6A 센서 이동·Modelling·nextRideTarget·Conquest·카메라)·force-push로 무관한 커밋 혼입·머지.

## 3. 제출

동일 PR #17 업데이트 후 수퍼바이저에:

```
시도: 1→2 제출
PR: #17
H2: 추출 모듈 경로 + 테스트가 제품 import 하는 증거
H1: test:e2e:ride 결과
H3: 스크린샷/단언 경로
남은 결함: …
```

수퍼바이저가 즉시 재검수한다. 2회차도 FAIL이면 시도 2/3 재지시. **3회 FAIL 시 중단·Chief 보고.**
