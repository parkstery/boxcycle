# 개발팀장 작업 계획 — `RIDE-NEXT-VISIT-2` 검수 PASS 후속

| 항목 | 내용 |
|---|---|
| 문서 유형 | **execution** — 개발팀장 지시 (수퍼바이저 → 개발팀장) |
| 최초 작성 | 2026-09-13 |
| 상태 | **완료** (Phase B 병합·배포·스모크 후 2단계 종결 · 다음 [3단계 지시서](260913-도로망-주행중-3단계-작업지시서.md)) |
| 작업 ID | `RIDE-NEXT-VISIT-2` (후속) / 다음 착수 후보 `RIDE-IN-RIDE-HIERARCHY-3` |
| 수퍼바이저 | Cursor IDE (Codex 이관 철회 · 2026-09-13 Chief 결정) |
| 연결 | [재착수 지시서](260911-도로망-다음접속-2단계-재착수-작업지시서.md), [PR #16](https://github.com/parkstery/boxcycle/pull/16), [실행 계획](../../260907-RTW-도로망-성취경험-실행계획.md) §3 |

## 0. 역할 고정

- **수퍼바이저**: Cursor — 지시·검수·Chief 보고. 구현 대신하지 않음.
- **개발팀장**: Cursor `impl-standard` 등 — 지시서 범위만 구현·검증·PR.
- Codex는 본 트랙 수퍼바이저가 **아님**.

## 1. 수퍼바이저 검수 결과 (PR #16)

| ID | 판정 | 근거 |
|---|---|---|
| V1 | PASS (정적) | `nextRideCardVisible` 시 `externalCameraJump` → `anchorLngLat` zoom 14 |
| V2 | PASS (정적) | `nextRideFramedRideIdRef` 로 rideId당 1회 |
| V3 | PASS (정적) | `FirstRideIntroCard` → `enterBasicHub(BASIC_SHARED_HUB_IDS[0])` |
| V4 | PASS (정적) | `showIdleHint` 가 카드·입문 CTA 표시 시 false |
| V5 | PASS (재실행) | `npm run test:next-ride` → pass 191 / fail 0 / todo 4 (`boxcycle-0b`) |
| V6 | **PASS** | PR #16 병합 `8ca6196` + Hosting 배포 |

**총평:** 제품 코드 반영 + 회귀 스위트 통과. **PASS**. 병합·배포는 Chief 승인 후.

금지 범위(`nextRideTarget` 파생·Go 게이트·Journey·Conquest·카메라 전면 교체) 위반 없음.

## 2. 지금 개발팀장이 할 일

### Phase A — Chief 승인 전 (즉시)

1. **신규 기능 착수 금지.** 3단계·Modelling·다른 핫픽스를 이 PR에 섞지 말 것.
2. PR #16 브랜치 `cursor/ride-next-visit-2b` 유지. force-push·범위 확장 금지.
3. Chief가 병합을 승인하면 Phase B로 전환.

### Phase B — 병합·배포 (Chief 승인 직후)

1. PR #16 → `main2` 병합 (수퍼바이저 또는 승인된 절차).
2. Firebase Hosting 배포 (`boxcycle-dc2df` → https://boxcycle-dc2df.web.app).
3. 스모크 (Hosting, 5분 이내):
   - 미완주 SavedRoute 계정: idle 새로고침 → NextRideCard + 지도가 앵커 근처로 **1회** 이동.
   - 지도 pan 후 자동 재점프 없음.
   - 후보 없는 계정(또는 dismiss 후가 아닌 null 후보): 「입문 경로 시작」 1탭 → 기존 입문 로드.
   - 카드/입문 CTA 보일 때 「MENU → 입문」 idle 힌트 숨김.
4. 스모크 결과·URL·시각을 수퍼바이저에 보고. archive 완료 보고 초안 작성.

### Phase C — 다음 제품 단계 (별도 지시서 발행 전 착수 금지)

실행 계획 §3 **단계 3** — 주행 중 변화와 선택 정리. 착수 전 수퍼바이저가 `document/ops/ride-relay/` 에 `RIDE-IN-RIDE-HIERARCHY-3`(가칭) 상세 지시서를 작성한다. 예상 범위(초안, 확정 아님):

- 라이브 궤적/진행률 위계
- 주행 중 불필요한 편집 UI 접기
- 추천 코스 제목·목적지 표현 개선(기존 Publication)
- [6A 센서 표면](../route-relay/260905-화면정리-센서칩-RouteDock-6A-작업지시서.md)과 중복 작업 금지 — 착수 시 실반영 여부 확인

**3단계 지시서 없이 코드 작성하지 말 것.**

## 3. 보고 형식 (Phase B 완료 시)

```
작업: RIDE-NEXT-VISIT-2 배포 스모크
병합: PR #16 → main2 (sha …)
Hosting: (배포 시각·URL)
스모크: V1…V4 현장 결과 (PASS/FAIL + 한 줄)
회귀: npm run test:next-ride → …
남은 결함: …
```

## 4. 수퍼바이저 다음 루프

1. Chief에게 PR #16 병합·Hosting 배포 승인 요청 (본 문서 §1).
2. 승인 → Phase B 감독 → archive·상태보드·결정 로그·지시서 상태 갱신.
3. 단계 3 상세 지시서 작성 후 개발팀장에 착수 지시.
