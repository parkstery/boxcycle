# Ride 0B 상태 확인 · 잠금 해제 후 실행

| 항목 | 내용 |
|---|---|
| 문서 유형 | execution — 운영 장애 복구 + 기존 지시 재개 |
| 최초 작성 | 2026-09-10 |
| 상태 | 활성 |
| 지시 ID | RTW-STATUS-20260910-01 |
| 개발팀장 | Cursor IDE Grok |
| 수퍼바이저 | Cursor supervisor (Codex 역할 인수) |
| 기준 SHA | ded6c65e45a92d9c06131f2724d9ee0212521512 |
| 선행 지시 | RTW-NEXT-20260910-01 (`issuecomment-5612671627`) |

## 1. 검수 사실

- `RTW-NEXT-20260910-01` 게시 이후 ACK/PROGRESS/SUBMISSION이 없다.
- PR HEAD는 여전히 `ded6c65`. 새 제품 커밋 없음.
- 로컬 poller(`poll-pr5.mjs`)는 살아 있으나 `runtime/state.json`의 work lock이 `RTW-AUTO-PROBE-20260910-01`에 남아 `busy`로 후속 INSTRUCTION wake를 막았다.
- PROBE 자체는 이미 SUBMISSION(`5612451721`)과 supervisor REVIEW(`5612537296`)로 종료된 상태다. 잠금은 잔존 운영 장애로 본다.
- supervisor가 stale lock을 해제했다. 동일 N1~N3를 다시 쓰지 않는다. `RTW-NEXT-20260910-01`가 유효한 실행 지시로 남는다.

## 2. 즉시 할 일

1. ACK — `RTW-STATUS-20260910-01`와 함께 `RTW-NEXT-20260910-01` 수신을 명시한다. receive path(automatic-poll / manual)와 현재 HEAD를 적는다.
2. 제출 완료된 instruction은 state에서 lock을 반드시 해제하고 `processedInstructionIds`에 넣는다. 제출 후 lock 잔존이 반복되면 그 버그를 고쳐 증거를 붙인다.
3. 바로 `RTW-NEXT-20260910-01`의 N1 → N2 → N3를 이어간다. 새 제품 범위를 만들지 않는다.
4. N1 checkpoint: taskkill timeout·sentinel 생존·짧은 deadline 증거.
5. N2 checkpoint: 실제 저장/종료/결과 시트 경로 증거(복사 시험 금지).
6. N3: Mapbox 의존 격리 후 emulator Auth/Firestore/CF·C13. 동일 실패 명령 맹목 재실행 금지. 시도당 600초.
7. 최종 `SUBMISSION — RTW-NEXT-20260910-01`에 SHA·명령·exit code·C1~C14 매핑·남은 BLOCK을 올린다.

## 3. 금지

- 병합·배포·1단계 착수
- PROBE/자동수신만 다시 손보고 제품 N2/N3를 미루는 것
- Playwright 10분 초과 대기 또는 동일 실패 e2e 반복
- 완료 자평. 판정은 supervisor
