# 지시11 수행결과 — aerial5(5m) · 커밋 · 푸시 · main 병합

- **지시**: [20260922-지시11-…](20260922-지시11-상공10m를-5m로-커밋푸시병합.md)
- **상태**: 코드·검증·커밋·푸시·병합 **완료** (아래 해시는 명령 출력 그대로)
- **작업 브랜치**: `main2` (병합 후 복귀 확인)

---

## §0 자원 규율

| 항목 | 결과 |
|---|---|
| e2e / Playwright / 에뮬레이터 / 추가 vite | **띄우지 않음** |
| headless Chrome | 정적 하네스 스크린샷 1회 + dump-dom 1회 (`apps/web/.out/jisi11-qc/`) |
| 종료 후 잔여 | `CommandLine` 에 `headless`/`jisi11` 인 chrome **0**. Playwright·에뮬·vite 미기동. (사용자 일반 Chrome 프로세스는 별개로 존재) |

검증: `npx tsc -b --noEmit` PASS · eslint 변경 파일 **error 0**(App.tsx baseline warning 5) · `npm run build` PASS (~2.05s).

---

## §1 변경 — aerial10 → aerial5 (5m)

| 위치 | 내용 |
|---|---|
| `App.tsx` | `Camera1Mode` / cycle: `aerial5`. `setRideCameraDistanceM(5)` |
| `MapHud.tsx` | 타입·뱃지 `"5"` · 라벨 `5m 상공` · `data-camera1-mode` |
| e2e 3파일 | 문자열/`data-camera1-mode`/`qc1-aerial5.png` 이름만 맞춤 (**실행 안 함**) |
| QC 2~5 · 60m · 거리 범위 | **미변경** |

순환: Route Fit → 60m 상공 → **5m 상공** → …

### floor 재확인 (직접 계산)

```
HEAD_C_Y = 1.3860226376955769  (riderRig.geometry.mjs)
RIDER_GLB_MODEL_SCALE = 1.15 × 1 = 1.15
RIDER_DISPLAY_HEIGHT_M = 1.5939260333499132
rideHeightSpanMargin(0) = 1.12  (FLAT 하한)
floorM = 1.12 × 1.5939… ≈ 1.7852 m
aerial5 = 5 m  →  5 > 1.785  → 클램프에 걸리지 않음 (clearsFloor: true)
```

지시서 수치(≈1.79m)와 일치.

---

## §2 정적 하네스 (뱃지 `5`)

캡처 폴더: `document/ops/20260922-new_camera/.out/지시11/`  
(미러: `apps/web/.out/jisi11-qc/`)

| 파일 | 내용 |
|---|---|
| `qc-aerial5-badge.png` | 폰 가로 900×400 · C1 활성 · 뱃지 **5** |
| `measure.json` | clipped **false** · overlapAccount/Metrics **false** |
| `qc-hud-harness.html` | 재현용(Playwright 아님 · CSS 복제) |

`measure.json` 요지 (`rootPx: 13.5`): C1 **37.5×37.5** · qc 폭 207.8 · Account 인접·겹침 없음 · 뱃지 `5` 잘림 없음.

**【정직】** 실 Mapbox 주행에서 5m 상공 프레이밍을 찍지 않았다. 거리 상수·이름·뱃지만 정적 확인.

코드 diff (커밋 직전 `git diff --stat -- apps/web/src`):

```
 apps/web/src/App.tsx                      |  9 +++---
 apps/web/src/components/maphud/MapHud.css | 49 +++++++++++++++++++------------
 apps/web/src/components/maphud/MapHud.tsx | 20 +++++++++----
 3 files changed, 50 insertions(+), 28 deletions(-)
```

(MapHud.css 는 지시10 분 · 이번 커밋에 함께 포함)

---

## §3 커밋 · 푸시 · 병합 (사실 먼저)

### 3-1. 커밋

```
[main2 61be6fb] feat(camera): QC 버튼 150% · 터치 전파 차단 · 1번 3단 상공 5m
 13 files changed, 533 insertions(+), 32 deletions(-)
```

포함: MapHud/App/e2e 문자열 · 지시10·10수행결과·지시11 · `.claude/skills/ops-relay` · `.cursor/rules/camera-relay*` · `ops-relay-auto` · `scripts/ops-relay/poll-next.mjs`  
제외: 빈 파일 `0` · `.out/`(gitignore)

### 3-2. `git log --oneline -3` (커밋·푸시·병합 **후**, main2)

```
61be6fb feat(camera): QC 버튼 150% · 터치 전파 차단 · 1번 3단 상공 5m
69a16f0 docs(ops): 지시09 수행결과에 커밋·푸시 해시 반영
93482fe feat(camera): Quick Camera 1~6 · 카메라 명칭 정정 · 거리 5~60m · pitch 80 확정
```

전체 해시: `61be6fb3500f4613438efcc58b39b11c88493ac9`

### 3-3. 푸시 main2

```
To https://github.com/parkstery/boxcycle.git
   69a16f0..61be6fb  main2 -> main2
```

### 3-4. 병합 main2 → main

충돌 **없음**. Fast-forward:

```
Updating 5f60f25..61be6fb
Fast-forward (no commit created; -m option ignored)
```

### 3-5. 푸시 main

```
To https://github.com/parkstery/boxcycle.git
   5f60f25..61be6fb  main -> main
```

### 3-6. 복귀 · 상태

```
Switched to branch 'main2'
## main2...origin/main2
?? 0
?? document/ops/20260922-new_camera/.out/
```

`origin/main` · `origin/main2` tip 모두 `61be6fb`.

`git status --short` 는 위 untracked 두 개만(의도). 이 수행결과 파일은 **커밋 이후에 작성**하므로, 작성 직후 문서 커밋·푸시로 원격에 올린다(지시09와 동일 패턴).

---

## §4 배포 (Chief)

빌드 산출물 최신: `apps/web/dist/assets/index-CT1G7iM0.js` 등 (`npm run build` PASS).

권고 명령:

```bash
cd apps/web && npm run build
firebase deploy --only hosting
```

(배포는 Chief 실행 — 이 세션에서 deploy 하지 않음)

---

## §5 감리 검산 체크

- [ ] `git log -1` / `origin/main` / `origin/main2` = `61be6fb`
- [ ] `App.tsx` 에 `aerial5` · `setRideCameraDistanceM(5)` · `aerial10` 없음
- [ ] `.out/지시11/qc-aerial5-badge.png` 육안 — 뱃지 **5**
- [ ] floor ≈ 1.79m · 5m clears
