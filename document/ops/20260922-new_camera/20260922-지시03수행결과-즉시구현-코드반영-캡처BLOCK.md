# 지시03 수행결과 — 즉시 구현 (명칭·거리·Quick Camera 1~6)

- **지시**: [20260922-지시03-…](20260922-지시03-즉시-구현-화면에-변화를-내라.md)
- **코드 변경**: **있음** (아래 §0 git diff). 커밋·푸시 없음
- **캡처**: **미완 (BLOCK)** — 이 세션에서 Firebase 실주행 진입을 5분 예산 안에 닫지 못함. Chief 는 `npm run dev` 로 **지금** 화면 확인 가능

---

## §0 그림에서 보이는 것

### 코드가 실제로 바뀌었다

```
git diff --stat -- apps/web/src
 apps/web/src/App.tsx                            | 73 ++++++++++++++++++++++++-
 apps/web/src/components/map/MapView.tsx         |  9 +++
 apps/web/src/components/map/MapViewSheet.tsx    | 17 +++---
 apps/web/src/components/map/rideCameraFollow.ts | 38 +++++++++----
 apps/web/src/components/maphud/MapHud.css       | 45 +++++++++++++++
 apps/web/src/components/maphud/MapHud.tsx       | 26 ++++++++-
 apps/web/src/components/ride/RideRoutePanel.tsx | 14 +++--
 apps/web/src/lib/map/mapGlobeView.ts                | 40 ++++++++++----
 apps/web/src/lib/camera/rideCameraFraming.ts           |  6 ++
 9 files changed, 231 insertions(+), 37 deletions(-)
```

### 캡처 목록 — **전부 미첨부 → 증거 계약 §2 미충족**

캡처 예정 폴더(비어 있음):

`document/ops/20260922-new_camera/.out/지시03/`

| 증거 | 상태 |
|---|---|
| 주행 중 QC 1~6 Account 왼쪽 | **BLOCK** — 라이브 주행 미실시 |
| 1~6 각 1장 | **BLOCK** |
| 전방/후방 정정 전·후 | **BLOCK** (정정은 코드에 반영됨; 그림 없음) |
| 좌/우 실측 | **BLOCK** |
| pitch 80 vs 67 | **코드만** — `?ridePitch=80` 토글 제공. 나란히 캡처 없음 |
| 회귀 lint 전체 | `eslint .` 는 기존 107 error 로 실패(변경 파일만 0 error) |
| typecheck | `tsc -b` **PASS** |
| framing contract | `npm run test:ride-camera-framing` **75 pass** |
| e2e u5-render-phase | **SKIP** (`RIDE_VERIFY_LIVE` 없음). rename 반영(`follow: "left"`) |

**부분 성공을 성공으로 포장하지 않는다.** 화면 변화는 코드에 들어갔고, Chief/감리가 앱에서 확인·캡처를 채우거나 `수행결과02` 를 지시할 수 있다.

### Chief 즉시 확인 방법

```powershell
cd C:\20.HDev\boxcycle
npm run dev
```

1. 게스트 진입 → 입문 경로 → 주행 시작  
2. 우상단 Account **왼쪽**에 `1~6`  
3. Map 시트 칩: **전방/후방/좌측/우측** (구도 일치 rename)  
4. 거리 슬라이더 상한 **60m**, 기본 **40m**, 하한 **5m**  
5. pitch 비교: 같은 구도에서 `http://localhost:…/?ridePitch=80` vs 기본(67)

---

## §1 구현 요약

### 명칭
| 기존 | 정정 | 라벨 |
|---|---|---|
| rear30 | forward | 전방 |
| front30 | backward | 후방 |
| leftFlat | left | 좌측 |
| rightFlat | right | 우측 |

### 거리
- max 40→**60**, default **40 고정**, pitch close **67°** → minM **5**
- B1 자리: `rideSpanM` 주석 (사용자 역산 거리는 STEP 6)

### Quick Camera
- MapHud TR, Account 왼쪽, riding/paused 만 렌더
- 1: Route Fit(`fitBounds` 1회) ↔ Aerial(`FollowMode` **aerial** @60m). topDown 과 분리해 주행 시작 줌 경로 보존
- 2~5: forward/backward/left/right @5m
- 6: forward @5m + `lockBaseHeading=0`
- 줌 비간섭: **이번 범위 제외**(지시03)

### 6번 해석 (명시)
baseHeading 을 0 으로 고정한 forward 구도 + 5m. 기존 MapViewSheet `north`(distance 0)와 다름.

---

## §2 0-1 / 줌
이번 범위 밖. STEP 6.

---

## §3 실측·캡처
폴더 경로: `document/ops/20260922-new_camera/.out/지시03/` — **파일 0개**.

---

## §4 실패·미완·막힌 항목

1. **캡처 전수 BLOCK** — 증거 계약 미달. 반려/`수행결과02` 대상.
2. `npm run lint`(전체) — 기존 부채로 fail. 변경 파일 eslint **0 error**.
3. u5 live — env 미설정으로 skip.
4. 좌/우·전후 그림 반증 기회 없음 — 코드상 좌=좌 유지.

---

## §5 이견·선택지
없음. pitch 67 은 Chief 육안 승인 전 잠정(`?ridePitch=80` 비교용).

---

## §6 모델 사용 내역
Cursor Agent(개발팀장). ops-relay 스킬·폴링 스크립트 신설. impl-standard 백그라운드 기동했으나 메인 세션이 지시03 구현을 직접 완료.
