# RTW 프로젝트 구조 감사 보고서

- 작성 2026-09-24 · HEAD `3b97160` (`main2`)
- **읽기 전용 조사였다.** 코드·설정·package 를 한 줄도 바꾸지 않았다. 산출물은 이 보고서와 `document/ops/20260924-structure-audit/` 의 갈래별 원본 3건뿐이다.
- 조사 분담: **A** `apps/web/src`(360 파일) · **B** 핵심 기능 영역 10개 · **C** `functions`·설정·문서·테스트
- 갈래별 원본 — [A-앱-인벤토리](../ops/20260924-structure-audit/A-앱-인벤토리.md) · [B-핵심영역](../ops/20260924-structure-audit/B-핵심영역.md) · [C-인프라-설정-문서](../ops/20260924-structure-audit/C-인프라-설정-문서.md)

---

## 1. 구조 요약

| 구역 | 규모 | 한 줄 평 |
|---|---|---|
| `apps/web/src` | 360 파일 · 48,750줄 | 두 개의 거대 파일과 평면 `lib/` 158개가 구조의 전부를 좌우한다 |
| `functions/src` | 106 파일 | 30개(28%)가 일회성 `cli*` 도구. 단위 테스트는 1개 |
| `apps/web/e2e` | 45 스펙 + 헬퍼 3 | 실질 커버리지는 「최근 돌려본 5개」 |
| `apps/web/scripts` | 하네스·계약 테스트 57개 | 스킬 문서가 호출하므로 정적 grep 으로 안 잡힌다 |
| `document/` | 359 md | 색인 규율은 잘 지켜지나 `ops/` 릴레이 8개가 적체 |
| 설정 | firebase ×2 · env ×5 · tsconfig ×4 | **같은 값이 여러 곳에 손으로 복제**되어 있다 |

**전체 형태.** 이 저장소는 「기능은 잘 동작하고 설계 근거는 주석에 남아 있으나, **정리 회수가 한 번도 일어나지 않은**」 상태다. TODO/FIXME 가 0건인 대신 `@deprecated` 28건이 부채를 표시하고 있고, 그중 대다수가 **2026-06-19 Phase 6 이름 이관** 당시 만든 호환 shim 이다. 석 달째 아무도 걷어내지 않았다.

가장 건강한 부분은 `lib/peerMotion/`(유일하게 모듈화된 하위 도메인), `routeDockUiPolicy`(표시 판정 단일 진실), `lib/firebase.ts`(단일 게이트웨이)다. 정비의 목표 형태는 새로 발명할 필요 없이 **이 셋을 따라가면 된다.**

---

## 2. 문제 영역 (Critical → Low)

### Critical — 지금 손해가 발생 중이거나 배포를 깨뜨린다

| # | 문제 | 근거 | 영향 |
|---|---|---|---|
| C1 | **배포 스크립트가 존재하지 않는 함수를 가리킨다** | 루트 `package.json:18,19` 가 `functions:courseActivityOnLiveCourseRideWritten,functions:courseActivityScheduledReconcile` 배포. `functions/src/index.ts:467-470` 은 `routeActivity*` 4개만 export (감리 직접 검산 완료) | `deploy:course-activity-functions`·`deploy:publication-presence` 실행 시 **해당 함수가 갱신되지 않는다**. 성공했다고 착각한 채 구버전이 도는 경로 |
| C2 | **`three` 가 devDependencies 인데 프로덕션 런타임 의존이다** | `apps/web/package.json:86` (`typescript` 바로 위 = devDeps 블록, 감리 검산 완료). 런타임 경로: `MapView.tsx` → `preservedRiderLayer.ts` → `from "three"` | `npm ci --omit=dev` 환경에서 **빌드 붕괴**. 현재 로컬은 dev 설치라 드러나지 않는다 |
| C3 | **테스트가 실제로는 안 돌고 있다** | e2e 45 스펙 중 27개 `test.skip(!LIVE)`, 12개는 어떤 npm 스크립트에도 미등록. 09-24 UI 변경(MapView·MapHud·RouteDock·App)에 대해 09-19 에 멈춘 스펙 4건이 단언 어긋남 고위험. functions 단위 테스트 **1 / 106** | 회귀 감지 수단이 사실상 없다. 「녹색」이 「안 돌았다」와 구분되지 않는다 |

### High — 다음 큰 작업을 막는다

| # | 문제 | 근거 |
|---|---|---|
| H1 | **`MapView.tsx` 4,903줄 · `useEffect` 68개 · props 89 · 내부 상태 96** | 지도·카메라·라이더·피어·오버레이·디버그 6책임 혼재. effect 실행 순서 의존이 암묵적 |
| H2 | **`App.tsx` 3,030줄 · `useState` 47 · import 91** | 주요 훅이 App state 를 **ref 20+개로 역주입**받아 독립 단위가 아니다. `MapView` 와 props 강결합이라 따로 리팩터 불가 |
| H3 | **경로 생성이 3갈래인데 공통 코어가 없다** | `useRoutePlanning`(boolean) · `useDistanceAutoRoute`(8단계 FSM) · `useReadyRide`(3단계 FSM). 공유는 `fetchDistanceAutoRoute` + 에러 포맷터뿐. 취소·토큰·재시도 정책이 3중 구현 |
| H4 | **`lib/` 158 파일 평면** | 도메인 폴더 없음. 순환 4쌍이 전부 여기서 발생 |
| H5 | **설정값이 손으로 복제돼 있다** | `firebase.harness.json` 이 `firebase.json` 의 포트·rules 통째 복제 · 에뮬레이터 포트가 6곳 · `asia-northeast3` 36곳 · Firebase 6키가 env 3곳 · 프로젝트 ID 가 `.firebaserc` 외 30+곳 |

### Medium — 혼란을 만든다

| # | 문제 | 근거 |
|---|---|---|
| M1 | 퇴역 용어 `course` 가 **ACTIVE 코드**에 잔존 | `firestoreCourses.ts` 554줄 · export 26 · **fan-in 16**. `fetchCourseRoutePayload(publicationId)` 처럼 인자는 publication, 이름은 course 인 혼종 |
| M2 | 퇴역 용어 `room`·`lobby` 잔존 | `?room=` 하위호환 · `lobbySpectator*` 15곳 · `firestoreRides.roomId` · **Mapbox layer id `boxcycle-lobby-spectator-*` 7개** |
| M3 | 순환 의존 4쌍 | `activityWorldLod` ↔ `rideSyncPolicy`(fan-in 14/18)가 가장 위험 — 초기화 순서에 따라 상수가 `undefined` 로 읽힐 수 있다 |
| M4 | 레이어링 역전 | `lib/mapGlobeView.ts:2` 가 `components/ride/RideRoutePanel` 에서 `FollowMode` import. **코어가 UI 패널 타입에 의존** |
| M5 | 카메라 거리 클램프 이원화 | 슬라이더는 `RIDE_CAMERA_DISTANCE_MAX_M`(60m) 클램프, `camera1Mode` preset 은 그 상한을 우회해 200m 통과 |
| M6 | 우회 경로(전역 브리지) 3개 | `distanceAutoRouteMapBridge`·`routeTokenSpendBridge`·`mountRouteTokenPopupFeedback` — props 를 안 거치고 맵에 명령을 꽂는다 |
| M7 | `document/ops/` 적체 | 릴레이 폴더 13개 중 8개가 README·색인 모두 없음. 완료분이 `archive/` 로 안 넘어간다 |
| M8 | functions lint 무방비 | `githooks/pre-commit` 이 eslint 설정 없는 `functions/` 를 영구 스킵 → 106 파일이 lint 밖 |

### Low — 청소하면 끝난다

| # | 문제 |
|---|---|
| L1 | DEAD 24건(A), `cliInspect*` 4건·`courseActivity*` alias 3건(C) |
| L2 | `components/` 루트 1줄 shim 10개(살아있음) + DEAD shim 4개 + 빈 디렉터리 `components/route/` |
| L3 | `console.log` 7줄(`[C1]`~`[C4]`·`[PhaseD]` 카메라 디버깅 잔재) |
| L4 | 루트 부산물 `0`·`debug.log`·`firestore-debug.log` |
| L5 | 문서 불일치 3건(색인 누락 2 + URL 인코딩 깨진 링크 1) |
| L6 | 용량 483MB — `.out/` 276MB + `tmp/rider-shape-preserving/` 207MB(실제 git worktree) |

---

## 3. 파일 분류표 (집계)

### `apps/web/src` — 360 파일

| 라벨 | 수 | 비고 |
|---|---|---|
| ACTIVE | ~232 | |
| SUPPORT | ~46 | 빌드 스크립트·타입 선언·리그 자산 |
| DUPLICATE | 14 | 루트 1줄 shim(살아있는 10 + DEAD 4) |
| DEAD | 24 | 도달 불가 + 저장소 전체 grep 0건 교차 확인 |
| LEGACY | 17 | `@deprecated` 28 마커가 걸린 파일 |
| EXPERIMENTAL | 9 | lightlab · DebugWorldLightMap · LOD 디버그 패널 |
| TEST | 1 | `lib/sensorChipSlot.ts` — 계약 테스트 전용 프로덕션 코드 |
| CONFIG | 4 | `.d.mts` ×3 · `vite-env.d.ts` |
| UNKNOWN | 3 | `FirstRideIntroCard`(유지 지시 있음) · lightlab · 디버그 3종 |

### `functions/src` — 106 파일

| 라벨 | 수 | 비고 |
|---|---|---|
| ACTIVE/SUPPORT | ~96 | `*Core.ts` 30여 개 + `cli*` 26 |
| LEGACY | 5 | `courseActivity*` alias (2~4줄) |
| DEAD | 4 | `cliInspect*` — 코드 참조 0 + npm 스크립트 없음 |
| TEST | 1 | `distanceAutoRouteCore.test.ts` |

> **조사 신뢰도 주의.** `tmp/rider-shape-preserving/` 에 프로젝트 전체 사본이 있어 순진한 `grep -rn` 은 참조 수를 부풀린다. A 갈래는 이를 인지하고 배제했으나, 정비 착수 시 **모든 재확인 grep 에서 `tmp/` 를 제외**해야 한다.

---

## 4. 중복

| 중복 | 내용 | 정비 난도 |
|---|---|---|
| **루트 re-export shim 14개** | `components/X.tsx` = `export * from "./<도메인>/X"` 한 줄. 10개는 `App.tsx`·`AppMapStage` 가 실사용 중 | 쉬움 (import 경로 치환). **단 `MapillaryRideViewer` 는 `App.tsx:162` 동적 import** |
| **`courseActivity*` ↔ `routeActivity*`** | functions 5개 alias 파일. 코드 참조 0, 배포 스크립트 참조만 | 쉬움 (C1 과 함께) |
| **Phase 6 클라이언트 shim 6개** | `useCourseActivity`·`useCourseActivityMapOverlay`·`useOfficialCoursesHub`·`firestoreCourseActivity`·`firestoreCoursePresence`·`CourseSharedPresence` — 전부 참조 0 | 쉬움 |
| **`firebase.harness.json` ↔ `firebase.json`** | rules·indexes·database·emulators 포트 전부 복제. 차이는 functions source 경로 1줄 | 중간 |
| **Firebase 6키 × env 3파일** | `.env`·`.env.emulator`·`.env.harness` | 중간 |
| **에뮬레이터 포트 × 6곳** | firebase ×2 · env ×2 · `playwright.config.ts` · `vite.config.ts`. 포트 5000/5002 규칙이 두 파일에 **각각 따로 구현** | 중간 |
| **`asia-northeast3` × 36곳** | functions 19 + web 3 + env | 중간 |
| **`admin:*` 스크립트** | functions 28개 중 루트에 13개만 프록시. 정본 불명 | 쉬움 |
| **publish flight 2벌** | `peerMotion/motionPublishFlight` ↔ `routePublishFlight` 거의 같은 구조 | 어려움 |
| **구독 허브 3벌** | `activeLiveRideTrailIds*`·`livePublicationRides*`·`rtdbMotion*` 이 같은 refcount 패턴 반복 구현 | 중간 |
| **`rideRecordPolicy.ts` 클라/서버 2벌** | 동기화 계약이 코드로 강제되지 않는다 | 중간 |
| **Sheet/Panel/Modal 삼중** | `RideSettingsSheet`(57L)가 `RideSettingsPanel`(63L)을 감쌈 | 쉬움 |
| **3D 라이더 레이어 2구현** | `glbModelLayer.ts` ↔ `preservedRiderLayer.ts` | 중간 |

---

## 5. Legacy

**뿌리는 하나다 — 2026-06-19 Phase 6 이름 이관.** 그때 만든 호환 shim 이 석 달째 회수되지 않았고, 삭제 후보의 대부분이 이 날짜에 몰려 있다.

| 계열 | 잔존 위치 | 상태 |
|---|---|---|
| `course` → `route`/`publication` | 파일명 10 · 내용 46 파일. `firestoreCourses.ts`(fan-in 16)·`usePublishedCoursesActivityMapOverlay.ts`(414L)·`OfficialCourseListModal.tsx` 는 **ACTIVE** | 부분 이관. **이름만 옛것** |
| `room`/`lobby` → `trail` | `?room=` 하위호환 · `lobbySpectator*` 15곳 · `roomId` 필드 · **Mapbox layer id 7개** | Firestore 경로는 완료, **코드 식별자 미완료** |
| `rideSyncPolicy` alias 5개 | fan-in 18 핵심 모듈 내부 | 점진 제거 대상 |
| `activityWorld*` alias 3개 | 순환 M3 의 원인 | 이관 중단 상태 |
| Firestore 규칙 잔존 컬렉션 | `rooms`·`worldActivity`·`pioneerChunks`·`livePresence` | 규칙만 남았을 가능성 |
| 완료된 마이그레이션 `cli*` | `cliMigrateRoomsToTrails`·`cliPhase7TerminologyPurge`·`cliPurgeConquestV1` 등 | 기록 가치 있음 → `archive/` 분리 후보 |

---

## 6. Dead 후보

### 즉시 삭제 가능 (참조 0건 이중 확인 완료)

**클라이언트 — Phase 6 shim 6**: `CourseSharedPresence.tsx` · `useCourseActivity.ts` · `useCourseActivityMapOverlay.ts` · `useOfficialCoursesHub.ts` · `firestoreCourseActivity.ts` · `firestoreCoursePresence.ts`

**클라이언트 — 고아 8**: `useTrailLivePublicationRidePublisher.ts`(이미 HANDOFF 기록됨) · `virtualRideDuration.ts` · `TrailSwitcher.tsx`+`.css` · `TrailheadPresence.tsx`(**`.css` 는 살아있음 — tsx만**) · `useWorldActivityCatalog.ts` · `components/weather/WeatherOverlay.tsx`+`.css`

**클라이언트 — 배럴·shim·빈 폴더 9**: `app/index.ts` · `components/{auth,map,trail}/index.ts` · 루트 DEAD shim 4(`RideHistoryPanel`·`SavedRoutesPanel`·`TrailSwitcher`·`TrailheadPresence`) · 빈 디렉터리 `components/route/`

**functions 4**: `cliInspectLivePresence.ts` · `cliInspectRoutePublications.ts` · `cliInspectSavedRouteFingerprints.ts` · `cliInspectTrailMembers.ts`

**부산물 3 + 주석 1줄**: `./0` · `./debug.log` · `./firestore-debug.log` · `publicRouteAutoReview.ts:14`

### 판단 보류 — 지우지 말 것

| 대상 | 이유 |
|---|---|
| `components/ride/FirstRideIntroCard.tsx` + `.css` | 기계적으로는 DEAD 이나 **`20260923-first_ride/지시01` 이 「지우지 마라」 명시**. 재도입 예정 자산 |
| `lib/openMeteoWeather.ts` | 소비자가 DEAD 컴포넌트뿐이나 날씨 기능 복원 계획 확인 필요. e2e stub 주석이 API 한도를 공유한다고 기록 |
| `components/ride/RideHistoryPanel.tsx` + `.css` | 주행 기록 UI 재도입 예정이면 보존 |
| `functions/src/courseActivityOnLiveCourseRideWritten.ts`·`ScheduledReconcile.ts` | **배포 스크립트가 붙잡고 있다.** C1 을 먼저 결정해야 삭제 가능 |
| `lib/sensorChipSlot.ts` | 계약 테스트 전용이지만 살아있는 계약이다 |
| `tmp/rider-shape-preserving/` | **실제 git worktree**(브랜치 `codex/rider-shape-preserving` @ `d386b78`). `rm -rf` 하면 메타가 고아가 된다 — `git worktree remove` 로만 |

---

## 7. 구조적 문제 (왜 이렇게 됐나)

1. **책임의 중력점이 두 파일에 있다.** `MapView.tsx`·`App.tsx` 가 각각 6·5 책임을 떠안으면서, 새 기능이 붙을 자리가 언제나 이 둘이 된다. 2026-09-24 추가분 5건 중 4건은 깨끗이 「얹혔」지만 그 얹힌 자리는 결국 이 두 파일이다.
2. **경계가 없어서 우회 경로가 자란다.** props 를 거치지 않는 전역 브리지 3개, nonce 기반 명령 채널 2개, `DebugMapStage` 라는 병렬 맵 스택 — 전부 「정식 경로가 너무 무거워서」 생긴 샛길이다.
3. **얹기는 잘하고 걷기는 안 한다.** Phase 6 이관에서 새 이름을 세우는 작업은 완수됐다. 옛 이름을 걷어내는 후반부만 없다. `@deprecated` 28건은 「지울 계획」이 아니라 「지우지 않은 기록」이 됐다.
4. **설정의 단일 진실이 없다.** 포트·리전·프로젝트 ID·Firebase 키가 손으로 복제돼 있고, 한쪽만 고쳐도 아무 에러가 나지 않는다. 드리프트가 **조용히** 일어난다.
5. **회귀 감지가 고장 나 있어서 3·4 가 발각되지 않는다.** C1(배포 no-op)·C2(dev/prod 오분류)는 둘 다 「돌려보면 바로 알 수 있는」 종류인데, 돌려보는 장치가 없다.

---

## 8. 목표 구조

새로 발명하지 않는다. **저장소 안에 이미 있는 모범을 확장한다.**

| 목표 | 본보기 | 지금 |
|---|---|---|
| 도메인별 하위 폴더 | `lib/peerMotion/`(14 파일, 잘 모듈화) | `lib/` 158 평면 |
| 판정의 단일 진실 | `routeDockUiPolicy`(dock + sensorChipSlot 공유) | 카메라 클램프 2벌, 경로 정책 3벌 |
| 단일 게이트웨이 | `lib/firebase.ts` | 컬렉션 경로 문자열은 파일마다 |
| 주입 가능한 커널 | `lib/rideEndPersistence.ts`(테스트 가능) | 대부분의 훅이 App 클로저 확장 |

### `lib/` 도메인 분할안

```
lib/
  geo/          geo.ts(fan-in 83) · routeWaypoints · routeFingerprint
  firebase/     firebase.ts · firestore*.ts 25개 · rtdb*
  ride/         ride*.ts 24개 · rideEndPersistence · rideRecordPolicy
  route/        route*.ts 17개 · distanceAutoRoute*.ts 6개 · readyRide
  trail/        firestoreTrail* · trailAccessPolicy · trailUrl
  conquest/     conquestTiles · firestoreConquest · conquestLayerEmphasis
  map/          mapGlobeView(+FollowMode 이동) · mapBootCenter · rtwMapConfig
  camera/       rideCameraFraming · camera1Mode · cameraFollowTrace
  rider/        riderPrototype/* (현행 유지)
  peerMotion/   (현행 유지 — 본보기)
  debug/        install*Debug · *Probe · *Phase  ← 빌드 플래그 게이팅 후보
```

순환 4쌍은 이 분할 과정에서 자연 해소된다(전부 평면 구조가 원인).

### 경로 생성 3갈래의 목표 형태

```
공통 코어 (취소 · 토큰 · 재시도 · 에러 정규화)
   ├── 클릭 A→B        (mapboxDirections)
   ├── 거리 자동        (distanceAutoRouteHttp)
   └── Ready Ride       (distanceAutoRouteHttp + 지역 시드)
```

지금 셋이 공유하는 건 결과 적용 지점(Go 게이트)뿐이다. **입구를 늘리는 건 괜찮지만 정책은 하나여야 한다.**

---

## 9. 정비 실행 계획

> 각 단계는 앞 단계가 끝나야 시작한다. **Phase 0 없이 Phase 2 이후로 넘어가면 안 된다** — 회귀를 볼 수 없기 때문이다.

### Phase 0 — 지혈 (반나절, 기능 변화 0)

C1·C2 는 **지금 손해가 나고 있다.** 다른 어떤 정비보다 먼저 한다.

1. 루트 `package.json:18,19` 의 `courseActivity*` → `routeActivity*` 로 수정 (또는 스크립트 폐기 결정)
2. `apps/web/package.json` 의 `three` 를 `devDependencies` → `dependencies` 이동
3. `.gitignore` 에 `.playwright-mcp/` 추가
4. 루트 부산물 삭제 — `0` · `debug.log` · `firestore-debug.log`
5. `document/README.md` 링크 2줄 수정(URL 인코딩 + 색인 누락)

**검증**: `npm ci --omit=dev && npm run build` 가 통과하는지 / `firebase deploy --only "functions:routeActivity..." --dry-run`

### Phase 1 — 안전망 (1~2일)

정비를 시작할 자격을 만드는 단계다.

1. 09-19 에 멈춘 e2e 4건(`route-dock-riding-content`·`ride-hierarchy-narrow`·`menu-declutter-a`·`ride-summary-compact`)을 **돌려보고** 실패면 현행 UI 기준으로 갱신
2. npm 스크립트 미등록 12 스펙을 등록하거나 명시적으로 폐기
3. `MapView.tsx`·`App.tsx` 에 대한 **계약 테스트 최소 세트** 신설 — 이 둘을 건드리기 전 필수
4. `functions/` eslint 설정 추가 → pre-commit 스킵 해제

### Phase 2 — 청소 (반나절, 위험 0)

1. DEAD 클라이언트 23건 삭제 (`TrailheadPresence.css` 제외, 보류 3건 제외)
2. DEAD functions 4건(`cliInspect*`) 삭제
3. `courseActivity{AggregateCore,HeatReconcile,OnRideCreated}.ts` 3건 삭제 — 나머지 2건은 Phase 0 결과에 따라
4. `console.log` 7줄 제거
5. 주석 처리 export 1줄 제거
6. 빈 디렉터리 `components/route/` 제거

**검증**: typecheck + Phase 1 의 계약 테스트

### Phase 3 — 경로 정리 (1일)

1. 루트 1줄 shim 10개 — 소비자 import 경로를 실제 경로로 치환 후 삭제. **`MapillaryRideViewer` 는 `App.tsx:162` 동적 import 라 수동 확인**
2. `FollowMode` 를 `components/ride/RideRoutePanel` → `lib/mapGlobeView.ts` 로 이동 (레이어링 역전 해소, 참조 6곳)
3. `RiderPrototypeMode` 의 도달 불가 `"legacy"` 분기 제거
4. `lobby` 변수·식별자 정리 — **Mapbox layer id 문자열 7개는 런타임 영향이 있으니 별건**

### Phase 4 — 설정 단일화 (1~2일)

1. 에뮬레이터 포트·리전·프로젝트 ID 를 단일 모듈로 모으고 6곳에서 참조
2. `firebase.harness.json` 을 `firebase.json` 기반 생성으로 전환(또는 차이만 명시)
3. Firebase 6키의 env 3중 정의 정리
4. `database.rules.json` 의 peer-sync 패킷 스키마와 `lib/peerMotion` 을 **테스트로 묶기**

### Phase 5 — 구조 (별건 계획 필요)

여기서부터는 이 보고서 범위를 넘는다. 착수 전 별도 계획 + 결정 로그를 거친다.

- `lib/` 도메인 분할 (§8)
- 경로 생성 3갈래의 공통 코어 추출
- `MapView.tsx` 책임 분할 — Phase 1 계약 테스트가 없으면 시작 금지
- `App.tsx` 상태 분해 — ref 역주입 해소
- `course` 일괄 개명 — **Ontology §0.1 에 따라 rename 은 결정 로그를 거친 별도 계획으로만**

---

## 10. 기능 보존 위험

정비 중 **조용히 기능이 죽을 수 있는 지점**이다. 각 항목은 해당 Phase 에서 명시적으로 확인한다.

| # | 위험 | 왜 조용한가 | 방어 |
|---|---|---|---|
| R1 | `TrailheadPresence.css` 동반 삭제 | `.tsx` 는 DEAD 이나 `.css` 는 `PublicationSharedPresence.tsx:39` 가 import 중. 빌드는 통과하고 **스타일만 깨진다** | `.tsx` 만 삭제, CSS 참조 재확인 |
| R2 | `MapillaryRideViewer` shim 제거 | `App.tsx:162` **동적 import** 라 정적 리팩터 도구·typecheck 가 못 잡는다. 실행 시점에야 터진다 | 수동 확인 + 실기 |
| R3 | `FirstRideIntroCard` 삭제 | 기계적으로 DEAD 인데 **유지 지시가 문서에만 있다** | 삭제 금지 목록에 고정 |
| R4 | `tmp/` 사본이 grep 을 오염 | 참조 수가 부풀려져 「살아있다」고 오판 → 정작 살아있는 걸 지울 수도, 죽은 걸 남길 수도 | 모든 재확인 grep 에서 `tmp/` 제외 |
| R5 | 카메라 200m 단계 | 클램프를 「일원화」하면 preset 우회 경로가 막혀 **200m 가 조용히 60m 로 잘린다** | Phase 4 에서 우회가 의도임을 명시 후 처리 |
| R6 | Mapbox layer id 변경 | `boxcycle-lobby-spectator-*` 를 이름만 바꾸면 소스·레이어 참조가 어긋나 **오버레이가 사라진다**. 타입 에러 없음 | 별건 처리, 실기 확인 필수 |
| R7 | `rideRecordPolicy` 클라/서버 2벌 | 한쪽만 고치면 주행 기록 인정 규칙이 갈라진다. 에러 없이 **데이터가 달라진다** | Phase 4 에서 테스트로 묶기 |
| R8 | `database.rules.json` 패킷 스키마 | `peerMotion` 필드를 바꾸면 RTDB write 가 거부된다. **동행 라이더만 안 보이고 앱은 정상** | Phase 4 에서 테스트로 묶기 |
| R9 | 순환 `activityWorldLod` ↔ `rideSyncPolicy` | import 순서가 바뀌면 상수가 `undefined`. 값이 0 이나 NaN 으로 흘러 **폴링이 멈추거나 폭주** | Phase 5 분할 시 값 검산 게이트 |
| R10 | `conquestClaimRead` ← `distanceAutoRouteHttp` | Claim 읽기가 Route 생성 CF 내부 의존. 경로 생성기를 통합할 때 **이 의존을 같이 옮기지 않으면 새 도로 비율이 조용히 0 으로 돌아간다** | Phase 5 에서 명시 |
| R11 | `window.__rtw*` 40종 게이팅 | 빌드 플래그로 막으면 **하네스·e2e 가 같이 죽는다** | 게이팅은 기능 변경 — 별건 |
| R12 | 완료된 마이그레이션 `cli*` 이동 | `archive/` 로 옮기면 `admin:*` 스크립트 경로가 깨진다 | 스크립트 동시 수정 |

---

## 부록 — 이번 감사에서 지킨 것

- 코드·설정·package **변경 0**. `git status` 는 미추적 `0`(조사 전부터 존재) 과 본 감사 산출 폴더만.
- 「안 쓰이는 것 같다」를 근거로 쓰지 않았다. 삭제 후보는 전부 **도달성 분석 + 저장소 전체 grep** 이중 확인을 거쳤고, 근거가 부족한 6건은 「판단 보류」로 남겼다.
- 좌표·주소·이메일·uid·환경변수 값은 하나도 기재하지 않았다. 환경변수는 **키 이름과 용도만** 적었다.
- Critical 2건(C1 배포 스크립트 · C2 `three` 오분류)은 감리가 **직접 파일을 열어 재확인**했다.
