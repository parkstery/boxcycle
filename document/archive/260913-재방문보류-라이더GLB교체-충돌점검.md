# 충돌 점검 보고 — 재방문 실험 보류 vs 라이더 GLB 교체

| 항목 | 내용 |
|---|---|
| 문서 유형 | **archive** — 수퍼바이저 충돌 점검 |
| 작성 | 2026-09-13 |
| 결정 | 재방문 실험(4단계) 및 이후 단계 **무기한 보류**. 라이더 GLB 교체 진행 |
| 결론 | **하드 충돌 없음.** 소프트 주의 2건(아래). GLB 교체 착수 가능 |

## 1. 검증 방법

| # | 방법 | 도구·대상 | 결과 |
|---|---|---|---|
| V1 | 열린 PR·브랜치 목록 | `gh pr list --base main2 --state open` | PR #18만 OPEN (`RIDE-RETURN-EXPERIMENT-4`) |
| V2 | 최근 병합 트랙 파일셋 | `gh pr diff 17 --name-only` / merge `5beb1a2` | MapHud·RouteDock·Publication·narrow e2e — **riderPrototype/GLB 경로 없음** |
| V3 | 4단계 PR 파일셋 | `gh pr diff 18 --name-only` | 이벤트·센서 호환 UI·운영 초안 — **제품 GLB·rig·MapView 없음** |
| V4 | 경로 교집합 | PR #18 경로 vs Modelling 계획 표면(`public/rider/prototype/`, `riderPrototype/`, `riderGlbPedalPose*`, `MapView.tsx`, `scripts/rider-cycle-fit/`, `scripts/rider-preview/`, `blender/rider-cycle-fit/`) | HARD 겹침 **0**. SOFT: `App.tsx`(이벤트만)·`package.json`(test 스크립트) |
| V5 | import 경계 | ripgrep `riderPrototype\|rider-lowpoly\|glbModelLayer\|riderGlbPedalPose` on MapHud·RouteDock·rideReturnExperiment | **매치 0** |
| V6 | 역방향 | `MapView.tsx`에서 RideSummary/NextRide/routeDockUiPolicy/rideReturnExperiment import | **매치 0** |
| V7 | 제품·계획 SoT 열람 | `260910-Modelling-라이더-교체-실행계획.md` §2–§4, `260912-…인계조건.md`, rider-cycle-fit SKILL | 교체면 = GLB+14노드+rig/geometry/fit 하네스. 도로망 성취 UI와 역할 분리 |
| V8 | 로컬 WIP | `git status` | Modelling 문서·`register-modelling-baseline.mjs`·inspect 스크립트만 uncommitted — ride PR과 경로 분리 |
| V9 | 자산 존재·해시 | `Get-FileHash` 제품 `rider-lowpoly.glb` · 신규 `roadcyclist_straight_upper_arms.glb` 존재 여부 | 제품 파일 존재. 신규 입력 경로는 인계 문서 기준(수신 시 재해시 필수) |

## 2. 충돌 판정

| 현재 작업 | GLB 교체와 | 조치 |
|---|---|---|
| Hosting의 2·3단계(idle 카드·주행 HUD) | **무충돌** — UI만, GLB 로더는 MapView | 유지 |
| PR #18 재방문 실험 | **무충돌(코드)** · **우선순위 충돌(일정)** | 무기한 보류 → PR 닫기·머지 안 함 |
| 실행 계획 §4·§5 | 보류 | 지시서 상태 갱신 |
| 로컬 Modelling WIP | **같은 트랙** — 교체 작업의 일부 | GLB 브랜치에서 정리·커밋 |
| 동행·peer-sync | 동일 GLB 소비 가능 | 교체 E단계에서 내 라이더·동행 둘 다 확인(계획 §D·E) |
| 카메라/거인 스케일 상수 | rig·AABB 바뀌면 연동 가능 | 교체 D단계에서 기존 giant/scale 회귀 포함 |

## 3. 소프트 주의

1. **`App.tsx`**: 4단계를 나중에 재개하면 GLB 브랜치와 각각 App 수정이 쌓일 수 있음. 지금은 4단계 미머지로 해소.
2. **`package.json` test 스크립트**: 동일. 교체 PR은 rider-cycle-fit/preview 스크립트만 건드리면 겹침 최소.

## 4. 권고

- PR #18 **머지하지 않고 close**(보류 사유 기록).
- 라이더 교체는 `cursor/…` 전용 브랜치 + rider-cycle-fit 단계 0부터. 제품 `rider-lowpoly.glb`는 승인 전 덮지 않음.
- 신규 입력은 `260912`의 `straight_upper_arms` 해시와 **수신 시 재대조** 후 단계 0 등록.
