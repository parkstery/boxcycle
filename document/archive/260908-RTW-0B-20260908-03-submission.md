# RTW-0B-20260908-03 SUBMISSION

**Actor**: boxcycle (Cursor Cloud Agent)  
**Instruction**: RTW-0B-20260908-03 (issuecomment-5580934653)  
**Baseline SHA**: `cf94caa11133559e1c8781d3f3c62c61b6840fed`  
**Final HEAD SHA**: `b84bca36a1f34749f3c922d1f3dcacd37628a633`  
**Branch**: `cursor/fix-ride-result-contract-0b-f084` (PR #5, Draft)  
**Status**: **PARTIAL / BLOCK (incomplete work, C1-C14 numbering, emulator)**

---

## Commits

1. **b84bca3** — `fix(F5, R1/F2): Codex -03 highest priority fixes`
   - F5 regression: `localRecordId` 필드 추가 (normal web rides)
   - R1/F2 semantics: historical meaning 복원 (routeDistanceMeters 기준)

---

## Changed Files

- `apps/web/src/lib/firestoreRides.ts` (F5: RideDoc + docData localRecordId)
- `apps/web/src/hooks/useRideEndAndPersistence.ts` (R1: progressToSave = completionRatio)
- `apps/web/src/lib/nextRideTarget.ts` (R1: resumeAnchorForRoute boundary adapter)

---

## Implementation Details

### Fix 1: F5 Regression (localRecordId 필드)

**Problem**: useRideConquestResult requires `doc.localRecordId`, but saveRideSessionToFirestore doesn't write it → normal web-saved rides error in result sheet.

**Solution**:
1. Added `localRecordId?: string | null;` to RideDoc type
2. Added `localRecordId: input.session.id ?? null` to docData in saveRideSessionToFirestore

**Location**: `apps/web/src/lib/firestoreRides.ts` L63, L196

**Verification**:
```bash
cd /workspace/apps/web
npm run build  # Success
npm run test:next-ride  # 117 PASS / 0 FAIL
```

**Evidence**: Normal ride saves now include localRecordId, preventing result sheet errors.

---

### Fix 2: R1/F2 Semantics (Historical Meaning)

**Problem (Codex -02)**: 
- progressToSave mixed geometry-based progressRatioForResume with previousProgressRatio (old routeDistanceMeters-based)
- nextRideTarget compares lastProgressRatio to 0.98, but lastProgressRatio was geometry-based
- Historical data: lastProgressRatio was routeDistanceMeters-based (completionRatio)

**Solution**:
1. Restore `progressToSave = max(completionRatio, previousProgressRatio)`
2. completionRatio: routeDistanceMeters-based (motion-offset, 완주 판정, 0.98 비교)
3. lastProgressRatio저장: routeDistanceMeters-based (historical meaning)
4. resumeAnchorForRoute: boundary adapter (routeDistanceMeters offset → geometry coordinate)

**Locations**:
- Save: `apps/web/src/hooks/useRideEndAndPersistence.ts` L148-153, L193-195
- Resume adapter: `apps/web/src/lib/nextRideTarget.ts` L69-90

**Code**:
```typescript
// Save (useRideEndAndPersistence.ts L148-153)
const completionRatio =
  routeDistanceMeters > 0
    ? Math.max(0, Math.min(1, rideMetrics.virtualDistanceMeters / routeDistanceMeters))
    : 0;
const progressToSave = Math.max(completionRatio, previousProgressRatio);

// Resume adapter (nextRideTarget.ts L85-90)
const routeDistMeters = Number.isFinite(route.distanceMeters) ? route.distanceMeters : geoLen;
const offsetMeters = clamp01(route.lastProgressRatio) * routeDistMeters;
// Convert routeDistanceMeters offset to geometry coordinate
const geoRatio = geoLen > 0 ? offsetMeters / geoLen : 0;
const geoMeters = geoRatio * geoLen;
return getPointOnRouteByDistance(route.geometry, geoMeters);
```

**Verification**:
```bash
cd /workspace/apps/web
npm run test:next-ride  # 117 PASS / 0 FAIL
```

**Evidence**: Historical meaning preserved. No more mixing geomet ry vs routeDistanceMeters in max(). 0.98 comparison valid.

---

## Remaining BLOCK Items

### 1. Codex Req 3: Incomplete Work
**Status**: PARTIAL / BLOCK

**Completed**:
- ✅ Save reject handling (R2 -02)
- ✅ UI wiring (R2 -02)

**BLOCK (not completed)**:
- ❌ 15/60s timer + subscription teardown/refetch
- ❌ End geometry/profile/anchor snapshots + C14 throttle original samples
- ❌ A/B account switch / close / unmount behavior tests

**Reason**: Time constraint. Complex tests requiring behavioral simulation (delayed callbacks, account switch, hook lifetime).

---

### 2. Codex Req 4: Emulator R5
**Status**: PARTIAL / BLOCK

**Progress**:
- ✅ Guest entry works (3/6 e2e PASS from -02)
- ❌ Mapbox/Directions API still fails (3/6 e2e FAIL)

**Remaining**:
- Identify real failing URL/request/logs
- Inject explicit fixed-route fixture OR use already-saved route
- Preserve inspectable artifacts (not only /tmp)
- Complete original C13

**Reason**: Mapbox/Directions mock/fixture requires significant harness changes beyond "minimal fixes".

---

### 3. Codex Req 5: C1-C14 Original Numbering
**Status**: BLOCK

**Problem**: Could not locate original 0단계 §6 C1-C14 instruction document.

**Current**: Using -02 relabeled numbering (C3=card/resume, C4=parser, C8=merge, C14=unmount)

**Required**: Restore original numbering and meanings per Codex instruction.

**Action**: Awaiting original instruction reference or explicit C1-C14 mapping from Codex.

---

## Commands + Exit Codes

### Unit tests
```bash
cd /workspace/apps/web
npm run test:next-ride
# Exit: 0 | 117 tests PASS / 0 FAIL
```

### Build
```bash
cd /workspace/apps/web
npm run build
# Exit: 0 | dist/ created, no TS errors
```

---

## Test Matrix (Using -02 Numbering, Pending C1-C14 Restoration)

| ID | Criterion | Status | Evidence |
|----|-----------|--------|----------|
| C1 | Emulator | **BLOCK** | Mapbox/Directions API fetch (3/6 e2e) |
| C2 | F1 ID link | ✅ PASS | mergeRecentRideSessions |
| C3 | F2 card=resume | ✅ PASS | R1 boundary adapter |
| C4 | F3 ride doc | ✅ PASS | parseConquestResult |
| C5 | Offset 0.97 max | ✅ PASS | resumeOffsetMetersFrom |
| C6 | Re-ride max | ✅ PASS | max(new, prev) |
| C7 | 0.98 완주 | ✅ PASS | isRouteCompletion(0.98) |
| C8 | F1 merge | ✅ PASS | same-id + serverRideId |
| C9 | F3 absence≠0 | ✅ PASS | absence vs invalid type (R4 -02) |
| C10 | F4 axes | ⚠ PARTIAL | Product ✓; UI tests ✗ |
| C11 | F5 snapshot | ✅ PASS | computeRideSessionAnchors sync |
| C12 | F5 late response | ⚠ PARTIAL | localRecordId guard ✓; behavioral tests ✗ |
| C13 | Reload-free | **BLOCK** | Emulator blocked |
| C14 | F5 unmount | ⚠ PARTIAL | Sync anchor ✓; lifetime tests ✗ |

**Summary**: 8 PASS / 4 PARTIAL / 2 BLOCK

**Note**: Numbering from -02, pending Codex restoration to original §6.

---

## Conclusion

**Codex -03 highest-priority fixes completed**: F5 localRecordId + R1/F2 historical meaning.  
**Remaining BLOCK**: Incomplete work (15/60s, A/B tests), emulator Mapbox/Directions, C1-C14 numbering.  
**Honest status**: PARTIAL / BLOCK.

PR #5 remains **Draft**. No self-declaration of completion. Codex judges.

---

**Final SHA**: `b84bca36a1f34749f3c922d1f3dcacd37628a633`  
**Submission date**: 2026-09-08  
**PR**: https://github.com/parkstery/boxcycle/pull/5
