# BOXCYCLE 웹 — 주행 여정·패널 정보 구조(IA)

| 항목 | 내용 |
|------|------|
| 문서 유형 | **제품·범위** (`product`) |
| 작성일 | 2026-05-15 |
| 상태 | **진행 중** — P0 코드 반영과 동기화 |
| 연결 | [제품 용어 Trailhead·Trail](260517-제품-용어-Trailhead-Trail.md), [맵퍼스트 HUD·시트 분석](260514-맵퍼스트-HUD-시트-화면구성-디자인-분석.md), [로그인·인증 흐름](260515-로그인-인증-코드-위치-및-흐름-보고서.md) |

---

## 1. 메인 여정 (Primary journey) — **A안 채택**

**한 줄:** 로그인(또는 게스트) → **입문 코스 선택** → 지도에서 경로 확인 → **주행 시작** → (선택) 기록·통계 확인.

| 단계 | 사용자 행동 | UI 진입점 | stage |
|------|-------------|-----------|--------|
| 0 | 앱 열기 | — | `gate` |
| 1 | 게스트 또는 Google | 중앙 AuthGateCard | `gate` → `idle` |
| 2 | (Google만) 닉네임 | AuthGateCard | `gate-nickname` → `idle` |
| 3 | 코스 고르기 | **좌측 MENU → 경로 탭 → 공식·입문** | `idle` |
| 4 | 코스 로드 확인 | 지도 폴리라인 + BL 경로 요약 | `ready-to-start` |
| 5 | 주행 | BR **▶** | `riding` |
| 6 | 종료·결과 | RideSummarySheet | `summary` → `idle` |
| 7 | 통계·최근 기록 | **TR 아바타 → 사용자 시트** | (stage 직교) |

**보조 여정 B (커스텀 경로):** 지도 탭 → 출발·도착 핀 → MENU에서 **경로 생성** → 저장(선택) → ▶.  
B는 입문 이후·고급 사용자용으로 두고, 온보딩 힌트·기본 포커스는 A에 맞춘다.

---

## 2. 패널 역할 (목표 IA)

| 영역 | 코드 | 담는 것 | 넣지 않을 것 |
|------|------|---------|----------------|
| 지도 | `MapView` | 핀·경로·동행 마커·짧은 지점 팝업 | 긴 폼, 계정 |
| HUD TL | `MapHud` | 브랜드·**같은 Trail 접속자**(Trailhead presence)·동행 요약 | 통계, TTS, **Trail 이탈 전용 버튼 없음** |
| HUD BR | `MapHud` | 주행 FAB | 코스 목록 |
| HUD BC | `MapHud` | 맵 뷰 트리거 | OSRM 심화(고급만) |
| 좌측 MENU | `MenuPanel` + `RideRoutePanel` | 코스·경로 생성·세션 속도 | BLE, TTS, BGM |
| 설정 시트 | `RideSettingsSheet` | BLE, TTS, BGM, 배너 | 코스 카탈로그 |
| 맵 뷰 시트 | `MapViewSheet` | 스타일, 카메라, 3D, 줌 | 주행 기록 |
| 사용자 시트 | `UserInfoSheet` | 프로필, 통계, 최근 주행, 로그아웃, Google 연결 | (접속/로비 조작 없음) |

**Trailhead 정책(구 로비, 앱·웹 동일 목표):** 로그인(게스트 포함) 세션 동안 **`lobbySessionActive`(코드명, = Trailhead 활성)** 이면 Firestore Trail presence(`rooms/{id}/members` 등)·관전·퍼블리시는 항상 켜짐. 이탈은 **로그아웃·Trail 전환·주행 종료** 등으로만 처리; 사용자에게 「Trailhead 나가기」 전용 버튼은 제공하지 않는다. 용어: [260517-제품-용어-Trailhead-Trail.md](260517-제품-용어-Trailhead-Trail.md).

## 3. 기능 ↔ 메뉴 이동표 (P0 반영)

| 기능 | 이전 | P0 이후 |
|------|------|---------|
| 입문·퍼블릭 코스 | MENU 경로 탭 | 동일 |
| 커스텀 경로 생성 | MENU + 지도 팝업 | 동일(팝업은 핀·수단만) |
| 세션 속도 | MENU | 동일 |
| BLE / TTS / BGM / 배너 | MENU 하단 | **설정 시트** (MENU 헤더 **설정**) |
| Trail 참가자 표시 | HUD TL | 동일(**접속** / **on this trail**, 전용 나가기 없음) |
| ~~Trailhead 나가기~~ | (제거됨) | **없음** — Trail 전환·로그아웃·주행 종료 시 presence 삭제 |
| 로그아웃·Google 연결 | 사용자 시트 | 동일 |
| 맵 스타일·카메라 | 맵 시트 | 동일 |

---

## 4. 오버레이 규칙

- **동시에 하나의 큰 패널만:** MENU · 맵 뷰 시트 · 사용자 시트 · 설정 시트 — 열 때 나머지는 닫는다.
- **주행 중(`riding`/`paused`):** MENU는 `locked`로 강제 닫힘(기존 유지).
- **gate / summary:** HUD 트리거 최소화(기존 `MapHud` 정책 유지).

---

## 5. 후속 (P1~)

- [x] MapViewSheet: OSRM/Mapillary를 「고급」 접기 (시트 `fixed`/`z-index:54`, 닫으면 접힘 초기화·오버레이 켜진 상태로 열면 자동 펼침)
- [ ] `App.tsx` 도메인 훅 분리 (`useRoutePlanning`, `useAuthFlow`)
- [x] Trail 변경 UI (`?trail=` · `?room=` 호환 · MENU 상단 **RoomSwitcher** · `history.replaceState` 동기화)
- [x] 커스텀 경로 온보딩( B 여정 ) — `stage===setup` 시 BL에서 안내 카드(sessionStorage 로 탭 세션당 1회 닫기)

---

## 6. 구현 체크 (P0)

- [x] 본 문서 확정
- [x] `RideSettingsSheet` 추가 (MENU 헤더 **설정**, `z-index:56`·`fixed`로 메뉴 위 레이어)
- [x] `RideRoutePanel`에서 설정 블록 제거
- [x] `UserInfoSheet`에서 로비 나가기 제거
- [x] `MapHud`: 같은 방 접속자 표시(「로비 나가기」UI 없음, 태그 **접속**)
- [x] `App.tsx` 패널 상호 배타 + idle 힌트(A안)
- [x] 로그인 세션 = 로비 presence 항상 활성 (`lobbySessionActive`; 수동 참여 플래그 제거)

## 7. 구현 메모 · 로비(프로그램 전용 개념)

- `useLobbyRoomSession`: `enabled: configured && user` — 탭 활성 시 upsert·하트비트.
- 로그아웃 시 `deleteLobbyPresence` 및 `signOut`; 코스 허브는 기존대로 `deleteCoursePresence` 병행.
