# RTW 결정 로그 (Decision Log)

| 항목 | 내용 |
|------|------|
| 문서 유형 | **메타** — 주요 결정·이유의 append-only 로그. "왜 이렇게 갔더라?"의 답 |
| 최초 작성 | 2026-07-07 (자문단 "04_DECISIONS" 제안 채택) |
| 상태 | **반영중** |

> **규칙**: 결정 1건 = 표 한 줄(날짜·태그·결정·이유 한 줄·근거 링크). 상세 맥락은 링크에 두고 여기 늘어놓지 않는다. 뒤집힌 결정은 지우지 않고 ❌ 표시 후 후속 결정을 링크한다. 최신이 위.
>
> **태그**: `[Ontology]` 용어·개념 · `[Product]` 제품 방향 · `[Conquest]` 정복 · `[Trail]` 세션·소셜 · `[Route]` 경로 · `[Economy]` 경제·tier · `[Map]` 지도 플랫폼·표현 · `[UI]` · `[Infra]` · `[Docs]` 문서 체계

| 날짜 | 태그 | 결정 | 이유 (한 줄) | 근거·상세 |
|---|---|---|---|---|
| 2026-09-25 | `[Infra]` `[Trail]` | **D6 빚 청산 — `peerMotion → trail/repo` 0. 주입 구현을 「전역 등록」이 아니라 job 의 필수 필드로 넣는다.** 방향 위반 41 → 35 | 전역 등록(setter)은 배선을 한 번 빠뜨렸을 때 **아무 에러 없이 동행 기록만 멈춘다** — 앱은 정상으로 보이는데 서로가 안 보인다(R10 과 같은 침묵). 필수 필드면 `tsc` 가 조립을 강제한다 | ⚠ 6건은 두 종류였다. **타입 3건**: `TrailLivePublicationRideRow` 를 `trail/trailTypes` 로 올렸다(저장소는 re-export 하되 `lib/` 안 소비자는 도메인 타입을 직접 본다 — 경유하면 결합이 남는다). **쓰기 2건**: `peerMotion/trailLiveRidePort`(계약) ← `trail/trailLiveRideSink`(구현) ← `publishLiveLocationFanout`(조립). ⚠ 포트가 `reason` 을 받지 않는다 — 활동 사유는 **Trail 의 어휘**이고 전송 계층이 알 이유가 없다. 구현이 채운다. ⚠ **DEV 프로브 1건**: `installDevLiveRideProbe` 가 Trail 컬렉션 경로를 동적 import 하는 마지막 고리였다. `installLiveRideExistsDebug`(계측 말단)로 옮겼고 **지우지 않았다** — `e2e/peer-sync-s41r.spec.ts` 가 `window.__rtwLiveRideExists` 를 기다린다. ⚠ 검증: `sink` 를 빼 보고 tsc 가 막는지 · 저장소를 다시 봤을 때 게이트가 35→36 으로 막는지 확인. web typecheck·build · functions 19 pass · 계약 251건 0 fail · **peer-sync replay·fixtures 통과** · 변경분 lint 0. ⚠ peerMotion 잔여 6건: `LiveLocationSnapshot` 타입·`progressRatioToRouteDistanceMeters`(전송과 주행의 **공용 계약** — 층 결정 필요) · 페달 스프라이트 등록 2(전송이 **표현**을 안다, 진짜 결합) · RTDB 동적 import 1(`peerMotion/repo` 로 내리면 해소). 근거: [lib 도메인 경계](260925-RTW-lib-도메인-경계와-의존-방향.md) §7 |
| 2026-09-25 | `[Infra]` | **D6 집행 — peerMotion 을 Ride **아래**로 뒤집고, 게이트에 「도메인은 봐도 저장소는 못 본다」(`mayImportRepoOf`)를 도입한다. 방향 위반 58 → 41** | 전송은 Ride 가 **쓰는 기계**이지 위에 있는 것이 아니다. 뒤집으니 「peerMotion 이 ride 를 본다」가 드러났고 **그 실체는 전부 오배치**였다 | ⚠ 옮긴 것 5종(값·구현 불변): 전송 상수 7개 → `peerMotion/peerSyncPolicy` · `rtdbTrailMotion`(5Hz peer 채널) → `peerMotion/repo` · `sanitizeTrailId`·`DEFAULT_TRAIL_ID` → `trail/trailId` · `lastSeenAtToMillis` → **`firebase/converters`** · `ACTIVITY_WORLD_POLL_*` → `activityWorldPollConstants`. ⚠ **`firebase/converters.ts` 는 감사 §8 목표 구조가 적어 둔 자리다** — Timestamp 를 ms 로 바꾸는 일에 Trail 은 무관한데 그 함수가 Trail 저장소에 있어서 **네 도메인의 저장소가 Trail 저장소를 import** 하고 있었다. ⚠ **옮기는 일에 재작성을 섞을 뻔했다** — `lastSeenAtToMillis` 를 「깔끔하게」 다시 쓴 초안이 원본의 **nanoseconds 합산**과 **`< 1e12` 는 초 단위** 해석을 빠뜨렸다. presence 시각이 조용히 달라질 뻔했다. 원본을 그대로 옮겼다. ⚠ **`mayImportRepoOf` 도입** — peerMotion 은 RTDB 경로 때문에 Trail **ID** 를 알아야 하지만 Trail **문서**를 읽을 이유는 없다. 종전 규칙(「Y 를 보면 Y/repo 도 본다」)으로는 D1 의 읽기 모델 소유권이 말이 되지 않는다. 이 조항은 규칙을 넓히는 게 아니라 **한 차원 좁힌다** — 도입하자 숨어 있던 위반이 55 → **57 로 늘었다**. 정당한 것만 명시(`ride → route/repo` 등)하고 나머지는 빚으로 남겼다. **지문에도 포함**했다: `peerMotion.mayImportRepoOf=["trail"]` 로 넓히면 41 → 35 로 줄지만 지문 불일치로 막힌다(검산 완료). ⚠ G5 상수 값 전수 대조 **바뀐 값 0 · 비유한 0 · 사라진 이름 0**. 계약 시험에 「전송 상수는 peerMotion, 폴링 상수는 activity 가 갖는다」를 추가하고 세 방향으로 깨뜨려 확인했다. ⚠ 검증: web typecheck·build · functions 19 pass · 계약 **251건 0 fail** · **peer-sync replay·fixtures 통과** · 변경분 lint error 0. ⚠ 남은 peerMotion 빚 6건(`routePublishFlight`·`rowToPacket`·`syncFromPresence` 의 Trail 저장소 읽기)은 이동이 아니라 **주입 채널**로 풀어야 한다. 근거: [lib 도메인 경계](260925-RTW-lib-도메인-경계와-의존-방향.md) §7 |
| 2026-09-25 | `[Infra]` `[Map]` | **P5-4 집행 — 마지막 순환(R9)을 끊어 lib 순환 0쌍. 「고치면 죽는 검산」을 합성 그래프 자가시험으로 교체한다** | 순환 ①의 **양쪽 모두 잘못 놓인 것**이었지 진짜 의존이 아니었다 — 한쪽은 지리 계산(`haversineMeters`)이 동기 정책 모듈에 있었고, 다른 쪽은 **소비자가 0인 호환 re-export** 였다 | ⚠ `activityWorldLod → rideSyncPolicy` 는 `haversineMeters` 때문이었다. **여기서도 같은 계산의 사본 셋**이 나왔다(`geo.getDistanceMeters`·`rideSyncPolicy`·`routeGeometryMidpoint` 지역 사본). **이름이 같다고 중복이라 단정하지 않고 값을 대조**했다 — 최대 상대차 **2e-12**(`asin` 대 `atan2` 표현 차이, 지구 반경은 둘 다 6,371,000m). 동일 확인 후 `geo` 로 모았다. ⚠ `rideSyncPolicy → activityWorldLod` 는 LOD 줌 상수 3개의 re-export 한 줄이었고 **두 파일 밖 소비자가 하나도 없었다**. 삭제. ⚠ **G5 — 값이 보존됐는지 실제로 쟀다.** 바꾸기 전 수치 export **40개를 스냅샷**하고 대조: **값이 바뀐 상수 0 · 비유한 0 · 새로 생긴 상수 0**, 사라진 것은 re-export 3개뿐. `sync-policy-constants-contract.test.ts` 로 남겨 pre-push 에 등록했다. **판정을 「0 이 아님」으로 쓰지 않았다** — `TRAIL_LIVE_PROGRESS_MIN_DIST_DELTA_M` 은 정당하게 0 이다. 유한성·파생 일치·폴링 범위·LOD 히스테리시스(enter > exit)·구조로 판정하고 세 방향으로 깨뜨려 확인했다. ⚠ **고치면 죽는 검산은 검산이 아니다** — R9 를 끊자 M0 의 「알려진 순환 R9 가 검출되는가」가 실패했다. 제품 코드가 **나쁜 상태로 남아 있어야만** 성립하는 전제였다. **합성 그래프**(3노드 순환 검출 + 비순환 오탐 없음, 양방향)로 알고리즘 자체를 시험하도록 바꿨고, 검출 조건을 망가뜨려 확인했다. ⚠ 누적: **방향 위반 59 → 58 · 순환 3쌍 → 0쌍.** 남은 58건은 이동이 아니라 **의존을 뒤집어야** 줄어든다(D6 `peerMotion → trail/repo` 11 · `ride → trail/repo` 7 등). 근거: [lib 도메인 경계](260925-RTW-lib-도메인-경계와-의존-방향.md) §9 |
| 2026-09-25 | `[Infra]` | **P5-5 집행 — Firebase 2층 분리와 저장소 25개 이동. 순환은 「폴더 이동」이 아니라 「타입의 위치」로 끊었다** | **파일을 폴더로 옮기는 것만으로는 순환이 하나도 끊기지 않았다** — 이동 직후 3쌍 그대로였다. 감사 §8 의 「자연 해소」가 틀렸음을 다시 확인했다 | ⚠ 세 순환 모두 **한쪽은 타입만, 반대쪽은 함수**를 가져가는 형태였다(정책이 저장소를 올려다보거나, 저장소끼리 타입을 주고받음). `lib/trail/trailTypes.ts`(도메인 층·import 0)로 타입 4개를 내려 **순환 3쌍 → 1쌍**. 남은 1쌍은 R9 로 P5-4 몫. 저장소는 종전 이름으로 re-export 해 소비자 30곳을 건드리지 않았지만, **`lib/` 안의 소비자는 타입 모듈을 직접 보게** 했다 — 호환 re-export 를 경유하면 순환이 살아남는다. ⚠ `lib/` 평면 145 → **126**, 도메인 폴더 2 → **10**. 방향 위반은 59 그대로다 — **이동은 위반을 줄이지 않는다.** 위치만 바뀐다. ⚠ **게이트의 축퇴 성질을 발견했다** — codemod 가 중간에 멈춰 import 가 깨졌을 때 위반이 **59 → 25 로 「좋아졌다」**. 해석 못 한 엣지를 조용히 버리기 때문이다. M0 에 「깨진 상대경로 0」을 세우고 깨뜨려 확인했다. ⚠ 그래프 정규식에 **팬텀 엣지 결함**도 있었다 — `[\s\S]*?` 가 `import.meta.url`(문장 중간의 `import`)에서 시작해 한참 뒤의 `from "…"` 까지 이어 붙였다. 줄머리 고정 + `[^;'"]*` 로 문장을 넘지 못하게 고쳤다. ⚠ **이동은 LLM 이 아니라 codemod 로 했다**(D8): `scripts/move-lib-files.mjs`(이동 전 specifier 해석 → `git mv` → 새 상대경로 재계산). 동적 `import("../firebase")` 6건이 실제로 있었고 그것이 깨지면 tsc 는 통과하고 **실행 시점에** 터진다(R2). ⚠ codemod 가 **두 가지를 놓쳤고 둘 다 보강했다**: ① 같은 specifier 가 한 파일에 두 번 나올 때 두 번째 치환을 「실패」로 오판해 이동 도중 멈췄다(중복 제거) ② **문자열 경로**(`readFileSync("lib/firestoreCourses.ts")`)는 tsc 가 잡지 못하고 **시험만 ENOENT 로 죽는다** — 3건을 손으로 고치고, 남으면 실패하는 검사를 넣었다. ⚠ 중단 복구는 **되돌리기가 아니라 앞으로 고치기**로 했다(`scripts/repair-lib-imports.mjs` — basename 유일 후보만 고치고 후보가 여럿이면 멈춘다). `git mv` 로 이력은 이미 보존됐고, 되돌리려면 작업분을 파괴해야 한다. ⚠ 파일명은 바꾸지 않았다(`firestoreTrail.ts` → `trailRepo.ts` 같은 rename 은 Ontology §0.1 에 따라 별건). 예외는 `firebase.ts` → `firebase/app.ts` 뿐. ⚠ 검증: web typecheck·build · functions 19 pass · 계약 241 pass · 셀 벡터 4 pass · URL 게이트 2 pass · 게이트 3방향 파괴 확인. lint error 35 는 전부 기존 react-hooks v7 baseline(내 변경은 import 한 줄씩). 근거: [lib 도메인 경계](260925-RTW-lib-도메인-경계와-의존-방향.md) §6 |
| 2026-09-25 | `[Conquest]` `[Route]` | **P5-3 집행 — `ClaimReader` 포트를 넣고, 셀 ID 계산을 `functions/src/geoTiles.ts` 단일 출처로 모은다.** 코어는 Conquest 를 모르고 `distanceAutoRouteHttp` 가 조립한다 | 실측하니 **코어는 이미 `claimedCellIds` 를 입력으로 받고 있었다** — 진짜 위험은 의존 방향이 아니라 **셀 ID 계산이 세 곳에 복제**돼 있는 것이었다 | ⚠ **복제 3곳**: `conquestClaimRead`(읽는 쪽, 상수 사용) · `distanceAutoRouteCore`(재는 쪽, `zoom = 20` **리터럴**) · `apps/web/src/lib/conquestTiles`(**쓰는 쪽**). 어긋나면 `claimedCellIds.has(...)` 가 영영 실패하는데, 그 결과(비율 전부 1.0)는 **「Claim 이 없는 사용자」와 구분되지 않는다** — 에러도 로그 이상도 없이 순위 기능만 사라진다. **이것이 R10 의 실체다.** ⚠ **네 번째 사본이 시험 안에 있었다** — `distanceAutoRouteCore.test.ts` 가 타일 계산을 자기가 다시 구현해 자기를 검증했다. 그래서 읽는 쪽이 어긋나도 통과했다. 공유 함수를 쓰게 고쳤다. ⚠ `functions` 안의 둘은 `geoTiles.ts` 로 **실제 통합**했고, 빌드가 나뉘어 코드를 공유할 수 없는 클라이언트와는 **양쪽에 같은 고정 벡터**를 박아 고정했다(`20_894282_406315` 등, 두 파일을 함께 바꿔야 한다). ⚠ 리더의 `"20_"` 리터럴도 상수 파생으로 바꿨다. ⚠ 포트는 기존 `fetchDirections` 주입 패턴을 그대로 따른다 — 기본값이 Firestore 구현이라 **동작 무변경**이고, 시험은 가짜를 주입한다. ⚠ 게이트 G3 계약 19건. **네 방향으로 깨뜨려 확인**: 코어에 사본 부활 · 코어가 conquest import · **컴파일은 통과하면서 매핑만 z19 로 어긋남**(가장 중요 — 앞의 둘은 tsc 가 먼저 잡지만 이건 안 잡는다) · 클라이언트 줌만 변경. ⚠ 탐지식을 `Math.log(Math.tan(` 로 잡으면 **Mercator 미터 투영 2곳(정당한 코드)** 을 오탐한다 — 타일 고유형(`tan+1/cos`)으로 좁혔다. 오탐하는 탐지는 결국 꺼진다. 근거: [lib 도메인 경계](260925-RTW-lib-도메인-경계와-의존-방향.md) §5 |
| 2026-09-25 | `[Infra]` | **`test:next-ride`(계약 245건·2.5초)를 pre-push 게이트로 승격한다.** 등록만 하고 돌리지 않으면 그것은 게이트가 아니다 | `sensor-chip-slot-contract` 가 **2026-09-23 `c66699a`(지시01 접힘 캐럿 LED)부터 red 인 채 이틀간 아무도 몰랐다.** 시험은 09-18 이후 갱신되지 않아 「칩 가드는 `cadence` 하나뿐」이라는 낡은 계약을 주장했다 | ⚠ **계약을 낡은 쪽으로 되돌리지 않았다** — 지시01 의 의도는 「접어도 센서 신호가 남는다」 그대로이고 **남기는 수단만** 바뀌었다(접히면 칩 대신 캐럿 LED 로 연결만, rpm 은 펼친 뒤). 그래서 시험이 지키는 것을 「칩이 항상 있다」에서 **「접힘에서 연결 표시가 사라지지 않는다」**(caretOnly·caretSensorView·LED CSS 동시 존재)로 바꿨다. 캐럿 LED 뷰를 지워 실제로 잡히는지 검산했다. ⚠ 이 red 를 감사가 못 본 이유: 감사는 e2e 만 돌렸고 이 스위트는 **어느 게이트에도 없었다**. 「미등록 스펙」보다 **「등록됐지만 아무도 돌리지 않는 스위트」** 가 더 위험하다. ⚠ pre-push 게이트 5종 → **8종**(+ 계약 스위트 · functions 계약 시험 · Claim 셀 벡터 클라↔서버) |
| 2026-09-25 | `[Infra]` | **Phase 5 착수 — `lib/` 도메인 경계와 의존 방향을 먼저 선언한다. 파일 이동은 그 다음이다.** 선언 형식은 선형 계층이 아니라 **허용 엣지 DAG**(D7) | 감사 §8 은 「도메인 폴더로 나누면 순환 4쌍이 자연 해소된다」고 적었으나 **실측은 반대다 — 분할은 순환을 드러낼 뿐 끊어주지 않는다.** 순환은 3쌍이고(1쌍은 Phase 3 에서 이미 해소) 그 중 2쌍이 `firestore*` 를 한쪽 끝에 둔다 | ⚠ **선형 rank 로는 표현할 수 없다** — `ride` 와 `trail` 은 서로를 모르는 **형제**다. 자문의 「계층으로 선언하지 말라」는 옳지만, 선언을 아예 버리면 역주행을 막는 게이트가 사라진다. 거부해야 할 것은 「선언」이 아니라 「선형 순위」였다. ⚠ **Trail 은 Ride 의 하위가 아니다**(D1) — `trail/` → `ride/` 직접 import 가 **0건**이다. Trail 은 주행보다 먼저 존재하는 **맥락**이다(`atTrailheadIdle`). 다만 `trail/repo` 가 Ride 의 정책·스키마를 import 하면 화살표가 `trail/` 에서 `trail/repo/` 로 **옮겨간 것**일 뿐이므로, repo 는 **Trail 이 소유한 좁은 읽기 모델만** 반환한다 — 게이트가 `trail/repo → ride/**` 도 위반으로 센다(축퇴 통과 방지). ⚠ **포괄 폴더(`shared`·`common`·`utils`)를 만들지 않는다**(D5) — 만들면 지금의 평면 `lib/` 이 이름만 바꿔 영구화된다. 이름 없이 섞여 있던 32개에 `identity`·`sensor`·`account`·`storage`·`terms` 를 세워 전원 행선지를 지정했다. ⚠ **파일 이름이 도메인을 정하지 않는다** — `rideCameraFraming`→camera · `routeWaypoints`→geo · `rideConquestResult`→ride. `routeFingerprint` 를 `geo` 에 둔 감사 §8 의 착오도 정정(경로 동일성 판단이므로 `route`). ⚠ 값의 단일 출처는 `apps/web/dep-layers.json`, 근거는 [lib 도메인 경계](260925-RTW-lib-도메인-경계와-의존-방향.md) |
| 2026-09-25 | `[Infra]` | **`firestore*.ts` 는 인프라가 아니라 Repository 다 — Firebase 를 2층으로 가른다**(D3). `lib/firebase/` 는 순수 인프라, 도메인을 아는 저장소는 `lib/<도메인>/repo/`. **「도메인 안이냐 밖이냐」의 양자택일을 기각** | `firestore*.ts` 25개가 이미 도메인 로직을 **수입**하고 있다 — `firestoreRides`→`rideRecordPolicy`, `firestoreTrailInstance`→`trailAccessPolicy`, `firestoreCourses`→`routeFingerprint`. 인프라로 취급해 아래에 깔았기 때문에 되짚어 올라가는 화살표가 30건 넘게 생겼다 | ⚠ 완료보고 §9 가 「전부 몰면 분할이 무의미, 도메인 안에 두면 Firebase 교체 시 전 도메인이 흔들림」이라고 양자택일로 적었으나 **제3의 답이 강제된다**. 2층이면 분할은 무의미해지지 않고(repo 가 도메인에 속하므로), 교체 시 흔들리는 것은 `firebase/` 와 각 `repo/` 뿐이다. ⚠ **순환 ②③은 선언으로 끊는 게 아니라 층이 갈리면 구조적으로 끊긴다** — `trailAccessPolicy`(정책)가 아래, `trailInstanceRepo`(저장소)가 위로 정렬된다. ⚠ **`peerMotion` 은 「모범」이면서 최대 위반자다**(D6) — 감사 §8 이 모듈화 본보기로 꼽았으나 밖으로 나가는 화살표를 재면 `trail/repo` **12건** 직통이고, 특히 **`peerMotion/types.ts` 가 `firestoreTrailLivePublicationRides` 를 import 한다**(가장 아래여야 할 타입 모듈이 가장 위를 본다). Ride 의 하위 **전송** 계층으로 등재하고 주입 채널로 전환한다 — 합격 조건은 `peer-sync` replay 게이트. ⚠ 근거: [lib 도메인 경계](260925-RTW-lib-도메인-경계와-의존-방향.md) §6·§7 |
| 2026-09-25 | `[Route]` `[Conquest]` `[Map]` | **제품 개념 관계와 코드 import 방향을 분리한다.** 경로 생성이 Claim 을 읽는 의존은 없애지 않고 `ClaimReader` **포트**로 뒤집는다(D2). 지도 레이어 순서는 `lib/map/layerOrder.ts` 단일 출처로 하고, **계약 판정을 렌더에서 순수 정책으로 내린다**(D4) | 「Conquest 가 Route 를 참조하면 안 되니 끊자」고 하면 **제품 기능 자체를 제거**하는 결과가 된다 — `distanceAutoRouteHttp.ts:503` 이 「출발점 주변 Claim만 읽어 신규도로·자기중복으로 순위(탈락 아님)」라고 적고 있다. 「Ride = Claim」의 핵심이다 | ⚠ **코어는 Claim 을 모르고 `route/http` 가 조립 지점**이다. 실제로 이미 `Http` 는 알고 `Core` 는 모른다 — seam 을 넣는 것이지 새로 만드는 것이 아니다. 부수 이득으로 코어가 Firestore 없이 단위 시험 가능해져 **R10(같이 옮기지 않으면 새 도로 비율이 조용히 0)** 이 게이트로 고정된다. 이 포트는 경로 생성 3갈래 공통 코어의 계약이 된다. ⚠ **`ClaimReader` 는 `functions/` 단독**이다(`apps/web/src` 에 참조 0) — `lib/` 분할과 파일이 겹치지 않아 병렬 실행 가능. 반면 **D4 는 `MapView.tsx` 4,870줄을 건드리므로 병렬로 두지 않는다**. ⚠ **D4 의 전제는 감사 P3 다** — 헤드리스 e2e 에서 preserved 라이더 레이어가 생성되지 않아 「라이더가 경로선보다 위」를 렌더로 판정할 수 없다(3번 시도 후 보류). 그래서 순서를 **z-rank 선언**으로 내려 순수 함수 시험으로 판정하고, 레지스트리 미등록 `addLayer` 를 막는다 — 현재 순서 결정은 7개 파일에 흩어져 있고(`MapView` 만 `addLayer`/`moveLayer` **38회**) 단일 정책 모듈이 없다. R6(layer id 를 바꾸면 오버레이가 조용히 사라진다)·P4(최상단이 라이더가 아니라 activity pulse dots)가 이것으로 회귀 감지 범위에 들어온다. **라이더가 실제로 그려지는지는 여전히 Chief 실기 몫**이다. ⚠ 근거: [lib 도메인 경계](260925-RTW-lib-도메인-경계와-의존-방향.md) §2·§5 |
| 2026-09-25 | `[Infra]` | **의존 방향·순환 게이트를 세운다(pre-push 5종). baseline 은 허용 목록이 아니라 갚아야 할 빚의 눈금이다.** 기계적 파일 이동은 LLM·Cursor 가 아니라 `git mv`+codemod+`tsc` 로 한다(D8) | 「통과했다」는 「게이트가 살아 있다」의 증거가 아니다. 이 프로젝트에서 판정 도구가 두 번 틀렸고 두 번 다 「통과」쪽이었다 — 수집이 0건이면 위반도 0건이 되어 **거짓 통과**가 난다 | ⚠ **M0 자가 검산을 판정 앞에 세웠다** — 수집 ≥250 · `geo.ts` fan-in ≥40 · **알려진 순환 R9 가 검출되는지**(검출기 생존) · **lib 파일 전원 도메인 지정**. 실패하면 exit 2 로 끝내고 위반 목록을 신뢰하지 말라고 알린다. ⚠ **게이트를 네 방향으로 깨뜨려 확인했다**: 금지 방향 1줄 추가(59→60, exit 1) · 새 순환 1쌍(3→4, exit 1) · 미지정 파일 추가(M0 FAIL, exit 2) · **규칙을 넓혀 우회**(위반 59→**35 로 줄었으나** 지문 불일치로 exit 1). 마지막이 핵심이다 — **이 게이트를 통과하는 가장 쉬운 길은 코드가 아니라 `mayImport` 를 넓히는 것**이라, baseline 이 허용 규칙의 **지문**을 함께 갖고 규칙 완화가 반드시 git diff 에 드러나게 했다. ⚠ 정책은 전부 선언(`dep-layers.json`)이 갖는다 — 스크립트에 예외를 박으면 선언이 진실이 아니게 된다. ⚠ **시작 부채: 방향 위반 59건 · 순환 3쌍.** 부채가 가장 몰린 곳은 **`trail/repo` 로 들어가는 화살표 28건** — 사실상 공용 저장소로 쓰이고 있다는 뜻이다. Phase 5 종료 시 0 이어야 한다. ⚠ **기계적 이동을 LLM 에 주지 않는 이유**(D8): 이번 위험은 정적 리팩터가 못 보는 세 곳 — 동적 import(R2)·문자열 Mapbox layer id(R6)·런타임 상수 순환(R9) — 에 몰려 있고, Cursor·서브에이전트의 멀티파일 편집은 셋 다 조용히 통과시키면서 M0 규율을 우회한다. 검산자가 `tsc` 로 완전한 작업에는 LLM 이 필요 없다. ⚠ 근거: [lib 도메인 경계](260925-RTW-lib-도메인-경계와-의존-방향.md) §8·§9 |
| 2026-09-25 | `[Infra]` | **구조 감사 Phase 4 종료 — 리전·dev 포트를 단일 출처로. `.env` 통합은 하지 않는다** | `asia-northeast3` 이 functions 23개 파일에, dev 포트 규칙이 두 config 에 각각 적혀 있었다 | ⚠ **리전**: `functions/src/region.ts` 로 모았다. 합격 조건이 「배포 주소 불변」이므로 그것을 직접 쟀다 — 변경 전후로 **28개 함수 전부 `asia-northeast3`** 임을 런타임 값으로 확인했다(문자열 출현 수 33→4 는 모듈로 모였다는 뜻일 뿐 주소의 증거가 아니다). ⚠ **dev 포트**: `apps/web/devPort.ts` 로 **값만** 공유한다. 「에뮬레이터인가」의 판단은 vite(`mode`)와 playwright(주입 env + Functions 번들 여부)가 서로 달라 각자에 남겼다 — 값과 판단을 묶으면 오히려 얽힌다. 두 포트 경로를 각각 e2e 로 확인했다. ⚠ **`.env` 3중 정의는 감사의 오판이었다** — 공통 9키 중 **7개의 값이 서로 다르다**. `VITE_FIREBASE_PROJECT_ID` 부터 `.env.emulator` 는 에뮬레이터 프로젝트를, `.env.harness` 는 하네스 전용 프로젝트를 가리킨다. 키 이름이 같을 뿐 다른 환경이고, 합치면 하네스가 잘못된 프로젝트를 본다. **감사가 이름만 보고 값을 대조하지 않았다.** ⚠ 이번 정비로 세운 pre-push 게이트 4종: 타입체크(web·functions) · lock 동기 · harness 설정 동기 |
| 2026-09-25 | `[Infra]` | **배포 사고 — functions 에 의존을 넣고 lock 을 갱신하지 않아 함수 28개 배포가 통째로 실패했다.** 재발 방지를 pre-push 게이트로 세운다 | Cloud Build 의 `npm ci` 가 `package.json` 과 `package-lock.json` 불일치를 거부했다. 로컬은 루트에 hoist 된 패키지로 돌아가 lint·typecheck·테스트가 **전부 통과**했다 | ⚠ 뿌리: **`functions` 는 워크스페이스가 아니라 자기 lock 을 따로 가진다**(루트 `workspaces` 는 `apps/web` 뿐). 루트에서 `npm install` 을 아무리 돌려도 `functions/package-lock.json` 은 갱신되지 않는다. ⚠ **로컬 통과가 배포 통과를 뜻하지 않는 첫 사례**다 — hoist 가 결함을 가렸다. 그래서 고친 뒤 `npm ci --dry-run` 으로 **배포와 같은 조건**에서 확인했다. ⚠ 게이트: `scripts/check-lock-sync.mjs` — 선언된 의존이 해당 lock 에 있는지 대조하고, `package*.json` 을 건드린 push 에서만 돈다. 없는 의존을 넣어 실제로 잡는지 검산했다 |
| 2026-09-25 | `[Infra]` | **구조 감사 Phase 4(설정 단일화) 착수 — 「조용히 어긋나는」 중복 셋을 단일 출처로 모은다** | 같은 값이 여러 파일에 손으로 복제돼 있어 한쪽만 고쳐도 아무 에러가 나지 않았다(감사 H5·R8) | ⚠ **RTDB 규칙 ↔ peer-sync 패킷**(R8): `database.rules.json` 의 `.validate` 에서 조건을 **읽어와** 코드와 대조한다. 필드·`ph` 값을 **양방향**으로 본다 — 「규칙이 요구하는 것을 담는가」만 보면 규칙이 느슨해지는 방향을 놓친다(첫 판의 구멍이었고, 규칙을 세 방향으로 깨뜨려 찾았다). ⚠ **`firebase.harness.json`**: 이제 `firebase.json` 에서 **생성**한다. 본체와 달라야 하는 것은 `functions.source`·`predeploy` 없음·`hosting` 없음 셋뿐이다. 생성 결과가 기존 파일과 동일함을 확인해 하네스 동작 불변을 보장했다. ⚠ **에뮬레이터 포트**: `scripts/emulatorPorts.mjs` 가 `firebase.json` 을 읽는다. **Node 영역(하네스·스크립트)까지만** 적용했다 — 브라우저 번들은 파일시스템을 못 읽고, 에뮬레이터·e2e 배선을 한꺼번에 흔들면 회귀를 가려내기 어렵다. ⚠ 남은 것: `.env` 3중 정의(Firebase 6키) · 리전 36곳 · `vite.config.ts`↔`playwright.config.ts` 의 포트 계산 중복 |
| 2026-09-25 | `[Infra]` | **Cloud Functions 런타임을 Node 20 → 24 로 올린다.** 하네스의 Node 요구 버전은 `functions/package.json` 의 `engines.node` 한 곳에서 읽게 일반화한다 | **nodejs20 은 2026-10-30 에 지원이 끝난다**(35일 남음). 방치하면 함수 배포가 막힌다 | ⚠ **22 가 아니라 24 를 골랐다** — firebase-tools 의 런타임 표(`supported/types.js`)를 직접 읽어 확인했다: nodejs22 와 nodejs24 의 **decommission 이 2028-10-31 로 동일**하고 deprecation 은 24 가 1년 늦다(2028-04-30 vs 2027-04-30). 같은 비용에 더 멀리 간다. ⚠ **부수 효과로 route-token 하네스가 되살아났다** — 종전에는 Node 20 을 강제하는데 portable(`.runner-cache/node-v20`)이 없고 host 는 v24 라 **아예 실행되지 않았다**. 이제 host Node 24 로 충족된다. ⚠ 버전이 두 곳(functions `engines.node` · 하네스 `node20.mjs`)에 박혀 있어 한쪽만 바꾸면 조용히 어긋나는 구조였다 — 단일 출처로 모았다(`nodeRuntime.mjs`). ⚠ 남은 문제(별건): `runner-fail-recovery` 시험이 `functions/.secret.local` 이 **없어야** 한다고 단언하는데, HARNESS.md 는 「개인 `.secret.local` 은 덮어쓰지 않는다」고 적고 있다. 개인 개발 환경에서는 그 파일이 정상적으로 존재하므로 **시험 전제와 문서가 모순**이다. ⚠ 검증: functions build·typecheck·lint, 단위 9 pass, 하네스 단위 13 pass. **실제 배포 확인은 Chief 실행 영역** |
| 2026-09-25 | `[Infra]` `[Ontology]` | **구조 감사 Phase 3 — 간접층·레이어링 역전·죽은 분기를 정리한다.** 런타임 동작은 바꾸지 않는다 | 루트 1줄 shim 9개가 `components/X` → `components/<도메인>/X` 를 가리키는 간접층으로만 존재했고, 코어가 UI 타입을 import 하는 역전이 있었다 | ⚠ **`FollowMode` 를 `components/ride/RideRoutePanel` → `lib/mapGlobeView` 로 옮겼다**(감사 M4). 카메라 추종 모드는 지도의 개념이지 패널의 개념이 아니다 — 코어가 UI 를 import 하던 방향을 되돌렸다. 패널은 하위 호환 re-export 만 남긴다. ⚠ 동적 import 라 위험 신호였던 `MapillaryRideViewer`(감사 위험 R2)는 Mapillary 제거로 이미 사라져, 이번 치환은 정적 경로만으로 끝났다. ⚠ `RiderPrototypeMode` 의 `"legacy"` 는 `getRiderPrototypeMode()` 가 절대 반환하지 않는 **도달 불가 값**이라 제거했다. `iso2d` 는 env 로 도달 가능하므로 남긴다. ⚠ 퇴역 용어 `lobbySpectator*`·`lobbyActive*` 18건을 **`trailhead*`** 로 바꿨다 — 실제로 `atTrailheadIdle` 일 때 쓰는 값이라 Trailhead 가 정확한 이름이다. ⚠ **Mapbox source/layer id 문자열 7개(`boxcycle-lobby-spectator-*`)는 그대로 뒀다** — 이름만 바꾸면 소스·레이어 참조가 어긋나 오버레이가 조용히 사라진다(감사 위험 R6). 실기 확인을 낀 별건으로 처리한다 |
| 2026-09-25 | `[Infra]` | **구조 감사 Phase 2 — DEAD 30개 파일을 지운다.** 삭제 판정은 자가 검산(M0)을 통과한 뒤에만 인정한다 | 2026-06-19 Phase 6 이름 이관 때 만든 호환 shim 을 석 달째 아무도 회수하지 않았다. 참조 0 을 재확인하고 걷어냈다 — 35파일 1,084줄 삭제 | ⚠ **판정 도구가 두 번 틀렸고 M0 가 두 번 다 잡았다.** 1판은 `execSync` 의 maxBuffer 초과 예외를 catch 해 「참조 0건」으로 처리했다(참조가 많을수록 안전해 보이는 정반대 버그). 2판은 `SKIP_DIR` 의 `lib` 가 `apps/web/src/lib` 를 통째로 빼 살아 있는 심볼이 0건이 됐다. **「0건이니 지워도 된다」는 판정 앞에는 「세는 일이 작동하는가」를 먼저 세워야 한다** — [축퇴값 자동통과 게이트]와 같은 함정이다. ⚠ functions 의 `courseActivityOnLiveCourseRideWritten`·`ScheduledReconcile` 2건은 **Phase 0 에서 배포 스크립트를 `routeActivity*` 로 고친 덕에 비로소 참조가 끊겼다** — 지혈이 청소의 전제였다. ⚠ `TrailheadPresence` 는 `.tsx` 만 지웠다. `.css` 는 `PublicationSharedPresence` 가 쓴다(감사 위험 R1 — 함께 지웠으면 빌드는 통과하고 스타일만 깨졌다). ⚠ 보류 3건: `openMeteoWeather`(날씨 복원 계획 확인 후) · `ride/RideHistoryPanel`(실체 보존, 루트 shim 만 삭제) · `FirstRideIntroCard`(지시01의 유지 명시). ⚠ `[C1]`~`[C4]`·`[PhaseD]` 디버그 로그는 **감싼 DEV 블록째** 걷었다 — 로그 줄만 지우면 빈 껍데기가 남는다 |
| 2026-09-25 | `[Map]` `[Product]` `[Infra]` | **Mapillary 를 완전히 제거하고, 그에 묶여 있던 three 를 0.134 → 0.186.1 로 올린다** | 「맵필러리는 퀄리티가 부족하기 때문에 사용하지 않기로 결정했다. 버려라」(Chief). 거리뷰는 pre-version 을 죽인 비용 구조의 잔재였고, 품질·연속성이 낮아 이미 보조 기능으로 강등돼 있었다 | ⚠ **three 0.134 를 고정하던 것이 `mapillary-js` 였다** — 그것이 사라져 엔진 선택이 자유로워졌다. 「당장 0.175 를 써야 하는 것은 아니지만 라이더가 제품의 얼굴이 될 앱에서 결국 올려야 하고, 그 시점이 머지않다」(Chief)는 판단에 동의했다. **GLB 교체를 재개하기 전이 마지막으로 싼 타이밍**이다 — 0.134 에서 조명·스케일을 맞춰 두고 나중에 올리면 그 맞춤을 두 번 한다. ⚠ 죽은 선언에 적혀 있던 0.175 가 아니라 **현 최신 0.186.1** 로 갔다 — 어차피 r152 색공간·r155 조명이라는 같은 breaking 구간을 건너므로 비용이 같다. ⚠ **출력 색공간을 코드에 명시적으로 고정했다**(`SRGBColorSpace`). 종전에는 three 기본값에 기대고 있어 업그레이드마다 라이더 색이 조용히 달라질 수 있었다 — 이번에 데인 것을 방어로 바꿨다. ⚠ 조명 값(0.9/1.2/1.6)은 그대로 뒀다 — 「조명은 그리 중요한 요소가 아니고 회귀되어도 괜찮다」(Chief). 다만 색감 변화와 라이더 정상 렌더는 **실기 확인 대상**이다(헤드리스 e2e 는 라이더를 그리지 못한다 — 구조 감사 P3). ⚠ 커버리지 오버레이는 OSRM 과 공용이었다 — Mapillary 쪽만 걷어내고 OSRM 은 남겼다(모드 `off|osrm`). ⚠ 남은 뒷정리: Functions 시크릿 `MAPILLARY_ACCESS_TOKEN` 은 Chief 실행 영역이다(코드 참조는 0) |
| 2026-09-25 | `[Infra]` | **구조 감사 Phase 0 — 배포 스크립트가 없는 함수를 가리키던 것과 `three` 의존 오분류를 고친다.** 앱 코드는 건드리지 않는다 | 감사에서 Critical 로 잡힌 2건이 **지금 손해를 내고 있었다**. `deploy:*` 두 스크립트가 `courseActivity*` 를 배포하는데 `index.ts` 는 Phase 6 이후 `routeActivity*` 만 export 한다 — 그 자리에서 아무 함수도 갱신되지 않았다 | ⚠ **`three` 는 `^0.175.0` 으로 선언돼 있었으나 lock 에 반영된 적이 없다** — 실제 설치·배포되어 온 것은 `mapillary-js` 가 끌고 온 **0.134.0** 이고, 코드도 0.134 에 있는 기본 클래스만 쓴다(GLB 교체 계획서 B1 도 0.134 를 전제). 선언을 실물에 맞췄다 — lock·설치물 무변경이라 라이더 그림은 달라지지 않는다. ⚠ 0.175 업그레이드는 GLB 교체 단계 C 와 묶어 판단한다. ⚠ `three` 가 devDependencies 에 있어 `npm ci --omit=dev` 에서 빌드가 깨질 상태였다 — dependencies 로 옮겼다. ⚠ 스크립트 **이름**(`deploy:course-activity-functions`)은 퇴역 용어지만 그대로 뒀다 — rename 은 Ontology §0.1 에 따라 별건이다. 보고서: [구조 감사](archive/260924-RTW-프로젝트-구조-감사-보고서.md) |
| 2026-09-25 | `[Infra]` | **구조 감사 Phase 1 — `functions/` 에 eslint 게이트를 켜고, 죽어 있던 e2e 를 되살린다** | `githooks/pre-commit` 이 설정 없는 `functions/` 를 영구 스킵해 **106 파일이 lint 밖**에 있었다. 설정을 두는 것만으로 게이트가 켜졌고, baseline 은 7건뿐이라 즉시 해소됐다 | ⚠ **감사의 「깨짐 의심 4건」 중 실제로 깨진 것은 `ride-summary-compact` 하나**였다 — 09-24 센서 칩 LED 전환으로 aria-label 이 「센서 설정 **열기** …」가 되면서 이름 매칭의 first() 를 가로챘다(제품 결함이 아니라 스펙 셀렉터 문제). ⚠ **e2e 스펙 2개가 산출물을 추적 경로에 쓰고 있었다** — `document/archive/` 의 커밋된 증거 PNG 가 테스트할 때마다 덮어써졌다. 둘 다 `.out/` 로 옮겼다. ⚠ **헤드리스 e2e 에서는 preserved 라이더 레이어가 뜨지 않는다**(20초 대기해도 미생성) — 라이더 관련 회귀는 현재 e2e 로 고정할 수 없다. 「라이더가 경로선 위」 계약은 그래서 보류했다. ⚠ QC1 거리 클램프 우회(위험 R5)는 계약 시험 14건으로 고정했고, 값을 일부러 무너뜨려 게이트가 실제로 잡는지 검산했다 |
| 2026-09-24 | `[Map]` `[UI]` | **표고 프로필 색 `#c36839`(테라코타) 확정** — Chief 승인. 경로선(`#ef4444`)과 분리된 별도 상수 `ELEVATION_LINE_COLOR` 로 관리한다 | 「표고 그래프의 컬러가 Red 인데 경로 컬러와 같아서 이따금 혼동을 준다」 → `#fdd836`(노랑) 시안 → 「조금 더 강한 컬러」 요청 → `#c36839` 실기 확인 후 승인 | ⚠ 색을 **상수로 분리**한 것이 본체다 — 종전에는 경로선과 같은 리터럴을 세 곳에 박아 두어 한쪽만 바꾸면 어긋났다. ⚠ 지도 위 표고 프로필은 **평면 레이어**라 라이더 가림 문제(같은 날 레이어 순서 수정)와 무관하다. ⚠ 라이더 주황과 계열이 가까우므로, 라이더 배색을 바꿀 때 이 색과의 구분을 함께 확인한다 |
| 2026-09-24 | `[Map]` `[UI]` | **라이더 조명 기본값을 「진한 색」으로 확정** — Ambient **0.9** · Hemisphere **1.2** · Key **1.6**(방향 `(-3, 8, 5)` 불변). 종전 1.25 / 1.7 / 2.4 | 「진한 색으로 설정한 게 그중 나아 보인다. 이걸로 가자」(Chief). 종전 강도 합 5.35 는 밝은 면이 흰색으로 날아가 저지·프레임의 주황이 사라졌다 | ⚠ 값은 **조절판(`?lightlab=1`)에서 실제 화면을 보고 골랐다** — 코드를 고쳐 배포하며 맞추는 왕복을 없앤 첫 사례다. 다시 바꿀 때도 같은 방법을 쓴다. ⚠ 방위각 329°·고도각 54° 는 기존 `position(-3, 8, 5)` 를 역산한 값이라 **방향은 바뀌지 않았다** — 강도 셋만 내렸다. ⚠ 조절판은 URL 파라미터가 없으면 렌더되지 않는다(일반 사용자 흔적 없음). 프리셋 4종 값: [지시02 수행결과](ops/20260924-camera-qc/20260924-지시02수행결과-라이더-조명-조절판.md) |
| 2026-09-24 | `[Map]` `[UI]` | **Quick Camera 1번을 3단 → 4단으로.** 순환은 **Route Fit → 200m → 60m → 5m**. 넓은 것에서 좁은 것으로 내려간다 | 「1번 카메라 3가지 스케일 순환에서 **200m 보기를 추가**해. 4가지로 늘려서」(Chief) | ⚠ **거리 상한 60m 는 QC1 preset 경로에 적용되지 않는다** — `RIDE_CAMERA_DISTANCE_MAX_M` 은 맵 뷰 시트 수동 슬라이더·휠줌 역산 전용 클램프다. 기존 `aerial60` 도 그 상한을 거치지 않았다. 200m 는 상한을 바꾸지 않고 통과한다(실측 zoom 18.375). ⚠ **QC1 은 pitch 0°(상공)** 다 — pitch 80° 는 QC2~6 이다. 감리 지시서가 둘을 같은 pitch 로 전제한 착오를 개발팀장이 잡았다. ⚠ 3단계는 **5m**(2026-09-23 에 10m 에서 내린 값) — 감리 지시서의 「10m」은 낡은 값이었다. ⚠ 단계 정의를 `lib/camera1Mode.ts` **한 곳**으로 모았다(App.tsx·MapHud.tsx 의 중복 삼항 제거) — 다음 단계 추가는 그 모듈만 고치면 된다. 캡처·e2e: `ops/20260924-camera-qc/` |
| 2026-09-24 | `[UI]` | **UI 공간 밀도 원칙을 문서로 고정한다**(D1~D7). 단위는 그룹당 1회 · 폭은 내용+최소 패딩(44px 은 높이로) · 풀 폭은 주 행동 하나 · 보조는 새 줄을 만들지 않는다. **지시서가 말하지 않아도 모든 UI 작업에 적용**된다 | 「좁디좁은 스마트폰 화면도 절약 대상이다. 쓸데없이 크게 만들어 낭비되는 공간이 생기지 않도록 하라. 이 척도는 모든 앱에 공통으로 적용해야 할 요소다. **매번 나는 이런 식의 짜증나는 작업 지시를 해 오고 있다**」(Chief) | ⚠ 원칙이 문서에 없어 **라운드마다 같은 지시가 반복**됐다 — 이 문서의 존재 이유가 그것이다. ⚠ **D2 는 「작게 만들라」가 아니다** — 터치 44px 은 유지하되 폭으로 그 크기를 만들지 말라는 뜻이다. 글자 축소·여백 0 으로 공간을 버는 것도 금지. ⚠ 같은 화면에 이미 정답이 있었다 — RouteDock 거리 칩 `1 3 5 10 20`. 한 앱에 두 밀도를 두지 않는다. SoT: [UI 공간 밀도 원칙](260924-RTW-UI-공간밀도-원칙.md) · 커서 상시 규칙 `.cursor/rules/ui-space-density.mdc` |
| 2026-09-24 | `[UI]` | **체험 속도를 주행 중에도 조절할 수 있게 한다 — 단 개발 편의이며 출시 전 최종 정리한다.** 2026-08-27 SENSOR-2 의 「체험 속도는 Go 전에 정한다·주행 중 재튜닝 금지」를 **한시적으로** 뒤집는다 | 「체험 속도의 경우는 개발에서 보다 자유롭게 컨트롤할 필요가 있다. 이 부분은 개발 완료 단계에서 최종 정리하기로 한다. **개발 시간을 아껴야 하기 때문이다**」(Chief). 완주 확인에 2~3 km 를 실제로 달려야 해 라운드마다 시간이 샜다 | ⚠ **영구 사양이 아니다** — [출시 전 확인사항 §5](출시%20전%20확인사항.md) 에 등재했고, 출시 전 원복·dev 한정·정식 승격 중 택일한다. ⚠ **센서 연결 시에는 노출하지 않는다** — 그때 진실은 실제 케이던스이고, 손으로 덮으면 기록이 거짓이 된다(어느 선택지에서도 유지). ⚠ 되돌리기 쉽게 **조건 한 곳**으로 만든다(`CadenceSensorSheet.tsx` 의 `!props.riding`). ⚠ 누적 거리는 속도 변경에 영향받지 않아야 한다 — 지시06 §B4 에서 숫자로 검산 |
| 2026-09-24 | `[Route]` `[Product]` | **Ready Ride 의 기본을 폐합(순환)에서 「단순 경로」로 바꾼다.** 폐합은 기본에서 내리되 코드·파라미터는 남긴다(나중에 선택지로 되살릴 수 있게). 「순환 경로를 찾지 못해…」 사과 문구는 제거 | 「거리가 짧을 때 순환경로를 만든다는 것 자체가 쉽지 않은 일이다. **순환경로에 목매지 말고 단순 경로로 진행하라**」(Chief). 실측이 이를 뒷받침한다 — 폐합 성공률 **논현동 10% · 서울 0% · 수도권 20곳 5%**(실 Mapbox 호출) | ⚠ **실내 주행이라 출발지로 돌아올 이유가 없다** — 폐합을 기본으로 잡은 것은 감리의 판단 착오였다([실행계획](260923-RTW-Local-First-Ride-실행계획.md) Q3 뒤집음). ⚠ 종전 구조는 **호출 예산 5회를 써서 95% 실패한 뒤에야** 편도로 내려갔다 — 기본을 바꾸면 성공률과 호출 예산을 동시에 얻는다. ⚠ 실패 원인은 자기중복이 아니라 **고정 5방위 표본 + ±20% 오차 허용치**다(지시04 진단). ⚠ 다음 라운드 게이트는 캡처가 아니라 **실측 성공률 ≥90%** 다 — synthetic 측정은 인정하지 않는다(지시02 의 「6/8」이 실제로는 5% 였다). 지시: [지시05](ops/20260923-first_ride/20260924-지시05-Ready-Ride-기본을-단순경로로.md) |
| 2026-09-24 | `[Docs]` | **개발팀장을 클로드 서브에이전트 → 커서로 되돌린다**(지시05부터). 감리·릴레이 규약·캡처 요구·커밋 금지는 그대로 | 「이 방식은 토큰 소모가 너무 크다」(Chief). 서브에이전트가 라운드당 **20~35만 토큰**을 썼다 | ⚠ 커서를 접었던 이유는 비용이 아니라 **관측 불가**였다 — 그때 없던 `await-next`(블록 수신)·`check-amend`(라운드 중 정정)·`PROGRESS.md` 10분 1줄·`watch-status`(Chief 상황판)가 지금은 전부 있다. ⚠ 서브에이전트는 **기계적 작업(impl-simple)** 용도로만 남긴다 — 2줄 가드 같은 건 haiku 가 훨씬 싸다. 인수인계: [HANDOFF](ops/20260923-first_ride/HANDOFF.md) |
| 2026-09-24 | `[Docs]` | **개발팀장을 커서 → 클로드 코드 서브에이전트(`impl-standard`)로 교체한다.** 감리(메인 세션)가 지시서를 쓰고 **Agent 로 직접 발주**하며, 릴레이 폴더 규약·캡처·커밋 금지는 그대로 승계. 커서 전용 수신기 `await-next.mjs` 는 폐지 | 「작업 진행이 매끄럽지 못하다. 일을 하고 있는지 어떤지 확인이 잘 되지 않는다. 커서를 쓰지 말고 클로드 코드의 하위 모델과 역할을 분담하라」(Chief). 실제로 지시02 착수 선언 후 **30분간 로그·코드·캡처·프로세스가 전부 0** 이었고, 파일 신호로 그 프로세스를 깨울 수단이 없었다 — **관측 불가 + 기동 불가** | ⚠ **감리/구현 분리는 유지한다** — 구현은 **컨텍스트를 물려받지 않은 새 에이전트**에 보낸다(`fork` 금지). 발주와 검수를 겸하면 「내가 의도한 대로」 읽어 오해가 통과한다. 감리는 여전히 **캡처·수치·diff 로만** 판정하고 제품 코드를 직접 쓰지 않는다. ⚠ 착수·완료가 **알림으로 확정**되므로 조용히 멈추는 구간이 사라진다 — 멈추면 완료 알림이 안 오는 것 자체가 신호다. ⚠ Chief 상황판(`watch-status.mjs`)·`PROGRESS.md` 10분 1줄은 그대로다. 역할표: [ROLES.md](ops/20260923-first_ride/ROLES.md) |
| 2026-09-24 | `[Economy]` | **Route Token 을 당분간 무제한으로 둔다** — 부여·소모 양쪽. 생성 비용은 `config/routeTokenEconomy.generateCostBase = 0`, 잔액·부족 문구는 UI 에서 내린다. **토큰 시스템 코드는 삭제하지 않는다**(원장·적립·온보딩 지급 유지) | 「route token 의 적정 부여 및 소모에 대해 고민이 필요하다. 현재 시점에서는 모두 무제한으로 설정하라」(Chief). 첫 사용자가 첫 화면에서 경로를 받자마자 토큰을 잃는 구조를 확정하기 전에, 수치 자체를 다시 정해야 한다 | ⚠ 되돌릴 수 있게 **한 값으로 끄고 켠다** — `spendRouteGenerateToken` 은 `cost === 0` 이면 차감을 건너뛰는 분기를 **이미 갖고 있다**(`functions/src/routeTokenCore.ts:218`). 새 우회로를 만들지 마라. ⚠ 서버 규칙을 우회하는 **클라이언트 플래그 금지** — 무제한의 진실은 config 한 곳이다. ⚠ **출시 전 복원 대상**이다([출시 전 확인사항](출시%20전%20확인사항.md)에 등재). ⚠ tier **quota**(생성·저장 수량 한도)는 별개 축이며 이번 결정 범위가 아니다 — 함께 풀지 마라. ⚠ 진행 중이던 [지시02](ops/20260923-first_ride/20260923-지시02-Ready-Ride-생성-방위자동-폐합-3km.md) 의 「첫 1건 무료/재생성 차감」 설계는 이 결정으로 **폐기**됐다(정정 블록 발행) |
| 2026-09-24 | `[Product]` `[UI]` | **첫 화면 기본 카메라를 「자기가 아는 곳을 인식할 수 있는 스케일」로 연다** — 지역을 고르기 전(S0)에도 적용. 임의의 저축척·골목 클로즈업으로 열지 않는다 | 「자기가 아는 곳을 인식할 수 있는 스케일로 한다」(Chief). 지시01 캡처에서 S0 첫 화면이 **10m 줌의 이름 없는 골목**이었다 — 「여기 내가 아는 곳인데」를 목표로 한 라운드의 첫인상이 그것이면 앞뒤가 맞지 않는다 | ⚠ 판정 기준은 줌 숫자가 아니라 **폰 가로(740×300)에서 시·구·동 라벨이 읽히는가** 다. 지시01 이 같은 기준으로 지역 확정 후 줌 **13** 을 채택했다 — S0 도 같은 계열로 맞춘다. ⚠ 기존 「다음 주행 카드」의 앵커 점프(zoom 14)·주행 중 카메라·Quick Camera 1~6 과 **경합하지 않게** 하라. 첫 화면 한 번의 초기값 문제이지 카메라 시스템 변경이 아니다. ⚠ 사용자가 지도를 움직인 뒤에는 다시 점프하지 않는다(지시01 의 1회 가드와 같은 성질) |
| 2026-09-23 | `[Ontology]` | **「Ready Ride」를 사용자 노출 명칭으로 확정** — 영문 그대로 표기(조사 붙임)하고 [Ontology §2.2](260714-RTW-Ontology.md)에 등재했다. 같은 날 앞선 줄의 기본안(내부 용어로만 사용)은 **뒤집힌다** ❌ | 「Ready Ride 사용자 노출 명칭으로 써라」(Chief) | ⚠ 등재하되 **데이터 모델은 만들지 않는다** — 그릇은 Route(필요 시 SavedRoute)이고 이름만 표면에 올린다. ⚠ 인접 표면과 구분이 필요하다: **추천 코스**(목록에서 고름)·**입문 코스**(고정 3경로)·**Ready Ride**(한 건이 제시됨)는 서로 다른 것이며, 첫 화면에 셋이 동시에 뜨면 선택지 과잉이 된다 — 표면 배치는 [실행계획 §3.2·Q2](260923-RTW-Local-First-Ride-실행계획.md)에서 정한다. ⚠ 한글 UI 문장에서 영문 고유명이 섞이는 첫 사례다(기존 UI 명사는 코스·경로·Trail). 카피 작성 시 「Ready Ride를 시작」처럼 조사 붙임을 표준으로 한다 |
| 2026-09-23 | `[Product]` `[Route]` | **Local First Ride 채택 — 첫 사용자에게 자유 선택 대신 「내 지역에서 첫 주행」을 준다.** 경험 순서는 **Recognition → Ride → Claim → Explore → Choose**. 첫 화면은 **권한 요청 없이** `[지역 선택]`+`[현재 위치]` 두 버튼이고, GPS 권한은 사용자가 `[현재 위치]`를 누를 때만 요청한다 | 지도에 도로는 있지만 「왜 여기를 달려야 하는가」가 없다 — Street View 를 쓰지 않기로 한 이상 첫 의미는 유명세가 아니라 **사용자와의 관계성**에서 온다(Chief 원안). 상태보드 §3.5 「목표 계층화 표면 원칙」(07-12 채택)의 구체안 | ⚠ 아직 **계획 채택이며 구현이 아니다**. ⚠ `apps/web/src` 전체에 `geolocation`/`getCurrentPosition` **사용처 0건** — GPS 는 새로 여는 문이고 거부·타임아웃·정밀도 실패 모드를 전부 새로 떠안는다. 첫 화면 권한 프롬프트는 첫 이탈 지점이 되므로 원안 §5(첫 실행 GPS)가 아니라 §6(수동 선택)으로 통일했다. ⚠ 「Ready Ride」는 같은 날 **사용자 노출 명칭으로 확정·[Ontology §2.2](260714-RTW-Ontology.md) 등재**됐다(바로 위 줄) — 다만 **새 데이터 모델은 만들지 않고** 「자동 생성된 Route」로 흡수한다는 원칙은 유지된다. 계획: [Local First 실행계획](260923-RTW-Local-First-Ride-실행계획.md) |
| 2026-09-23 | `[Route]` | **자체 도로 그래프(Edge/Node) 미채택** — Ready Ride 생성기는 기존 `distanceAutoRoute` 확장으로 만든다(방위 자동 선택 + 출발=도착 폐합 + 스코어 항목 추가). 동시에 **v1 Route Score 는 측정 가능한 5개**(거리 오차·cycling 라우팅 성공·출발점 이격·신규 도로 비율·자기 중복 비율)로 한정 | 원안의 Route Generator 는 이미 `functions/src/distanceAutoRouteCore.ts` 에 있다 — 후보 스윕(거리 7×방위 5)·도로 스냅·거리 오차 스코어·목표 절단·호출 예산 13회. 자체 그래프는 OSM 원데이터 파이프라인을 요구해 「전 세계 어디서나」와 가장 비싸게 충돌한다 | ⚠ 원안 §11 의 「복잡한 교차로·막다른 길·주행 부적합 도로·경로 다양성·주행 단순성」은 Directions 응답으로 **판정 근거가 없다** — 넣으면 항상 같은 값이 되어 **게이트가 자동 통과**한다(이 리포 반복 사고). 문서에는 「미측정」으로 남기되 판정 코드에는 넣지 않는다. ⚠ 「신규 도로 우선·중복 감점」은 [5A 중복 구간 정복 손실](ops/route-relay/260904-자동Route-중복구간-정복손실-5A-작업지시서.md)과 **같은 문제**이므로 따로 풀지 않고 합친다. 재검토 조건: 폐합 성공률이 호출 예산 내에서 목표 미달일 때 |
| 2026-09-23 | `[Product]` `[UI]` | **첫 Ready Ride 목표 거리는 5~10 km 가 아니라 기본 3 km(선택 2/5/10 km)** 이고, **기존 입문 코스 3경로는 폐기하지 않고 병존**시킨다. 원안 Phase 3 **Mission 체계는 보류**하고, 대신 원안 Phase 4(Claim 연동)를 앞당긴다 | 현재 프로덕션 입문 코스는 400 m 대다 — 첫 주행에서 검증할 것은 지구력이 아니라 「달리면 내 길이 남는다」의 1회 완결이고, 세션이 길수록 첫 Claim 도달률이 떨어진다. Mission 1/2/3 이 하려는 일은 기구현된 `NextRideCard` 의 파라미터 변경으로 더 싸게 된다 | ⚠ Claim 연동을 앞당기는 근거는 데이터가 이미 있다는 것 — `rides/{id}.conquestResult.newMeters`·`conquest/{uid}` 로 「안 달린 길 우선」은 지금 스코어 한 항목이다. ⚠ 어느 쪽(Ready Ride vs 입문 코스)이 첫 주행 완료율이 높은지는 **관측 후 판단**이며 지금 정하지 않는다. ⚠ 사용자 모집은 **Node 20 런타임 만료(2026-10-30, 상태보드 §3.8)** 뒤로 두지 않는다 — Functions 가 멈추면 경로 계산·Claim 집계가 함께 죽는다. 계획: [Local First 실행계획](260923-RTW-Local-First-Ride-실행계획.md) |
| 2026-09-23 | `[Map]` `[UI]` | **미니맵 도입 — 두 번째 Mapbox 인스턴스가 아니라 SVG 한 장**으로 그린다. 표시는 주행 중(`riding`·`paused`) 상시, 자리는 **좌측**(RTW 아래 ~ 접힌 RouteDock 위), 탭 동작 없음. 동시에 **HUD 좌상단을 한 행으로 재배치**(RTW 최상단 + 계기판 우측 이동 + 계기 간격 절반), **주행 중 접힌 RouteDock 은 캐럿 폭만 남기고 캐럿 자리에 센서 LED** | 「주행이 시작되면 화면 왼쪽에 1/6 크기 미니맵」「RTW 버튼이 최상단에 놓이는 게 낫다」「센서 버튼 텍스트는 불필요 — 캐럿 자리에 LED 로」(Chief). 경로 기하는 이미 `routeGeometry` 로 앱 안에 있어 인스턴스를 하나 더 띄울 이유가 없다 | ⚠ 좌상단 재배치는 **미니맵의 세로 자리를 만드는 선행 작업**이다(두 줄 → 한 줄). ⚠ 「화면의 1/6」은 정사각이 아니라 **화면과 같은 종횡비**다(Chief 정정) — 상자 비를 `innerWidth/innerHeight` 로 런타임 계산하고 폭은 화면 폭의 42%×0.7=29.4% 로 잡는다(면적 ≈1/11.6). 폰 가로는 CSS 높이가 ~275px 뿐이라 세로가 상한이고, 모자란 크기는 **가로로 벌어** 채운다. ⚠ 위치는 **RouteDock rect 실측**으로 붙인다 — CSS 식을 두 벌 두면 `@media (max-height:560px)` 같은 분기에서 한쪽만 바뀌어 **폰에서만 34px 뜬다**(실제로 발생). ⚠ 미니맵 배경에 실제 지도(Outdoors)를 까는 일은 **미완**이다 — 그때 투영을 Web Mercator 로 바꿔야 한다(현재 cos(위도) 근사는 타일 위에서 미끄러진다). ⚠ 지도 위에 얹는 UI 는 `pointerdown`/`touchstart` 를 막지 않으면 터치가 지도로 샌다(이 리포 4회 반복). 계획: [개발계획서](ops/20260923-minimap/20260923-Chief원안-미니맵과-HUD-재배치-개발계획서.md) · 브리핑: [착수 브리핑](ops/20260923-minimap/20260923-미니맵-착수브리핑.md) |
| 2026-09-23 | `[Map]` `[UI]` | **Quick Camera 1~6 작업 종결 — `main` 병합 완료**(`61be6fb`). 1번 3단의 3단계는 10m → **5m 상공**. **줌 비간섭은 구현했으나 제품 판단으로 미적용 종결** | 「(줌 비간섭은) 현재로서는 필요성을 느끼지 못하겠다. 이대로 마무리하겠다」(Chief) | 확정값·경위는 바로 아래 줄(pitch 80·거리)과 [완료보고서](archive/260923-RTW-Quick-Camera-작업-완료보고서.md)에 있다. 줌 비간섭 코드는 남아 있으나 제품에서 쓰지 않는다 — 다시 열 때는 **실 휠 제스처**(Mapbox scrollZoom 감속 구간)에서의 경합부터 확인해야 한다. 이번 수치 검증은 `map.fire` 시뮬이었고 실 휠은 아니었다. 다음 작업: [미니맵 착수 브리핑](ops/20260923-minimap/20260923-미니맵-착수브리핑.md) |
| 2026-09-23 | `[Map]` `[UI]` | **밀착 pitch 80° 확정 · 거리 상한 60m·기본 40m·하한 6.0m · QC 1~6 확정**(원안 5m는 floor 때문에 6m 적용). 67° 폐기 | 「80으로 하자」「거리도 현재 디폴트값으로」(Chief). 5m를 열려 67°로 내렸으나 지평선이 사라져 「재미없다」— 그림이 숫자를 이김 | floor≈5.59m(전고×margin@80°)→눈금 6.0m. `?ridePitch=` 비교용 유지. 지시: [지시09](ops/20260922-new_camera/20260922-지시09-각도-거리-확정-빌드-커밋.md) |
| 2026-09-23 | `[Map]` `[UI]` | **주행 중 Quick Camera 1~6 도입**(Account 좌측). 동시에 **기존 카메라 명칭 반전을 정정**(`rear30`→`forward`·`front30`→`backward`, `30`·`Flat` 유물 접미어 제거)하고 **거리 범위를 5~60m 로 연다** | 「카메라 작업에 우선권을 둔다. 기존 것을 수정하라 — 개발 당시 수정하지 못한 채 현재에 이른 것이니 이참에 수정하자. 거리 범위도 미뤄 둔 작업이니 동시에 진행한다」(Chief) | 새 카메라 시스템을 만들지 않는다 — `getCameraForFollowMode` + `computeRideFollowFraming`(실거리 m → zoom 역산)이 이미 「5m 는 zoom 값이 아니다」를 계약으로 갖고 있어 Quick Camera 는 그 위에 얹는 얇은 층이다. ⚠ 명칭은 **동작이 아니라 이름이 틀린** 경우였다: `rear30`(라벨 "후방")은 카메라가 라이더 뒤에서 진행 방향을 보는 구도로 사용자 어휘로는 「전방」이다. `followMode` 는 영속화되지 않고(useState, localStorage 없음) 리터럴 사용처가 제품 4파일 + e2e 1개뿐이라 마이그레이션이 없다. ⚠ 거리 하한 6.0m 는 임의 상수가 아니라 물리다 — `floor = (0.5+3.1·sin²pitch) × 전고`, pitch 80° 에서 5.59m. **숫자만 5 로 낮추면 라이더가 프레임에서 잘린다.** pitch ≤ 약 67° 로 낮춰야 5m 가 열린다(원안 §12 가 허용한 구도 조정). 상한 40m 는 제품 상수이므로 60m 로 올린다. 지시: [C1 rev.1](ops/20260922-new_camera/20260922-지시01-Quick-Camera-1-6과-기존-카메라-명칭-거리범위-정정.md) · 원안: [개발계획서](ops/20260922-new_camera/20260922-Chief원안-Quick-Camera-1-6-개발계획서.md) |
| 2026-09-19 | `[Map]` | 라이더 GLB 교체의 분해 입력은 **`roadcyclist_s5_wheelfix.glb`**(비병합 210노드). 드로우콜 병합본 `s6_wheelfix_merged` 는 쓰지 않는다 | s5·s6 는 형상이 동일(53,514 tri)하고 병합은 재그룹화일 뿐인데, s6 의 그룹이 앱 14노드와 어긋나 몸통·양팔이 `static__` 에 묶이고 허벅지+정강이가 합쳐져 있다 — 우리가 만들 6개 절단면이 정확히 s6 가 못 푼 자리다. 드로우콜은 귀속 후 14노드 경계로 직접 병합해 얻는다 | [작업계획 rev.3 §0](260919-RTW-라이더-GLB-교체-작업계획.md) |
| 2026-09-19 | `[Infra]` `[Route]` | **경로 표고를 경로당 1회만 외부에 묻는다** — Firestore `routeElevations/{기하해시}` 에 write-once 저장, 조회 순서는 세션 캐시 → 공유 저장소 → Open-Meteo | 「호출 구조를 줄이는 것도 하자」(Chief). 도로의 고도는 변하지 않는다 — 호출량이 「주행 수」가 아니라 **「경로 수」**로 떨어진다 | 공급원 교체(Mapbox·자체 DEM·유료 Open-Meteo)보다 **먼저** 와야 하는 구조다: 갈아끼울 지점이 `SharedElevationStore` 하나로 모이고, 교체 전까지 무료 한도로 버틸 여유도 여기서 나온다. ⚠ 키를 기존 `routeElevationSignature`(좌표 수 + 시종점)로 쓰면 **안 된다** — 한 탭 안에서는 충분해도 전 사용자가 공유하는 저장소에서는 시종점·꼭짓점 수가 같은 다른 경로가 남의 표고를 받아간다(계약 테스트가 이 충돌을 명시적으로 고정한다). 그래서 전체 좌표를 FNV-1a 로 접은 `routeElevationCacheKey` 를 따로 만들었다. 저장은 **create 만** 허용 — 같은 키는 같은 기하이므로 나중 값이 앞 값을 밀어낼 이유가 없다. 라이브러리는 Firestore 를 모르고 인터페이스만 받는다(순수 유지·가짜 저장소로 시험). 규칙은 눈으로 읽지 않고 에뮬레이터에 실제로 써 봤다(`npm run test:ride-elevation-rules`, 8/8) |
| 2026-09-19 | `[Infra]` | **e2e 가 실 Open-Meteo 를 쓰지 않는다** — 전역 auto 픽스처로 고도·forecast 스텁, 못 잡은 요청은 차단하고 teardown 이 「0건」을 검사 | 스펙 36개 중 스텁이 하나뿐이라 나머지가 실행마다 실 API 를 때렸다(경로 1회 = 72콜). 하루치 반복이 일일 한도를 태워 429 → 「고도 데이터를 불러오지 못했습니다」가 **코드 회귀로 오인**됐다 | 「스텁을 깔았다」는 사실은 보장이 아니다 — 조용히 안 걸려도 전부 초록이다. 그래서 ① 차단 catch-all 이 실 네트워크 유출을 막고 기록하며 ② 픽스처 teardown 이 모든 스펙에서 기록 0건을 검사하고 ③ 전용 가드 스펙이 가로채기·우선순위·차단·`newContext` 커버리지를 검산한다. **우선순위는 실측했다**: Playwright 는 나중에 등록된 라우트가 이기므로 스펙이 본문에서 거는 시나리오 곡선은 살아남는다. peer 주행자는 스펙이 `browser.newContext()` 로 직접 만들어 기본 `page` 라우트로 안 덮이므로 생성 지점을 감쌌다. 함께: 429 를 `ElevationQuotaError` 로 분리해 「고도 API 일일 한도 초과」로 표시한다 — 다음에 같은 일이 나도 회귀로 오해하지 않게. 상세: [작업지시서](ops/ride-relay/260919-표고-API-한도-소진-재발방지-작업지시서.md) |
| 2026-09-18 | `[UI]` | **코칭 메시지에 2중 테두리** — 안쪽 1px + 바깥쪽 2px, 둘 다 **완전 불투명 검정**(`--rtw-map-text-keyline-strong`) | 「흰색이라서 흰색이 배경인 맵에서 잘 보이지 않는다. 바깥쪽 테두리는 텍스트 색과 대비가 확실한 컬러를」(Chief) | 배경을 걷어낸 대가다 — 대비를 글자 자신이 만들어야 한다. 계기판(0.95rem)에는 1px 키라인으로 충분했지만 코칭은 0.78rem 이라 획이 가늘어 밝은 바탕에서 묻혔다. **반투명은 밝은 바탕에서 회색이 되어 대비가 무너진다** — 바깥 테두리를 `#000` 완전 불투명으로 둔 이유. 지도를 흰색·밝은 베이지로 덮고 3배 확대 촬영해 확인(계기판 때와 같은 방법). 계기판은 기존 토큰 유지 |
| 2026-09-18 | `[UI]` | **코칭 메시지를 계기판과 같은 표기로** — 검은 알약 폐기, 지도 위에 글자만. R1 칩도 배경 없이 골드 글자만 | 「코칭 메시지의 텍스트 스타일을 주행계기판 HUD 와 같은 스타일로. 현재의 검은색 배경이 어울리지 않는다」(Chief) | 알약이 **두 파일**에서 왔다 — `MapHud.css` 의 배경·테두리·블러와 `boxcycle-theme.css` 의 글래스 톤 그룹 셀렉터. 한쪽만 지우면 그대로 남는다. 외곽선은 토큰 `--rtw-map-text-keyline` 하나로 묶었다 — 계기판과 코칭이 값을 각자 복사해 갖고 있으면 다음에 한쪽만 바뀐다. 주행 중 `opacity: 0.82` 도 걷어냈다: 배경이 있을 땐 양보였지만 배경이 없는 지금은 흐리기가 곧 대비 손실이다(위계는 글자 크기가 맡는다) |
| 2026-09-18 | `[Map]` | **라이더 이름표·경로 핀이 지도 조작을 막던 것 수정** — `pointer-events: none` 에 `!important` | 앵커 0.6 으로 라이더를 내리자 이름표·S/E 핀이 표고 그래프 면까지 들어와 **21 지점**을 막는 것이 계약에 걸렸다 | 둘 다 이미 `pointer-events: none` 을 선언하고 있었으나 Mapbox 자체 `.mapboxgl-marker` 와 특이도(0,1,0)가 같아 밀리고 있었다 — 내 위치 마커(09-17)와 **똑같은 함정**이 세 번째로 나왔다. 클릭 핸들러가 없는 표시 전용임을 확인하고 못 박았다. 앵커를 바꾸지 않았으면 이 결함은 계속 숨어 있었을 것이다 — 요소를 옮기면 그 아래 있던 문제가 드러난다 |
| 2026-09-18 | `[UI]` `[Map]` | **주행 중 라이더를 화면 「위 6 : 아래 4」 자리로.** 정중앙(0.5) → `RIDE_RIDER_SCREEN_ANCHOR = 0.6` | 「지난 경로는 상대적으로 중요성이 덜하지만 주행해야 할 앞의 경로는 보다 길게 보여줄 필요가 있다」(Chief) | 화면 위쪽이 진행 방향이므로 **카메라 겨냥점을 앞으로 밀면 라이더가 아래로 내려온다**. ⚠ 함정: 주행 기본 모드 `topDown` 은 `offsetBearing: null · distanceM: 0` 이라 `computeRideFollowFraming` 이 **계산 없이 조기 반환**한다 — 거기에 보정을 안 넣으면 순수 계약은 통과하는데 화면은 그대로다(실제로 그랬다: 실측 0.484). 거리 개념이 없는 경로는 `spanM` 으로 배율을 못 구하므로 **줌에서 m/px 을 얻는다**(`rideRiderAnchorBiasMAtZoom`). 보정 방향을 정하려면 화면 위쪽 방위가 필요해 `screenUpBearing` 을 스냅·틱 두 호출부에서 넘긴다. 적용 후 실측 0.681(요소 기준). 정확한 0.6 은 순수 계약이 수식으로 잡고, e2e 는 밴드(0.55~0.78)로 회귀만 막는다 — 시험 환경은 `.map-view { min-height: 320px }` 가 뷰포트(275px)보다 커 요소 비율이 화면 비율과 다르다 |
| 2026-09-18 | `[Route]` `[UI]` | **주행이 시작되면 경로 설정 표면을 접는다** — 팝업·도크 닫기 + 목표 거리 원·방향 클릭 임시 표시·세션 제거 | 「주행이 시작되면 경로설정 팝업창을 닫는다 / 도착지점 표시 좌표와 마커를 제거한다」(Chief) | **신호를 잘못 고르면 개발 빌드에서 통째로 안 걸린다**: `lockRouteWorkspaceDuringRide` 는 `import.meta.env.PROD && …` 라 dev·시험에서 항상 false 다. 이미 있던 `rideActive`(주행 중)를 쓴다. 정리는 `finalizePickClose`(팝업·도크·원을 한 번에) + `disarm()` 두 개로 끝난다 — 손으로 하나씩 지우지 않는다. `disarm()` 호출 자리는 effect 가 아니라 `handleStartRide` 다: 주행 시작은 파생 상태가 아니라 사용자 행동이고, effect 안 동기 setState 는 연쇄 렌더를 부른다(`react-hooks/set-state-in-effect` 가 잡았다). 계약은 **팝업을 먼저 열어 두고** 주행을 시작해 닫히는지 본다 — 닫힌 채로 시작해 「없다」를 확인하면 축퇴다 |
| 2026-09-18 | `[Route]` | **「거리」 체크박스를 끄면 세션까지 끝낸다.** `setDistanceDirectionMode(false)` 가 `releasePickArm()` → **`disarm()`** | 「체크박스는 단순 preference 가 아니라, OFF 되는 순간 살아 있는 거리 기반 자동 End 선택 세션까지 종료시키는 확실한 ON/OFF 컨트롤이어야 한다」(Chief) | 종전에는 선호만 끄고 `sessionActive` 는 살려 뒀다 — 클릭 해석과 팝업 초기값이 세션을 보므로, 「여기에서 계속」으로 켜진 세션이 남으면 체크를 풀어도 지도 클릭이 방향 선택으로 먹힐 수 있었다. ❌ 1차(09-18)는 **고쳐지지 않았다** — 재현 없이 「명세에 맞췄다」로 넘어간 것이 원인. 실제 원인은 따로 있었다: 팝업이 브리지를 **만들 때 한 번 붙잡아** 쓰고 있어(`const mapBridge = getDistanceAutoRouteMapBridge()`), 등록 전이거나 재등록된 뒤에는 `?.` 로 **조용히 무시**돼 훅이 해제를 영영 못 들었다. 같은 파일의 다른 자리들은 이미 호출 시점에 다시 찾고 있었는데 이 한 곳만 캡처본이었다. 2차에서 ① 브리지를 호출 시점에 재조회 ② 끌 때 원 소유자에게 직접 `onClearDistanceAutoRouteCircle()` 을 더해 해결. 
| 2026-09-18 | `[Infra]` | **자동 Route 안내 문구가 바뀐 줄 모르고 5개 파일이 낡은 문구를 찾고 있었다** — `ride-continue-phase-c` 의 오래된 2건 실패 원인 | 「거리 ON 인데 방향 안내가 안 보인다」를 파고들다 발견 | 현재 문구는 「{N} km 반경의 원 주변 도로를 선택하세요」(`distanceAutoRouteErrors.ts`)인데 스펙들은 「도착하고 싶은 도로 위 지점을 클릭…」을 기다리고 있었다. 5곳(e2e 3 · scripts 2)을 갱신하니 phase-c 가 **2 passed** 로 돌아왔다. 교훈: 사용자 문구를 셀렉터로 쓰면 문구를 바꿀 때 함께 옮겨야 한다 — 안 옮기면 조용히 빨개진 채 「원래 그런 것」이 된다 |
| 2026-09-18 | `[UI]` | **센서 시트 「센서 연결」·「센서 없음」 버튼 높이 80% 축소** — 28.17 → 22.52px (70% 로 줄였다가 「너무 많이 줄었다」는 판단으로 되돌림) | 「공간을 아껴 써라」(Chief) | 세로 패딩만 줄이면 글자가 테두리에 닿는다 — 기본 `line-height: 1.5` 가 15.4px 를 먹기 때문. `line-height: 1` 로 글자 상자를 글리프 크기(10.26px)까지 조인 뒤 남은 몫을 패딩(0.28rem)으로 나눴다: 2px(테두리) + 10.26 + `0.38rem` × 2 ≈ 22.5px(**79.9%**). 계약은 밴드(21~24px). ⚠ 44px 터치 규칙과는 더 멀어진다 — 캐럿 가로 12.55px 와 같은 성격의 잔여 위험(Chief 지시로 축소) |
| 2026-09-18 | `[UI]` | **RouteDock 가로 80% 축소** — 패널 21rem → 16rem(폰 가로 17.5 → 13.32rem) | 「공간을 아껴 써라」(Chief) | **패널만 20% 줄이면 dock 전체는 83.8% 에 그친다** — 캐럿(12.55px)과 테두리(2px)는 고정이라 같이 줄지 않기 때문이다. 지시는 「dock 가로」이므로 **전체 기준**으로 역산했다: 243.00px → 194.40px 목표 → 패널 179.85px = 13.32rem. 결과 194.36px(**80.0%**). 계약은 밴드(188~201px)로 고정. 대가: 출발·도착 주소가 말줄임(…)으로 잘린다 — 폭을 줄이면 피할 수 없고, 전체 주소는 탭하면 팝업이 보여 준다 |
| 2026-09-18 | `[UI]` | **스피드 바를 종전의 70% 로, 시트 폭도 그만큼 축소.** 시트 `22rem` → `18.17rem` | 「공간을 아껴 써라」(Chief) | 바는 `flex: 1 1 auto` 라 행에서 남는 폭을 혼자 먹는다 — 옆 요소(±·숫자·SPD)가 모두 고정 폭이므로 **시트에서 뺀 만큼이 그대로 바에서 빠진다.** 그래서 바에 폭을 직접 주지 않고 시트 폭만 조정했다. 먼저 실측하고(바 172.20px / 시트 297.00px = 22rem) 목표를 계산한 뒤 적용 — 결과 바 120.48px(**70.0%**), 시트 245.28px. 계약은 폰 가로 기준 밴드로 고정(바 112~129px, 시트 < 255px) |
| 2026-09-18 | `[UI]` | **센서 설정 시트 정리 6건** — ① 제목과 연결 상태를 한 줄(`케이던스 센서  연결 안 됨`) ② 버튼 이름 「센서 연결」·「센서 없음」 ③ 「현재 입력」 줄과 선택 전 안내문 제거 ④ SPD / km/h 두 줄 ⑤ 숫자 입력 스피너 제거 ⑥ 숫자 입력과 「＋」 위치 교체 | 「불필요하다」·「공간을 아껴 써라」(Chief) | ①은 `cadenceSensorShortStatus()` 를 새로 뒀다 — 기존 `cadenceSensorStatusLine` 은 「연결됨 · 페달을 돌려…」처럼 길어 제목 옆에 못 붙는다. ③에서 안내문을 통째로 지우지 않았다: 선택 전(`choice-required`) 문구만 빼고 「연결하는 중」·「페달을 돌려 확인」은 남긴다 — 앞의 것은 버튼 둘이 이미 말하지만 뒤의 둘은 버튼으로 알 수 없는 **진행 상태**다. ⑤는 `type="number"` 를 유지하고 CSS 로 화살표만 숨겼다 — type 을 바꾸면 `role=spinbutton` 이 사라져 e2e 셀렉터가 통째로 깨진다. ②의 파급이 컸다: 「체험 속도로 준비」를 **16개 파일**이 셀렉터로 쓰고 있었고, 그 버튼을 설명하던 주석 4곳도 함께 옮겼다 |
| 2026-09-18 | `[UI]` | **RouteDock 공간 정리 5건** — ① 경로가 잡혔는데 센서 미준비면 SENSOR 칩이 깜빡여 안내 ② 캐럿 폭 60%(1.55→0.93rem) ③ 칩·주소를 왼쪽 끝까지 밀착(좌측 padding 0) ④ Go 를 줄의 오른쪽 끝으로 ⑤ 삭제(X) 오른쪽 2px 여백 | 「공간을 낭비하지 마라」·「사용자에게 센서 설정을 안내」(Chief) | 깜빡임 판정은 **App 이** 한다(`routeGeometry && !routeLoading && !rideInputReady`) — dock 은 `routeGeometry` 를 모른다. 준비가 끝나면 스스로 멎는다(안내가 소음이 되지 않게). 계약은 **켜지는 쪽과 꺼지는 쪽을 모두** 본다 — 한쪽만 보면 「항상 켜짐」·「항상 꺼짐」이 통과한다. Go 는 CSS `order` 가 아니라 **DOM 순서**를 옮겨 키보드 순서가 보이는 대로 가게 했다. 실측: 캐럿 12.55px(=60.0%), 칩 x 23.20 = 캐럿 오른쪽 끝 23.20(빈틈 0), Go 우측 여백 5.4px, X 오른쪽 여백 2.0px. ⚠ 캐럿 **가로**는 12.55px 로 44px 규칙에 한참 못 미친다 — 세로 44px 은 유지되나 가로는 원래도 20.9px 였고 이번에 더 줄었다(Chief 지시) |
| 2026-09-17 | `[UI]` | **「다음 주행」 카드를 좌하단 스택 밖 우하단으로.** 자리는 기존 UI 격자에 맞춘다 — 오른쪽 끝 = 우상단 계정·맵 버튼, 아래 끝 = RouteDock 아래 끝. 더해서 센서 칩 문구 `CAD` → `SENSOR` | 「카드가 뜨면 RouteDock 을 위로 밀어올리고 닫으면 내려온다. **창의 위치가 흔들려서는 안 된다**」(Chief) | 좌하단 스택(2026-09-16 신설)은 **하단 기준**이라 카드가 붙으면 그 위 dock 이 밀린다 — 겹침을 막으려 묶은 구조가 이번엔 흔들림을 낳았다. 카드만 내보내 자리를 갈랐다(남은 dock·입문 CTA 는 서로 배타적이라 스택으로 충분). **「우하단」이라는 방향만으로는 부족하다** — 처음에 임의 여백(컨트롤 2.9rem·축척 1.5rem)을 붙여 기준선을 벗어났고 Chief 가 지적했다. `--hud-pad`·`--map-bl-pad`·`--next-ride-pad` 가 모두 같은 `clamp(0.45rem, 1.4vw, 0.85rem)` 이라 여백을 걷어내면 정확히 일치한다. 실측: 카드 오른쪽 끝 680.34 = 계정·맵 680.34, 카드 아래 끝 260.63 = dock 260.63, **dock 이동 0**. 폰 가로(세로 275px)에서는 카드 상단이 우측 지도 컨트롤과 14×32px 겹치나 **버튼은 모두 눌린다**(각 버튼 중심 `elementFromPoint` 로 계약화) |
| 2026-09-17 | `[UI]` | **주행 결과에서 「도착」·「완주」 배지 제거**, 「거리」 라벨 제거, 「새 도로」를 한 줄로(라벨이 숫자 왼쪽), 보조 수치 +2pt, 시트 배경 10% 투명. 더해서 **완주면 분자를 전체거리로 맞춘다** | 「사용자는 주행을 마치는 순간에 완주를 인식하고, 결과의 숫자를 보고 역시 완주를 확인할 수 있다」(Chief) | 그 근거가 성립하려면 숫자가 완주를 말해야 하는데, 완주 인정 기준이 100% 가 아니라 **98%**(`ROUTE_COMPLETION_RATIO_THRESHOLD`)라 그대로 두면 완주해도 「0.99 / 1.00」 로 보였다 — 배지를 뺀 자리에서 이건 완주 여부를 알 수 없게 만든다. RC3 e2e 가 실제로 `0.99 / 1.00` 로 이를 잡아냈다. `routeCompleted` 면 분자를 전체거리로 고정한다. 미완주 진행률 배지(`N% → M%`)는 남긴다 — 숫자만으로는 **이전 대비 얼마나 늘었는지**를 알 수 없기 때문. 보조 수치는 `0.72rem`(폰 가로 실제 약 9.7px) → `calc(0.72rem + 2pt)`, `pt` 는 절대 단위라 루트 폰트가 바뀌어도 정확히 2포인트 |
| 2026-09-17 | `[UI]` | **주행 결과는 「이번 주행」 세 값만 — 주행거리 / 총거리 / 새 도로.** 「오늘」(하루 누적) 제거. 단위 km 는 **헤더가 한 번만** 말하고 숫자에는 붙이지 않는다(`0.18 / 0.18`, `0.13`) | 「이 화면에서 오늘을 보여줄 필요는 없으며 방금 주행한 결과를 보여주도록 한다」(Chief) | 주행거리는 **경로상 누적 위치**다(세션 거리가 아니다 — Chief 확정). 이어달리기로 중간부터 재개해 끝낸 경우에도 「완주 = 0.18 / 0.18」 이 성립해야 하고, 주행 중 계기판의 「누적 / 전체」와 같은 숫자여야 화면이 이어진다. `result.progressRatio × routeDistanceMeters` 로 계산 — 새 prop 불필요. `formatNewRoadHero` 도 `+0.80 km` → `0.80` 로(단위·부호 제거), 계약 3건 갱신. 「50m 미만 미표시」는 유지. 실측(690×275): `scrollHeight === clientHeight`(125=125), 보이는 줄 4개 |
| 2026-09-17 | `[Infra]` | **시험 주행은 200m 이하 경로로.** 퍼블릭 최단이 414m 라 조건을 못 맞춰, 180m SavedRoute 를 에뮬레이터에 심어 쓴다 | 「테스트 경로를 짧게 하는 이유가 시간을 줄이기 위해서다. 긴 경로를 완주하면 그 시간을 어떻게 감당하나」(Chief) | 폐기 임계(100m 초과)에 80m 여유를 두면서 체험 속도 상한 50km/h 로 약 13초면 완주한다. 규율 자체를 계약으로 박았다 — `expect(totalKm).toBeLessThanOrEqual(0.2)` 가 긴 경로로의 회귀를 잡는다 |
| 2026-09-17 | `[UI]` | **주행 결과 시트 11줄 → 4줄.** 설명문 2종(「…내 도로망에 더해졌어요」·「다음 출발점이 저장되었습니다」)과 「끝점에서 새 경로」 CTA 제거, 「경로를 완주했습니다」는 **「완주」 배지**로 축약, 새 도로+오늘을 2열, 저장 폼을 한 줄로 | 「폰 가로가 주 용도다. 작은 화면을 극단적으로 효율적으로 써야 한다. 강조할 것은 강조하되 **스크롤을 강요하는 것은 용납할 수 없다**」(Chief) | 실측(690×275): 시트 `scrollHeight === clientHeight`(158=158), 보이는 줄 **5개**, 시트가 뷰포트 안에 완전히 들어옴. **정보를 버린 게 아니라 자리를 바꿨다** — 명시 제거 3건 외에는 전부 남고, 저장 상태 칩도 독립 줄 대신 보조 수치 줄에 합류. 「끝점에서 새 경로」는 **기능이 아니라 이 시트의 진입점만** 사라졌다(`handleStartRouteFromAnchor` 는 UserInfoSheet·지도 마커가 계속 쓴다) — 그래서 계약을 지우지 않고 **살아 있는 진입점(사용자 정보 → 마지막 주행 → 「여기서 새 경로」)으로 재조준**했다. 주의: `confirmed_zero` 에서 hero·statusCopy 가 모두 null 이라 설명문을 걷어낸 뒤 **테두리만 남은 빈 박스**가 생겼다 — 「50m 미만 미표시」 결정을 뒤집지 않으려면 숫자를 채울 게 아니라 칸을 접는 것이 맞다 |
| 2026-09-17 | `[UI]` `[Conquest]` | **「새 도로」 펄스를 내 위치 마커와 같은 박자·위상으로 동기화.** 값 변화마다 터지던 0.35s 팝 폐기 | 빠르게 달릴수록 값 갱신이 잦아 주기가 짧아져 촐랑거렸다 — **속도가 박자를 결정**하고 있었다. 펄스가 아니라 갱신 알림이었던 셈(Chief) | `key={conquestLiveMeters}` 가 값마다 요소를 갈아끼워 애니메이션을 재시작시켰다. 이제 새 도로를 먹는 동안 2.2s 무한 펄스, 끊기면 한 박자 더 뛰고 정지. **CSS 만으로는 위상을 못 맞춘다** — 애니메이션은 요소에 적용된 순간부터 돌아 마운트 시각이 다르면 위상이 어긋난 채 고정된다. 공통 시계로 계산한 음수 `animation-delay` 로 같은 격자에 못 박는다(`lib/ridePulse.ts`, 박자는 `--rtw-ride-pulse-period` 토큰). 계약: `getAnimations()` 의 정규화 `progress` 차이 < 0.03 + duration 일치 + `infinite`(= 값 기반이 아님). 주의 — `currentTime` 은 「시작 이후 경과」라 음수 delay 가 안 들어가 위상 비교에 쓸 수 없다 |
| 2026-09-17 | `[UI]` | **주행 결과에서 경로 그림(「이번에 달린 길 / 내 도로망」) 제거.** 컴포넌트 파일까지 삭제 | 「그 이미지를 봤을 때 어떤 의미인지 와닿지 않는다. 효용 없는 정보로 아래 내용을 스크롤하게 만든다」(Chief) | 실측: 시트 `scrollHeight === clientHeight`(393=393) — **스크롤이 사라졌다**. `conquestTraceGeometries` 계산부는 지도 렌더가 따로 써서 유지. `RideEndResult.sessionPathLngLat` 는 소비자가 없어졌으나 주행 종료·영속화 로직을 건드려야 해 별건으로 남김 |
| 2026-09-17 | `[UI]` | **계기판은 패널이 아니라 지도 위에 뜬 숫자다.** 배경·테두리·그림자·블러·구분선 폐기, 글자마다 8방향 1px 키라인. 셀마다 **고정 폭**(`flex: 0 0`)으로 값 자릿수가 바뀌어도 이웃이 안 밀린다 | 「시커먼 배경으로 맵을 왕창 가리기보다」(Chief). 또 「평균 9.9 → 10.0」 한 자리 차이로 오른쪽 계기가 통째로 밀리고 당겨져, 주행 중 같은 자리에서 같은 값을 못 찾았다 | 배경을 지우면 대비를 **글자 자신이** 만들어야 한다 — 부드러운 그림자 3겹은 밝은 배경 3배 확대 실측에서 판독 불가였다(1차 실패). 키라인으로 바꾸니 값은 해결됐으나 8px 라벨은 1px 선이 한글 획을 메워 뭉갰다(2차) → 작은 글자만 촘촘한 헤일로로 분리. `tabular-nums` 는 **같은 자릿수**끼리만 맞춘다 — 자릿수가 바뀌면 소용없어 고정 폭이 필요했다. 고정 폭 셀에서 **좌우 패딩은 글자를 옮기지 않는다**(가운데 정렬) — 상자만 넓혀 순수하게 간격으로만 나타나므로, 패딩을 걷고 그만큼 폭을 줄여 간격을 절반으로(25→12.5px). 계약 `hud-distance-elevation-shots.spec.ts`: 주행 중 26회 표본에서 셀 `offsetLeft`·`offsetWidth` 단일값 + 값이 실제로 변했음을 먼저 세우고(축퇴 방어), 「평균」에 `8888.8888` 을 주입해도 이웃이 안 밀림을 확인. `getBoundingClientRect` 가 아니라 `offsetLeft` 인 이유는 「새 도로」 팝(`scale(1.16)`)이 레이아웃 밀림과 섞이기 때문 |
| 2026-09-17 | `[UI]` | **계기판을 상단 중앙 → 좌상단 첫 줄로, RTW 메뉴를 둘째 줄로 고정.** 지명검색 버튼은 HUD 에서 빼 **MENU 헤더 칩**(「설정」 왼쪽)으로 | 「라이더 진행 방향의 지도를 계기판이 가린다」(Chief). 지명 검색은 전폭 행 하나로 메뉴 본문 한 줄을 통째로 썼다 | 계기판(`showMetrics`)과 RTW 스택(`showMenuTrigger`)은 **표시 조건이 다르다** — 절대 위치 슬롯 2개를 매직 오프셋으로 쌓으면 캡슐 높이가 바뀔 때(2줄→1줄) 즉시 어긋나므로, 바깥 슬롯 하나 안에서 각각 조건부로 렌더하고 `.map-hud__tl-metrics` 가 **비어도 높이를 예약**한다. 실측: RTW y = 41.375 로 「경로 없음·코스 로드·주행 중」 세 상태 모두 동일. `.map-hud__tc`(top-center)는 이름이 거짓이 되어 폐기 |
| 2026-09-17 | `[UI]` `[Map]` | **표고 그래프 「면 전체」가 지도 조작을 통과시킨다** — 막던 것은 오버레이가 아니라 **그 위에 얹힌 `.map-hud__bc`(코치 토스트 슬롯)**. 이 슬롯과 `.hud-coach`, Mapbox 축척 바, 내 위치 마커를 표시 전용으로 전환 | 09-17 에 오버레이를 `pointer-events: none` 으로 바꿨는데도 Chief 는 여전히 막힌다고 했다 — **오버레이는 이미 통과하고 있었다**. 「그래프를 조작할 일이 없으니 이 영역을 포함해 맵 터치가 되게 하자」(Chief) | 주행 중 하단 절반 격자 실측: 차단 95 지점 중 코치 토스트 계열 **27**·축척 바 6·내 위치 4, 나머지는 정당한 버튼·dock. 종전 계약이 초록이던 이유는 **오버레이 정중앙 한 점만, 그것도 주행 전(idle)** 에서 봤기 때문 — 코치 토스트는 주행 중에만 뜬다. 내 위치 마커는 이미 `none` 이었으나 Mapbox `.mapboxgl-marker` 와 특이도(0,1,0)가 같아 밀리고 있어 `!important` 로 못 박았다. `MapHud.css` 첫머리 주석이 규정한 계약(「슬롯은 none, 내부 위젯만 auto」)과 코드(`.map-hud > * { auto }`)가 반대인 것이 근본 원인 — 전면 교정은 회귀 범위가 커 하단 중앙만 되돌렸다(우하단 버튼 뭉치 빈 공간 2 지점은 잔여). 계약 `touch-targets-44.spec.ts` 가 **주행 중 오버레이 면 960 지점**을 훑어 차단 0 을 단언(표본 400 미만이면 계측 실패) |
| 2026-09-17 | `[UI]` `[Map]` | **HUD 거리 셀을 「누적 / 전체」 한 줄로** — 세션 거리 별도 표기 폐기(U4 이중 표기 뒤집음), **진행률 %는 표고 그래프 마커로 이전**(`NN% covered`). 전체거리(목표)는 흰색 볼드로 승격, 종점에 빨간 깃발. RouteDock caret 은 「경로」 글자를 빼고 `>` 만 | 세션 거리와 누적 거리의 차이는 **이어 달리기 offset 뿐**이라, 처음부터 달리면 두 숫자가 주행 내내 **항상 동일**했다 — 히어로 자리를 2줄로 중복이 쓰고 있었다. 전체거리는 「목표」라 되새길 값이고, 진행률은 이미 절반이 구현된 표고 그래프가 새 그래프 없이 맡을 수 있다(Chief) | 표고 그래프의 %는 `getProgressRatioOnRoute()` 한 곳에서 나와 **점 위치와 숫자가 어긋날 수 없다** — HUD 의 `누적/전체` 는 각각 2자리 반올림이라 `0.03/0.16`(=19%)에 22% 가 붙는 불일치가 있었다. 라벨은 SVG **밖** HTML — `preserveAspectRatio="none"`(420×100 → 약 386×106) 이라 `<text>` 는 가로로 눌린다. 최고점에서는 위로 뺄 높이(8.5px)가 라벨(16px)보다 작아 메타 행을 침범 → `yPct<20` 이면 아래로 뒤집는다. 계약 `hud-distance-elevation-shots.spec.ts` (표고 응답을 스텁해 **최고점 케이스를 인위 재현**, 겹침·앵커·1줄 높이 실측) |
| 2026-09-17 | `[UI]` `[Map]` | **표고 그래프를 표시 전용으로(`pointer-events: none`) + HUD 터치 타깃 44px.** 버튼의 **보이는 크기는 그대로** 두고 투명 히트 영역만 넓힌다(`::after`) | 폰에서 메뉴를 누르려는데 지도 지점 팝업이 떴다. 표고 그래프는 배경이 투명한데 터치만 삼켰다(Chief) | 실측: 표고 386×120px = 화면의 **24.4%** 를 막고 있었다(하단 중앙). HUD 버튼은 rem 이라 폰 가로에서 25.2~32.4px 로 축소 — 자기 규칙(인수인계 §2.2, 44px·px 고정) 위반. 계약 `touch-targets-44.spec.ts` 가 `elementFromPoint` 로 **경계 2px 바깥이 버튼에 잡히는지**까지 확인 |
| 2026-09-16 | `[Map]` `[UI]` | **경로선 흰 테두리(casing) 폐기 — 폭 차이로 둘 다 보이게.** 경로 설정 중 내 도로망 폭에 **하한 7px**(경로선 4px 양옆 1.5px 띠), 불투명도 0.45 → 0.6 | 테두리(7.5px)가 내 도로망(7px)을 통째로 덮어 **경로선은 살고 내 도로망이 죽었다** — 문제를 반대편으로 옮겼을 뿐(Chief) | 하한이 필요한 이유: 경로선은 줌 무관 4px 인데 내 도로망은 z12 에서 4px·z8 에서 2.8px — 낮은 줌에서 경로선이 더 굵어 통째로 가려진다. 하한은 **stop 값에 직접** 건다(`["max", <interpolate>, n]` 은 Mapbox 가 거부 — 실주행 e2e 콘솔 오류로 확인) |
| 2026-09-16 | `[Map]` `[Conquest]` `[UI]` | **내 도로망 ↔ 경로선 강조를 단계별로.** 주행 중=궤적이 위(0.95, 종전 그대로) · **경로 설정·확인 중=경로선이 위, 내 도로망 0.45** · 경로 없는 화면=종전 그대로. 더해서 **경로선에 흰 테두리(casing)** 신설 | 내 도로망이 z16 에서 7px·0.95 로 4px 경로선 **위**를 덮어, 이미 달린 도로에서는 빨간 경로선이 통째로 사라졌다 | 09월 초 「궤적을 경로선 위로」 결정은 **주행 중** 요구였고 이번 불편은 **설정 중** 요구다 — 하나의 순서로 덮으려다 한쪽이 죽었다. 전역 투명도를 낮추지 않은 이유: 「넓은 맵에서 한눈에」라는 내 도로망 설계 의도가 상시 약해진다. 계약 `conquest-emphasis-contract.test.ts` |
| 2026-09-16 | `[UI]` | **우상단 센서 칩 폴백 완전 제거** — RouteDock 을 `idle` 에도 띄워(숨기는 곳은 게이트·결과 시트뿐) 칩이 항상 dock 에 산다. 우상단은 **모든 stage 에서** 계정·맵 둘. 좌하단은 `MapBottomLeftStack` 으로 dock·「다음 주행」 카드·입문 CTA 를 세로로 쌓는다 | 폴백이 남은 이유는 `idle` 에 dock 이 없어서였다 — 그 stage 하나 때문에 칩이 두 자리를 오갔다 | 미병합 `fix/ui-declutter-6a`(6A-R2, 09-07)의 아이디어를 되살린 것. 단 그 브랜치가 재지 않은 **카드 겹침**을 실측해 0 확인. `idle` 은 접힌 채로 뜬다. 계약 `sensor-chip-slot-contract`·`sensor-chip-route-dock-6a` |
| 2026-09-16 | `[UI]` | **경로 목록 정렬을 세 목록 공용으로**(내 경로 · 입문 · 퍼블릭). 비교 규칙은 `lib/routeListSort`, 컨트롤은 `RouteSortSelect` 하나. 세 목록 모두 기본값 **`최근순`** | 정렬이 `SavedRoutesPanel` 안에만 있어 퍼블릭 32개는 정렬 없이 훑어야 했다 | 「최근순」의 시계는 목록마다 다르다 — 내 경로는 `updatedAt`, 퍼블릭은 **등록 시각**(`routePublications.createdAt` → `publishedAtMs` 신설). 입문 허브는 시각이 없어 최근순이 원래 순서로 물러나 Basic 1·2·3 이 지켜진다. 항목 2개 이하면 정렬 바 미표시. 계약 `route-list-sort-contract.test.ts` — 거리순 방향(긴 것이 위)까지 고정 |
| 2026-09-16 | `[UI]` | **MENU 정리 B단계 — 「내 경로」를 별도 모달로.** 공식 코스와 **같은 껍데기**(`RouteListModalShell`)를 쓴다. 목록·검색·정렬·필터는 `SavedRoutesPanel` 이 그대로 소유 — 자리만 옮긴 이동이지 재작성이 아니다 | 더 많고(수십 개) 더 자주 쓰고 이름변경·삭제·퍼블릭 등록까지 달린 내 경로가, 정작 공식 코스보다 좁은 자리(61px)에 있었다 — 우선순위가 거꾸로 | 목록 가용 높이 **61 → 184px(3배)**. MENU 안 인패널 목록 제거. 계약 `menu-declutter-a.spec.ts` |
| 2026-09-16 | `[UI]` | **MENU 정리 A단계** — 섹션 라벨 2줄·탭 계층(공식경로/내 경로)·「이벤트」·「공식」 키커 제거. 경로 출처를 **칩 한 줄**로 통합(`입문 · 퍼블릭 N · 내 경로 N`) | 폰 가로에서 크롬이 패널 275px 중 **214px(78%)** 을 먹고 목록에 61px 만 남았다. 그중 51px 은 조작 없는 순수 라벨 두 줄 | 실측 크롬 **214 → 131.9px(−38%)**, 목록 가용 61 → 약 143px. 계약 `menu-declutter-a.spec.ts`. 「이벤트」는 `준비 중` 한 줄만 그리던 빈 껍데기 — 실제 이벤트가 생기면 되살린다 |
| 2026-09-16 | `[UI]` `[Map]` | **지도 지점 팝업 첫 줄을 제목 행으로.** 첫 줄 = 「경로 생성」(출발 핀을 찍으면 「경로 생성 잔여 토큰 N개」), 주소는 둘째 줄로 내린다. 토큰 줄은 이제 제목이라 **잔액을 몰라도 숨기지 않는다**. 완성 문구는 종전과 동일 | ① 긴 주소가 첫 줄에서 닫기 ✕ 를 덮었다 ② 팝업에 제목 구실을 하는 행이 없었다 | `routeTokenUiCopy.ROUTE_TOKEN_TITLE` · `setRouteStartPinned` · 계약 `route-pick-popup-title.spec.ts`(✕ 겹침 면적 0 계측) |
| 2026-09-16 | `[UI]` | **센서 칩을 우상단에서 RouteDock 으로**(`UI-DECLUTTER-SENSOR-6A`). **첫 행 좌측**(헤더 행을 접히는 본문 밖으로 hoist) → 「경로 → 센서 → Go」가 한 줄로 읽히고, 주행 중 접어도 rpm 이 남으며, **dock 크기는 칩 유무와 무관**(세로 컬럼 시안은 폭 +53px 로 지도를 더 가려 폐기). 다만 **`idle` 에는 우상단 폴백을 남긴다** — dock 이 없는 화면에서 센서 시트(=「체험 속도로 준비」, Go 의 사전조건)에 닿을 길이 사라지기 때문 | 준비물이 화면 양끝에 갈라져 있었다. 우상 3개 → 2개(dock 이 있는 네 stage) | `lib/sensorChipSlot.ts` 단일 판정 · 계약 `sensor-chip-slot-contract.test.ts`·`sensor-chip-route-dock-6a.spec.ts` · [지시서](ops/route-relay/260905-화면정리-센서칩-RouteDock-6A-작업지시서.md) · [REPORT](ops/route-relay/REPORT-6A.md) |
| 2026-09-15 | `[UI]` | **계정 패널 정비(블록 12→7).** ① 플랜은 상태+행동 한 줄 ② **일일 주행을 기간 탭으로 흡수**(`오늘·주간·월간·연간`) → 통계 타일 **한 벌** ③ 거리 히어로 + 보조 3(시간·평속·칼로리) ④ 마지막 주행 카드가 최근 주행 목록 입구 ⑤ 터치 타깃 44px | 폰에서 스크롤·클릭 강요. 같은 사실을 세 번 말하고(Free) 같은 타일 세트를 두 벌 그렸다 | `RideStatsPeriod` 에 `day` 추가 · 계약 `account-panel-tidy.spec.ts`·`ride-stats-aggregate-contract` |
| 2026-09-15 | `[UI]` | **주행 중 RouteDock 은 「접는다」이지 「내용을 없앤다」가 아니다.** `autoCollapse`(접기)와 `ridingDiet`(편집 UI 제거)를 분리하고, 경유지·주소는 펼치면 보이되 삭제만 잠금 | Chief 의도는 지도 가림 면적 최소화. PR #17 이 목록까지 숨겨 펼친 패널이 빈 껍데기가 됐다 | `routeDockUiPolicy.ts` · 계약 `route-dock-riding-content.spec.ts` |
| 2026-09-15 | `[Infra]` | **Vite 의존성 스캔 대상을 `index.html` 하나로 고정** | `scripts/*/**.html` 하네스(three 를 importmap→CDN 으로 받음)까지 훑다 실패해 **pre-bundling 이 통째로 skip** 되고 있었다 | `apps/web/vite.config.ts` · 참고: `three` 선언 `^0.175.0` ↔ 실제 `0.134.0`(mapillary-js dedupe) 불일치는 미해결 |
| 2026-09-14 | `[Route]` | **퍼블릭 등록 게이트의 좌표수·bbox 하한을 연장 하한에 연동**(`MIN_COORDS=max(2, 연장하한/250)` · `MIN_BBOX=max(50, 연장하한×0.1)` — 운영 5,000m 에서 종전 20점·500m 정확히 복원). 파싱·좌표수·밀도가 공유하던 **단일 문구를 원인별로 분리**하고 실제 수치를 노출. 클라에도 같은 게이트를 선검사로 추가 | dev 완화로 연장 하한만 5,000→100m 로 낮춰 112m·4점 정상 경로가 3회 영구 거부. 상수로 못박힌 하한이 서로 어긋난 것이 원인 | `functions/src/publicRouteAutoReviewCore.ts` · 진단 `npm run admin:diag-public-route-request` |
| 2026-09-14 | `[Map]` `[Conquest]` `[UI]` | **줌아웃 LOD = 집계 광채(paint)로 Phase A 구현.** 누적 궤적 아래에 같은 색을 굵고 흐리게 한 겹(`RTW_TRACE_ACCUMULATED_HALO_PAINT`, z13에서 0). **타일 채우기 집계는 Phase B로 유지**. 「지역 확보」류 면(面) 레이어 신설은 **기각** | 원거리에서 하루치 주행이 몇 px라 성취가 얇아 보이던 문제는 인정 규칙이 아니라 가시성 문제. 면을 쓰면 「채워야 할 영역」=분모가 생겨 §3.4 탐험도·비율 금지와 충돌 | [Conquest §3.1](260703-Conquest-정복-레이어-설계.md) · `feat/conquest-zoomout-lod` |
| 2026-09-13 | `[Map]` `[UI]` | **`MAP-SELF-LOCATION-MARKER-1` 착수** — 화면 고정 크기 현재 위치 Marker. Giant 확대 금지. 구현=`260913-개발팀장`, 수퍼바이저=지시·감리만 | 재주행·소축척에서 내 위치 즉시 식별 | [작업지시서](ops/map-relay/260913-지도-현재위치표시-작업지시서.md) |
| 2026-09-13 | `[Product]` `[UI]` `[Docs]` | **4·5단계 폐기**(별도 태스크만). **3단계 잔여 + 통계(마지막·일일 주행)** (`RIDE-3R-STATS-LAST-DAILY`). 수퍼바이저=계획·지시·감리(코드는 Chief 명시 시에만), 개발팀장=구현, FAIL 최대 3회 | Chief 목표 수정·역할 고정 | [작업계획서](ops/ride-relay/260913-3단계잔여-통계-마지막일일-작업계획서.md) |
| 2026-09-13 | `[Infra]` `[Docs]` | **라이더 교체 단계 0 입력은 명시 인자로만 등록.** 외형 보존 경량화 후보 `20260913-162030-d32f2fc2` 단계 0 완료 · 사용자 승인 대기 | 폴더 자동 탐색은 작업 중 파일·구형 릴리스를 섞는 경로 | [단계 0 기록](archive/260913-Modelling-라이더-교체-단계0-외형보존후보.md) |
| 2026-09-13 | `[Product]` `[Map]` `[Docs]` | **재방문 실험(4)·이후 단계 무기한 보류.** 우선 **라이더 GLB 교체**. 충돌 점검 HARD 없음 · PR #18 미머지 닫음 | 제품 외형 교체를 도로망 성취 실험보다 우선 | [충돌 점검](archive/260913-재방문보류-라이더GLB교체-충돌점검.md) |
| 2026-09-13 | `[Product]` `[UI]` `[Infra]` | **3단계 Hosting 배포** · **4단계 `RIDE-RETURN-EXPERIMENT-4` 착수**. 제품 코드는 **`260910-개발팀장`만** 구현·PR, 수퍼바이저는 지시·즉시 감리(3회 한도) | 단계별 자동화·역할 분리 | [4단계 지시](ops/ride-relay/260913-도로망-재방문실험-4단계-작업지시서.md) · [착수 브리핑](ops/ride-relay/260913-개발팀장-착수브리핑-4단계.md) |
| 2026-09-13 | `[Infra]` `[Docs]` | **개발팀장 제출 즉시 검수** · 기준 미달 시 재지시 · **동일 작업 3회 FAIL 시 중단·Chief 보고**. `RIDE-IN-RIDE-HIERARCHY-3` PR #17 시도2 PASS · 병합은 Chief 승인 후 | 수용 증거 없이 진행하지 않음 | [재작업 시도1](ops/ride-relay/260913-도로망-주행중-3단계-재작업-시도1.md) · [PR #17](https://github.com/parkstery/boxcycle/pull/17) |
| 2026-09-13 | `[Product]` `[UI]` | **`RIDE-IN-RIDE-HIERARCHY-3` 착수 승인** — 개발팀장 구현·PR, 수퍼바이저=Cursor 검수 | 실행 계획 §3 단계 3 · 2단계 Hosting 종결 직후 | [3단계 지시서](ops/ride-relay/260913-도로망-주행중-3단계-작업지시서.md) |
| 2026-09-13 | `[Product]` `[UI]` | **`RIDE-NEXT-VISIT-2` 종결**(PR #16 병합·Hosting). 다음 착수 후보 `RIDE-IN-RIDE-HIERARCHY-3`(주행 중 위계) — Chief 착수 승인 후 | 2단계 수용·배포 완료, 실행 계획 §3 순서 | [완료 보고](archive/260913-도로망-다음접속-2단계-완료보고.md) · [3단계 지시서](ops/ride-relay/260913-도로망-주행중-3단계-작업지시서.md) |
| 2026-09-13 | `[Infra]` `[Docs]` | **수퍼바이저=Cursor 유지**(Codex 이관 철회). `RIDE-NEXT-VISIT-2` PR #16 검수 PASS · 병합·Hosting은 Chief 승인 후 | 역할 혼선 방지·검수 연속성 | [후속 작업계획](ops/ride-relay/260913-도로망-다음접속-2단계-검수PASS-후속-작업계획.md) |
| 2026-09-12 | `[Map]` `[Docs]` | 라이더 교체 입력을 최신 anatomy_reference 계열 상완 수정 결과로 갱신 준비. 이전 `roadcyclist_reference_revised` 기준선은 이력으로 보존하고 최종 입력 채택·승인 승계 금지. 제품 교체는 미실행 | Modelling에서 가슴·어깨·상완 외형을 후속 수정 중이며 확정 파일 인계가 필요 | [최신 인계 조건](260912-Modelling-최신라이더-인계조건.md) |
| 2026-09-11 | `[Docs]` `[Infra]` `[UI]` | **새 도로 HUD 라인 종결·git 정리**: PR #8 닫음(2단계 보류), 머지된 fix/* 원격 브랜치 삭제, 메인 워크트리 `origin/main2` sync. 이어서 **2단계 재착수** | Chief 「새 도로는 이 정도로 정리」+ 정리 후 다음 단계 | [실행 계획](260907-RTW-도로망-성취경험-실행계획.md) §3 |
| 2026-09-11 | `[Product]` `[Conquest]` | **「새 도로」= 나 기준 미보유 구간**(남이 달렸어도 내 conquest에 없으면 증가). 전 세계 최초는 Pioneer(별도·Phase B) | Chief 용어 확인 | [Ontology §2.4](260714-RTW-Ontology.md) |
| 2026-09-11 | `[UI]` `[Conquest]` | **라이브 HUD 「새 도로」는 세션 무장 후 0 포함 항상 표시** (+0.00=이미 내 도로). 결과 시트 0/50m 정책은 유지 | 미표시만으로는 고장과 소유 도로를 구별 못 함. cellIds 미도착은 ~2초 후 빈 스냅샷 | `useLiveConquestPaint` |
| 2026-09-11 | `[UI]` `[Conquest]` | **라이브 새 도로: 비주행 시 0 고정·소유 셀 스냅샷은 cellIds 도착 후·매 렌더 실측** — 시작 순간 잔여값 깜빡임 제거 | 이전 세션 meters가 남았다가 리셋되며 사라졌다가 50m처럼 다시 보임 | `useLiveConquestPaint` |
| 2026-09-11 | `[UI]` `[Conquest]` | **주행 중 HUD 「새 도로」는 1m부터 표시** (결과 시트 hero 50m과 분리). 0만 미표시 | HUD 50m 대기는 저속(~5km/h)에서 ~37초 지연으로 체감됨 | `LIVE_NEW_ROAD_HUD_MIN_METERS` |
| 2026-09-10 | `[UI]` `[Conquest]` | **라이브 「새 도로」는 셀×30m 근사가 아니라 경로 실측 미터(미보유 셀)** — HUD 거리와 첫 주행 시 정합 | 셀 근사로 80m 주행에 +0.15km 표시되던 불일치 | `useLiveConquestPaint` / `computeLiveNewRoadFromRoute` |
| 2026-09-10 | `[UI]` `[Conquest]` | **주행·새 도로 km 표기는 소수 2자리로 통일** (`formatRideDistanceKmNumber`). HUD 거리·새 도로·결과 hero·NextRideCard 동일 | 소수 1자리/2자리 혼용으로 같은 주행이 다른 거리처럼 보임 | `apps/web/src/lib/rideDistanceFormat.ts` |
| 2026-09-03 | `[Map]` `[UI]` `[Product]` | **라이더 자이언트 20배 최종 미채택 — 라이더는 현재 크기(계수 1) 유지.** 다만 **카메라 거리 범위·프레이밍을 라이더 전고에서 유도**하는 구조와 `RIDER_GIANT_SCALE_FACTOR` 계수는 남긴다 — 계수만 바꾸면 어떤 크기의 캐릭터도 카메라가 따라온다. 부산물로 follow look-at 무보호(거리 1m에서 라이더가 화면 밖)·`RIDER_HEIGHT_SPAN_MARGIN` 원근 미반영 결함을 고치고 순수 시험 70건을 세웠다 | 20배는 줌은 그대로였으나 라이더가 뷰포트의 1.66배를 차지해 2026-08-27 거절 사유의 「지도를 가린다」 절을 그대로 재현했다(G-3·G-4 실측). 카메라를 라이더에 맞추면 화면은 성립하나, 얻는 것이 「라이더가 큼」뿐이라 세 라운드의 비용에 값하지 않는다고 판단 | [G-3](ops/giant-relay/REPORT.md) · [G-4](ops/giant-relay/REPORT-G4.md) · [G-5](ops/giant-relay/REPORT-G5.md) |
| 2026-09-03 | `[Docs]` `[Map]` | **`gient` 오타를 `giant` 로 정정** — 문서 58곳·디렉터리 `document/ops/giant-relay/`·원격 브랜치 `260825-giant`·태그 `experiment/260825-giant` 전부 개명. 종결 보고의 「오타를 고치지 마라」 규칙은 폐기한다. 브랜치 안 commit message 의 `260825-gient` 문자열은 이력 재작성을 피해 그대로 둔다 | 사용자가 최초 오타를 자기 실수로 확인하고 정정을 지시(2026-09-03). 오타를 보존할 제품적 이유가 없다 | [G-3 작업지시서](ops/giant-relay/260903-giant-20배-제품채택-G3-작업지시서.md) |
| 2026-09-03 | `[Trail]` `[Product]` | **동행은 같은 Trail 만 인정** — 같은 경로라도 Trail 번호가 다르면 동행이 아니다. 따라서 동행 인원수의 단일 진실은 publication 범위 `routeActivity` aggregate 가 아니라 Trail 실시간 구독이다 | 4C 의 `max(aggregate, 실시간)` 은 증가만 즉시였고, 이탈 시 낡은 aggregate 가 「2명」과 「다른 라이더 없음」을 다시 충돌시켰다. aggregate 는 Trail 경계를 넘는다 | [4D](ops/route-relay/260903-동행-Trail단일진실-이탈반영-4D-작업지시서.md) |
| 2026-09-03 | `[UI]` `[Trail]` | ❌ **동행 HUD 인원수는 max(aggregate, 1+실시간 다른 라이더)** — 폴링 주기·캐시 TTL 은 60초 불변. 실시간 구독으로 체감 지연만 없앤다. aggregate 를 대체하지 않는다 → **[4D](ops/route-relay/260903-동행-Trail단일진실-이탈반영-4D-작업지시서.md) 로 뒤집힘** | 같은 경로 2인 주행에서 폴 위상 차로 두 화면 숫자가 갈라졌다. live ride 행은 이미 실시간인데 숫자만 60초 aggregate 를 썼다 | [4C](ops/route-relay/260903-동행-인원수-갱신지연-4C-작업지시서.md) |
| 2026-09-03 | `[UI]` `[Trail]` | **동행 HUD 빈 문장을 live ride 행으로 판정** — 「지금 N명 주행」과 「다른 라이더 없음」을 동시에 쓰지 않는다. Trail 이름 dedup 은 유지. `origin/fix/hud-companion-consistency`(`7cbc007`) 를 main2 에 회수 | 같은 Trail 동시 주행이면 동행자는 반드시 활성 접속자라, 07-12 이름 dedup 이 `coursePeerNames` 를 비워 빈 문장이 잘못 떴다 | [4B](ops/route-relay/260903-동행HUD-다른라이더없음-오판정-4B-작업지시서.md) · `7cbc007` |
| 2026-09-02 | `[Route]` `[Product]` | **거리·방향 자동 Route 실패 없는 도달 제안(3F-C-R1)** — 클릭 지점에서 끝나는 경로를 만들고, 불가능하면 실패 대신 방향 도로 위 목표거리 지점을 `offered` 로 제시한다. 직선 비율 도넛은 도로거리와 단조 관계조차 없어 폐기했다. | 사용자 6클릭 실측 성공 1/6 → 6/6 | [3F-C-R1](ops/route-relay/260902-거리방향-자동Route-실패없는-도달제안-3F-C-R1-작업지시서.md) |
| 2026-09-02 | `[Economy]` | **Route Token 온보딩** — Guest **10** · 로그인 **15** (기존 3). `config/routeTokenEconomy` 문서 우선. 기존 사용자 소급 없음. | 배포 전 config 확인 필요 | [3G](ops/route-relay/260902-거리방향-자동Route-3G-병합푸시-Token온보딩기본값-작업지시서.md) |
| 2026-08-29 | `[Route]` `[UI]` | **주행 결과 시트를 닫으면 지도를 idle 로 되돌린다** — 방금 달린 Route·핀·재개 후보를 워크스페이스에서 비우고, 그 자리에 「다음 주행」 카드를 띄운다. 같은 경로를 다시 타려면 카드의 「이어 달리기」가 다시 불러오고, 「처음부터」는 그 뒤 RouteDock 보조 선택으로 남는다. 아울러 종료 시 진행률을 **낙관 반영**해 Firestore 왕복 전에도 카드가 올바른 재개 지점을 보인다 | 실주행 수용 1차 실패의 원인 — 결과를 닫아도 방금 달린 Route 가 지도에 남아 stage 가 `ready-to-start` 로 머물렀고, RouteDock 이 Go 를 다시 띄우면서 카드는 「Route 없는 idle」 조건을 만족하지 못해 숨었다. reload 해야 카드가 보이는 상태는 「어제 멈춘 곳이 오늘 자동으로 다음 출발점이 된다」를 만족하지 못한다 | [작업지시서 §12.4](ops/ride-relay/260829-다음-주행-이어달리기-작업지시서.md) · 증거 `document/archive/ride-verify-evidence/c1-next-ride-card-no-reload.png` · e2e C1 |
| 2026-08-29 | `[Route]` `[UI]` | ❌ **HUD 거리 이중 표기 확정**(2026-09-17 뒤집힘 — 맨 위 「HUD 거리 셀을 「누적 / 전체」 한 줄로」 참조) — 주행 중 거리 셀에 **오늘 N km**(세션 실주행)와 **누적 M / 전체 km (P%)**(경로 위치·진행률)를 함께 표시한다. 운동·칼로리·Claim·Ride 기록은 기존처럼 세션 구간만 인정(`virtualDistance − offset`, `fromMeters`). `aria-label="오늘 거리"`·`"누적 진행"`으로 E2E 계약 고정 | RIDE-CONTINUE-1 구현 당시 HUD 는 「오늘 / 전체」만 보여 U4(위치는 누적)와 어긋났다 — 재개 주행에서 「얼마나 더 달렸나(오늘)」와 「경로 어디까지 왔나(누적)」를 한 화면에 분리해 보여 줘야 이어 달리기 판타지가 성립 | `MapHud.tsx` · `App.tsx` · `ride-continuation.spec.ts` · [작업지시서 U4](ops/ride-relay/260829-다음-주행-이어달리기-작업지시서.md) |
| 2026-08-29 | `[Route]` `[Infra]` | **RIDE-CONTINUE-1 구현 확정** — ① 진행률의 진실을 **서버 문서**로 옮겼다: `updateSavedRouteProgressInFirestore` 를 transaction 으로 바꿔 `max(server, requested)` 를 보장하고, 완주 문서는 되돌리지 않으며 stale write 는 `lastRideId` 도 덮지 않는다(규칙은 순수 모듈 `savedRouteProgressPolicy.ts` 하나로 로컬·중복 저장과 공유) ② 다음 주행 후보는 pointer 문서 없이 최근 Ride + SavedRoute 파생, 재개점의 진실은 **SavedRoute 의 `lastProgressRatio`**(최근 Ride 종료 좌표가 아니다) ③ 카드는 SavedRoute 첫 로드가 끝나기 전에는 뜨지 않는다 — 빈 목록으로 재개 가능한 Route 를 놓치고 「새 경로」로 잘못 제시하는 창을 막는다 ④ ~~HUD 거리 표기는 이번 범위에서 바꾸지 않았다~~ → **후속 결정으로 U4 HUD 이중 표기 반영**(위 항목) | 클라이언트가 계산한 `max` 를 그대로 덮어쓰면 다른 탭·기기의 늦은 낮은 진행률이 높은 진행률을 되돌린다 — 「어제 멈춘 곳」이 조용히 뒤로 밀리는 건 이 기능의 전제를 깨는 실패다. 후보를 파생으로 두면 Route 삭제·완주 시 카드가 자동 무효화되어 stale 상태를 시작할 수 없다 | [RIDE-CONTINUE-1 작업지시서](ops/ride-relay/260829-다음-주행-이어달리기-작업지시서.md) · `lib/savedRouteProgressPolicy.ts` · `lib/nextRideTarget.ts` · `lib/rideSessionAnchors.ts` |
| 2026-08-29 | `[Product]` `[Route]` `[UI]` | **최근 Ride를 기록에서 다음 행동으로 연결** — 모든 유효 Ride 종료 후 결과 시트를 열고 실제 세션 종료 좌표를 사용자 전용 Ride에 저장한다. idle 지도는 최근 Ride+SavedRoute에서 파생한 **「다음 주행」** 카드를 먼저 보여 준다. 미완주 SavedRoute는 기존 진행점에서 재개하고, 완주·ad-hoc·Publication Ride는 실제 종료점에서 새 Route를 만든다. 카드 CTA는 Route 준비까지만 수행하며 실제 시작은 기존 Go·입력 준비 게이트를 유지한다. SavedRoute geometry는 mutate하지 않고 `Journey` 용어·새 모델도 이번에 확정하지 않는다. | 현행 이어 달리기는 계산상 구현됐지만 `MENU→내 경로→선택→열기` 뒤에 숨고 재개 후보도 주행 시작 때 소멸한다. 최근 주행은 읽기 전용이며 실제 종료 좌표도 없어, 어제의 끝이 오늘의 시작으로 자동 이어지지 않는다. 별도 주행일지 없이 지도가 다음 행동을 기억해야 개인 도로망 축적 루프가 성립한다. | [RIDE-CONTINUE-1 작업지시서](ops/ride-relay/260829-다음-주행-이어달리기-작업지시서.md) · [Conquest §9.5](260703-Conquest-정복-레이어-설계.md) |
| 2026-08-27 | `[Map]` `[UI]` `[Product]` | **폰 실주행 1차 UX 확정** — ① 기본 맵 스타일 **RTW Dark → Outdoors**(RTW Dark 는 목록 유지) ② 주행 카메라 프리셋 **「상공」(topDown, pitch 0·진행방향 위) 신설**하고 **주행 시작 기본 카메라**로(이전 leftFlat), 시작 줌 16→17.5 ③ 지도 궤적 3색 확정 — **빨강 `#ef4444` 아직 안 간 길(루트) / **BlueViolet `#8A2BE2`** 내가 달린 길(이번 주행 궤적 + 내 도로망 공통)**. 「이번에 달린」과 「예전에 달린」을 색으로 가르지 않는다 — 사용자에게 중요한 구분은 「달렸나 / 아직 안 갔나」 하나뿐이고, 이번 주행분은 glow·굵기로만 구별한다. 색은 Outdoors 가 실제 쓰는 색상환(35·50~60·98·194·205·224°)과 앱 오브젝트(0·142·187°)를 뽑아 **비어 있는 260~340° 대역**에서 골랐다 — 골드는 도로(주황)에, 청록은 강·습지(194~205°)에 섞였다. 그 대역 안에서 마젠타(330°)가 아니라 **BlueViolet(271°)** 을 쓰는 이유는 마젠타가 빨강(0°)과 30° 이웃이라 재주행 구간에서 한 덩어리로 읽혔기 때문이다 — 90° 떨어뜨렸다. 명도 53% 로 Outdoors·RTW Dark 양쪽에서 보인다(Indigo `#4B0082` 는 명도 25% 라 밝은 지도에서 검게 읽혀 기각). 아울러 **시각 위계를 내 궤적 > 경로 > 비동기 흔적** 으로 세웠다 — 흔적(pulse 0.92→0.45 · heat 0.78→0.28)은 red 계열을 유지하되 강도를 낮춘다. 재주행 시 궤적이 안 보이던 진짜 원인은 색이 아니라 **레이어 순서**였다 — 누적 궤적이 `beforeId:"route"` 로 경로선 **아래**에 깔려 강한 빨강에 덮였다. 순서를 `route < 누적 < live` 로 바로잡아 확정(`MapView.orderConquestLayersAboveRoute`). 이제 빨강은 「아직 안 갔고 내 것도 아닌 구간」에만 남는다. 골드(`#E8A33D`)는 **HUD 강조색으로만** 남고 지도 궤적에서는 뺀다 — Outdoors 도로가 주황·황토라 궤적이 도로망에 섞였다 ④ **날씨·밤낮 표시 제거**(HUD 한 줄 + 화면 오버레이 + Open-Meteo 30분 폴링). 실내 가상 주행에서 판단에 쓰이지 않고 밤 틴트가 지도 판독을 해쳤다 ⑤ 맵 뷰 트리거를 하단 중앙 → 우상단 계정 칩 아래, 맵 뷰 시트는 폰 가로에서 우측 좁은 패널 ⑥ Mapillary 거리뷰 **기본 off**(맵 뷰 시트 토글로 on, 기능·조회 로직은 유지) ⑦ 접속·동행 패널과 Trail 번호 배지는 폰 가로에서 숨김(MENU 와 중복) | 개발자가 처음으로 실물 센서를 달고 폰으로 직접 주행하며 확인한 결과다. 데스크톱 기준으로 만든 HUD 가 CSS 높이 275px 화면에서 지도의 41.8% 를 덮고 있었고, 궤적 색은 베이스맵과 섞여 「내가 달린 길」이 보이지 않았다 — 핵심 판타지가 화면에서 전달되지 않는 상태였다 | 실측(690×275): 지도 잠식 41.8%→23.4% · 주요 조작 겹침 0px² · 커밋 `ac73e70`·`93c0a2a`·`bc7749f`·`45ce499` |
| 2026-08-27 | `[Product]` `[Sensor]` `[Infra]` | **한 번 허용한 케이던스 센서는 유휴 단절·페이지 복귀 뒤 chooser 없이 자동 재연결을 시도** — 같은 문서에서는 선택한 `BluetoothDevice`를 보존하고, 새 문서에서는 `navigator.bluetooth.getDevices()`로 이 origin에 허용된 단일 센서를 복원한다. 단절 뒤 `watchAdvertisements()` 광고 수신 시 즉시, 그 외에는 0.5→1→2→4→8→10초 상한 백오프로 GATT·CSC notification을 다시 구성한다. 명시적 연결 해제와 백그라운드 탭에서는 자동 루프를 멈춘다. 복수 후보는 임의 선택하지 않는다. **코드·계약 시험 반영 완료 · CYCPLUS C3 유휴→페달 wake 물리 검증 대기** | 매번 센서 선택기를 다시 여는 절차는 주행 재개의 맥을 끊는다. 다만 웹은 최초 권한·열린 Chromium 탭이라는 경계 안에서만 네이티브 앱에 가까운 복귀를 제공할 수 있다. | [`bleAutoReconnect.ts`](../apps/web/src/lib/bleAutoReconnect.ts) · [`useBleCrankRpm.ts`](../apps/web/src/hooks/useBleCrankRpm.ts) |
| 2026-08-27 | `[Product]` `[Sensor]` `[UI]` | **센서의 주 표면을 RouteDock에서 HUD 우상단 계정 왼쪽의 전역 상태 칩으로 이관하고, 주행 입력 준비를 Go 이전에 완료** — 주행 전에는 흰/초록 LED로 미연결/연결, 주행 중에는 RPM을 표시한다. 칩 전체는 전용 센서 상세 설정 버튼이며, 전체 장치명·연결/해제·입력 모드·체험 속도는 상세에만 둔다. **센서 연결+유효 RPM 확인 또는 명시적 체험 입력 선택 전에는 Go 불가**다. RouteDock은 준비가 끝난 뒤 주행을 실행하는 표면이며 센서/체험 입력 설정을 두지 않는다. 주행 중 상세 설정은 cadence 단절 재연결·안전한 manual 전환만 담당한다. **코드 반영 완료(2026-08-27) · 실물 H1~H14 검증 대기** — 자동 게이트(sensor 시험 46 · build · ride selector 8단계 · ride-entry e2e 6)는 통과. | 센서는 계정 속성이 아니라 주행 시작 전 완료해야 할 핵심 입력 준비다. Go를 누른 뒤 다시 설정시키면 준비→주행이라는 익숙한 순서를 깨고 몰입을 끊는다. | [SENSOR-2 작업지시서](ops/sensor-relay/260827-BLE-케이던스-HUD-상태칩-작업지시서.md) |
| 2026-08-27 | `[Product]` `[Sensor]` | **케이던스 센서를 가상 주행 속도의 직접 입력으로 채택** — 주행 입력 모드를 `manual`(체험·T0 슬라이더)/`cadence`(BLE CSC 크랭크 RPM·T1)로 명시 분리하고, cadence 모드의 목표 속도는 `min(30, rpm × 0.32)`(deadzone 8rpm). **센서 stale·단절은 manual fallback 신호가 아니다** — 목표 속도 0으로 감속 정지하며, manual 복귀는 사용자의 명시적 선택으로만. BLE 연결은 주행 세션이 아니라 앱 생명주기에 묶어 주행 전 확인·주행 후 재사용이 가능하다. `0.32`·`30km/h`는 파워·저항·기어 정보가 없는 초기 고정 가상 기어비이며 **현실 속도·경쟁 기록이 아니다**(30은 현행 T1 인정 상한과 정렬). **코드 반영 완료 · CYCPLUS C3 물리 검증 미완** | "BLE 연결됨"은 사용자가 원한 결과가 아니다 — 페달링이 주행을 지배해야 하고, 단절 시 슬라이더 속도로 몰래 계속 달리는 것이 가장 위험한 실패다 | [작업지시서](ops/sensor-relay/260827-BLE-케이던스-직결-작업지시서.md) · `lib/cadenceRideInput.ts` · `lib/bleCscCadence.ts` · [Conquest OQ-9](260703-Conquest-정복-레이어-설계.md) |
| 2026-08-27 | `[Map]` `[UI]` `[Product]` | **라이더 자이언트 스케일(G-1 20배·G-2 400배) 미채택** — `main2` 병합 금지. 실험은 브랜치 `260825-giant` @ `2045e79` 와 태그 `experiment/260825-giant` 에만 보존. 제품 `RIDER_GLB_MODEL_SCALE` 은 실험 전 기준(1.15) 유지 | 400배에서 전고 ~638 m 가 follow 카메라를 밀어 줌이 빠지고 라이더가 지도를 가린다. 엔진 배율은 G0 로 맞았으나 제품 기본값으로는 부적합 | [종결 보고](archive/260827-라이더-자이언트-스케일-실험-종결.md) · 브랜치 `document/ops/giant-relay/REPORT.md` |
| 2026-08-25 | `[Trail]` `[Map]` `[Infra]` | **동행 라이더 앞뒤 튐(S4-4) 을 미해결·미확정 상태로 일단락** — S4-14 가 문제 구간을 `displayDistM` 이후~최종 DOM 으로 좁혔고, S4-15 는 그 안에서 **`projected` 단계를 최초 관측 지점으로 기록**했다. **「카메라·투영 확정」이 아니다** — 확정된 것은 「**11.45 Hz 로 기록된 S4-15 trace 한 건에서 왕복이 처음 나타난 단계가 `projected` 였다**」뿐이고, 원인 파라미터(bearing·center·pitch·zoom)와 60 fps 재검증은 **전부 미확정**이다. Chief 조건(좌측 카메라·5 km/h·근접·양쪽 visible)은 창 전체에서 처음 성립(간격 2.03~2.34 m, 93 프레임). 수치: `lngLat` 반전 0 · `projected.x` 반전 26·최대 7.88 px · DOM 상대 x 7.98 px(증폭 0.09 px). **self 반전 0 은 카메라 무죄가 아니다**(self 는 카메라 중심에 있어 변동이 상쇄된다). bearing 자릿수 검산 182 px × 0.0249 rad ≈ 4.5 px vs 관측 7.88 px — 동일 자릿수이나 미확정. **탈락·미채택**: S4-13 A/B 적응 발행 E + 흡수 τ(실주행에서 OFF 와 구분 불가) · S4-5 시간축 변경(포함된 OFF 에서도 증상) · N×M 증폭 · merge 확장 전제. **병합**: main2 제품 코드는 S4-1~S4-3 까지(`d879588`). **S4-4~S4-15 는 main2 병합 금지** — 계측기 전용이 아니라 **미채택 제품 실험(S4-5 상시 동작 시간축 변경 · S4-13 A/B DEV 전용)이 섞여 있어** 그대로 병합하면 미채택 변경이 제품에 들어간다(`fix/multiplayer-read-amplification@0f5d35a`). **다음(미확정 계획)**: 카메라 4파라미터 상관 분기 + 60 fps 단일 브라우저 재검증. 파라미터 확정 전 보간 상수·마커·카메라 코드 수정 금지 | 15 라운드의 배제 결과를 남기되 확정 범위를 trace 한 건으로 못 박아야, 다음 착수가 같은 배제를 되풀이하지도 않고 미확정을 확정으로 물려받지도 않는다 | [현황 보고서](archive/260825-동행-라이더-튐-S4-4-현황-보고서.md) · [REPORT.md](ops/sync-relay/REPORT.md) · [INSTRUCTION S4-15](ops/sync-relay/INSTRUCTION-S415.md) |
| 2026-08-20 | `[Infra]` `[Trail]` | **Sync 문제 정의를 3분할로 확정** — ① 큰 위치 오차는 Sync 1 단계에서 **해결로 채택**(작은 잔차 인정, 재개발 없음) ② **Firebase 비용이 현재 최우선** — S4-1 쓰기 폭주 감소와 S4-2 중복 구독 2→1 은 유효하나 **전체 비용 종결이 아니다**. S4-3 에서 공유 Trail 문서의 writer×subscriber 증폭을 실측하고 기능 부작용이 없을 때만 추가 감축, 감소 여지가 없으면 **「추가 감축 불채택」도 정상 결론** ③ **상대 라이더 앞뒤 튐을 신규 미해결 항목으로 공식 등록** — 큰 위치 오차의 재발이 아니라 화면 안정성 후속 결함. S4-3 과 섞지 않고 종결 뒤 독립 지시. 착수는 **재현 우선**(원본 패킷·병합 출력·integrator ingest·displayDistM·화면 좌표 동시 기록 → replay 시나리오 고정), 하네스가 이중 스트림 merge·reconcile 을 재생 못 하면 범위 확장이 먼저이며 상수 감 조절 금지. 판정: 원본 단조 전진 시 displayDistM 역행 ≤ 0.5 m · 이유 없는 순간이동 없음 · 화면 좌표 튐과 실제 거리 튐 구분 · 1 단계 위치 정확도와 S4 비용 성과 무훼손. 순서 **A** S4-3 → **B** 튐 → **C** 인원 집계 과다·F-1 재판단 | 하나의 "싱크 문제"로 뭉뚱그리면 해결된 항목이 재개발되고 비용 작업이 화면 결함에 밀린다. 세 항목을 분리해야 각각의 종결 조건과 순서를 판정할 수 있다 | [INSTRUCTION S4-3 §11](ops/sync-relay/INSTRUCTION.md) · [REPORT.md](ops/sync-relay/REPORT.md) |
| 2026-08-16 | `[Route]` `[Infra]` | **입문 실도로 3경로 프로덕션 반영 + 폐기 대상을 3건→5건으로 확대** — Chief 승인 후 `admin:seed-basic-intro-publications --archive-legacy` 1회 실행(created 3 · archived 5 · errors 0). dry-run 에서 프로덕션 published 입문 계열이 3건이 아니라 **5건**임이 드러나(고아 seed `basic-intro-nyc-0_5km`·`basic-intro-rome-0_5km` 가 `walking`·524/543m 로 published 상태) 폐기 목록을 넓혔다. **삭제 없음** — `status=archived`+`presenceEnabled=false` 만, geometry 보존. 부수 문서(`publicationPresence`·`routeActivity`·세션 멤버)는 미접촉 | 옛 seed 2건을 두면 제목이 "입문 · …" 이라 퍼블릭 카탈로그에 옛 입문 경로로 계속 노출된다. 옛 3건은 nominal 거리조차 실측과 어긋나 있었다(500↔505.8 · 1000↔786.1 · 1500↔1029.6m) | [보고서 §7](archive/260816-입문-실도로-경로-교체-보고서.md) · 실 Firebase 앱 검증 캡처 `prod-intro-list.png` |
| 2026-08-16 | `[Route]` `[Map]` | **입문 Basic 1/2/3 을 실도로 경로로 전면 교체 + publication ID 신규 발급** — 허구 직선 4점 geometry(건물·바다 관통)를 Mapbox Directions `cycling`/`overview=full` 실도로 seed 3건으로 교체: `basic-intro-seoul-namsan`(남산공원길 413m) · `basic-intro-paris-pont-neuf`(퐁뇌프 448m) · `basic-intro-nyc-central-park`(West Drive 453m). 옛 ID(`basic-mountain-0_5km`·`basic-coastal-1_0km`·`basic-mountain-1_5km`)는 **유지하지 않고 폐기** — 이름에 박힌 "해안/산악·1.0km·1.5km"가 내용과 영구히 어긋나고, 새 ID 는 거리를 담지 않는다. 옛 문서는 **삭제하지 않고** `--archive-legacy` 시에만 archived. Functions allowlist 하드코딩(교집합 0 이었음)을 생성물 `basicIntroHubSeeds.ts` 로 대체해 web·functions·presence·overlay ID 집합 일치. 프로덕션 rules 상 클라이언트가 `routePublications` 를 create/수정할 수 없으므로 Admin CLI `admin:seed-basic-intro-publications` 신설(멱등·삭제 없음). production 실행은 미승인 — dry-run 까지만 | 스크린샷상 세 경로 모두 실제 도로 geometry 가 아니었고, 500m 이하 조건·cycling 조건·ID 정합성이 동시에 깨져 있었다. 500m 이하는 nominal 이 아니라 좌표 재계산으로 증명 | [교체 보고서](archive/260816-입문-실도로-경로-교체-보고서.md) · [증거](archive/260816-입문-실도로-경로-증거/evidence.json) · `verify-basic-routes.mjs` |
| 2026-08-16 | `[Map]` `[UI]` | **주행 카메라 기본 거리 4 m → 40 m** · U-1R/U-6 은 Chief 실사용 판정으로 추가 수정 없이 종결 | 실사용 기본 구도를 멀리 두고, 근접(1~4 m)은 슬라이더·`?rideCam=` 오버라이드로만 남긴다. 머리 잘림·구도 미세조정은 문제 제기 취소 | [INSTRUCTION U-11](ops/sync-relay/INSTRUCTION.md) · [HANDOFF §7-3](ops/sync-relay/HANDOFF.md) |
| 2026-08-14 | `[Infra]` `[Trail]` | **멀티라이더 발행 수명주기 WARNING 채택(1 단계 종결)** — route(S4-1R2) + motion(S4-M1R) 양쪽 종결. PASS 아님. 부채 W-2'(route 시각 미기록) · W-3(cruise 꼬리) · W-4(M4 대조 부재). 위치 동기화 전체 종결 아님(S4-2·S4-3·F-1) | 늦은 쓰기가 지운 행·노드를 되살리지 않게 하는 발행 수명주기만 닫음. 읽기 증폭·heartbeat·목록 시계는 남음 | [REPORT.md](ops/sync-relay/REPORT.md) · [HANDOFF.md](ops/sync-relay/HANDOFF.md) |
| 2026-08-12 | `[Infra]` `[Docs]` | **오케스트레이터 후속 개발 책임을 Claude로 일원화** — Codex는 오케스트레이션을 포함해 코드를 직접 작성·수정하지 않고 Chief의 자문·독립 검토에 한정. Cursor 보고완료 → Claude 자동 감리 1단계는 미완성 인수인계 대상으로 전환 | 단위 로직·Claude 연결은 정상이나 foreground watcher 수명주기가 workflow에 연결되지 않아 새 S3A-V REPORT가 자동 감리되지 않음. 역할 중복과 오동작 반복을 막고 Claude가 계획·지시·감리를 이어받기 위함 | [Claude 인수인계](260812-AI-오케스트레이션-Claude-인수인계.md) |
| 2026-07-29 | `[Infra]` `[Docs]` | **신규 스킬 `rider-cycle-fit` 신설(최소 범위)** — 절차 생성 라이더용 rider-preview와 별개로, 별도 제작된 rider GLB + 독립 cycle GLB를 결합·피팅하는 작업의 3계층 스킬 신설. 현재 범위: 단계 0(register-inputs — 입력 6종 해시·경로 manifest 등록)·verify-fit(안장 혼합좌표·페달 위상 대칭·ETT≠reach 정적 불변식). anti-pattern 10건(위상 90°어긋남·메시 좌표검색 회전·scale/다리 혼용·혼합좌표·후드 근사·ETT=reach·seatTop 도식/표 불일치·발목클릿 선언≠적용·동일라이더인데 몸통각 변화·HTML만 렌더없이 승인)을 SKILL에 고정. 단계 B~E 렌더·promote·extract-glb-meta는 미구현 | "3번 규칙" 신호(한 세션에 안장 좌표·후드·다리길이를 3번 이상 재계산) — 반복 원인은 결합 작업을 재현·검증할 하네스 부재. 동료 감사가 rider-preview를 결합 작업에 과잉 적용했음을 자기정정, "입력을 고정 등록하지 않은 것"이 진짜 문제로 재규정 | [rider-cycle-fit SKILL](../.claude/skills/rider-cycle-fit/SKILL.md) · [HARNESS](../apps/web/scripts/rider-cycle-fit/HARNESS.md) · [아키텍처 §5](260722-Skill-Harness-아키텍처.md) |
| 2026-07-22 | `[Infra]` `[Docs]` | **Skill/Harness 3계층 아키텍처 확정** — Skill(`.claude/skills/<name>/SKILL.md`=왜·PASS·anti-pattern) / HARNESS.md(=어떻게) / 실행코드(`apps/web/scripts/<name>/`)로 분리. 하네스는 three·playwright·vite·src·public 의존으로 apps/web에 물리 고정(루트 `harness/` 반려). 신규 스킬은 이 2문서 템플릿을 따름. "3번 규칙"(한 세션에 3번 이상 고치면 그 영역 Harness 부재)을 프로젝트 철학으로. `.claude/`는 gitignore되나 agents/·skills/·settings.json은 화이트리스트 추적 | 스킬을 더 만들기 전에 경계를 확정해 rider-preview처럼 "규율·사용법 섞였다 분리"를 반복하지 않기 위함. 사용자가 Skill(읽기)/Harness(실행)를 헷갈리지 않게 | [Skill/Harness 아키텍처 SoT](260722-Skill-Harness-아키텍처.md) · [rider-preview](../.claude/skills/rider-preview/SKILL.md) |
| 2026-07-19 | `[Map]` `[UI]` | **라이더 GLB 형태 전면 재작성 + IK 버그 수정** — 절차 생성 메시를 테이퍼 튜브·lathe 회전체·근육 blob로 재구성(통짜 원기둥→사람 실루엣), 자전거를 실제 로드바이크 다이아몬드 프레임+드롭바로, 헬멧을 낮은 에어로형+선글라스로. 페달링 IK의 다 펴진 다리 분기(`cosHip=-1`→`1`) 버그 수정 — "정강이가 허리 위로 솟구침"(모델 초기부터 있던 문제) 해소 | 기존 라이더가 "초등 2학년 수준"·프레임 비현실·헬멧 풍선형·페달링 괴상 — 메인 캐릭터 기본 퀄리티 확보가 후속 작업 전제. IK 좌표 불변식·노드 6종·전고는 보존 | [인수인계 §4.1·4.1b](260719-라이더-GLB-작업-인수인계.md) · `generate-rider-prototype-glb.mjs` · `riderGlbPedalPose.ts` |
| 2026-07-18 | `[Route]` `[UI]` | **관리자 공개 경로 심사 UI 제거 + 안전망 재설계** — MENU의 '관리자 공개 경로 심사' 진입·심사 탭·`AdminPublicRouteQueue`(+CSS) 삭제. CF 자동 심사 정상 시 pending은 수 초 내 approved/rejected로 확정돼 큐가 항상 비므로 UI가 무의미. CF 연결·서버 오류로 pending이 남던 경우는 신청을 즉시 회수(withdraw)하고 "잠시 후 다시 시도" 안내로 폴백 — 관리자 심사 폴백 약속 폐기. 심사 백엔드(`loadPending·approve·reject·isRouteReviewer·config/routeReviewers`)는 콘솔 전용 안전망으로 코드만 유지 | 자동 등록으로 전환해 수동 심사 대상이 발생하지 않음 — 화면상 심사 큐는 상시 공집합. 실패 폴백을 '관리자 심사'가 아닌 '재시도'로 바꿔 pending이 갇히지 않게 함 | [정책 §3](260717-퍼블릭-경로-자동등록-정책.md) · `RideRoutePanel.tsx` · `publicRouteRequests.ts` |
| 2026-07-18 | `[Map]` `[UI]` | **밀착 카메라 pitch 준수평 80° 고정(3D 무관)** — 전/후/좌/우 프리셋 pitch를 enable3D 종속(3D 72/78·2D 30)에서 `RIDE_CAMERA_PITCH_CLOSE=80` 단일값으로. 2D에서 terrain 없이 지도가 눕는 건 라이더 밀착 체험을 위해 의도된 것 | 30° 탑다운은 자전거를 위에서 내려본 그림 — 사용자가 원한 건 옆·뒤 수평 추적(GoPro). 라이더 체험이 지도 왜곡보다 우선. (2026-07-18 "pitch 3D 재종속" 결정을 뒤집음) | `mapGlobeView.ts` `RIDE_CAMERA_PITCH_CLOSE` |
| 2026-07-18 | `[Map]` `[UI]` | **주행 카메라 라이더 밀착화 — 프리셋 거리 통일** — 후방/전방/좌/우 4방향 모두 라이더 반대편으로 카메라를 밀어(offsetBearing) 고정 거리·pitch에서 라이더가 화면에 잡히게. 거리를 미터로 단일 관리(zoomForRiderDistanceMeters 근사) — rear 전용 offset 16~58m·zoom 특례 폐기. north/free는 기존 유지. 주행 중 줌 슬라이더→거리 슬라이더(개발용, 확정 후 제거) | 기존은 멀찍이 관망(RTS/드론)뿐, 밀착 측면·후방 뷰 부재 — RTW는 드론이 아니라 라이더 체험이 핵심. 자유도보다 클릭 한 번의 일관 시점 | `mapGlobeView.ts` · MapView `getCameraForFollowMode` |
| 2026-07-17 | `[Map]` `[UI]` | **교통 정체·사고 레이어 상시 숨김** — RTW Dark(navigation-night 베이스)의 mapbox-traffic·mapbox-incidents 소스 전체 비표시. POI 비교 토글은 poi-label의 운전자용 클래스 필터 해제로 실효화 | RTW는 내비 기능 미사용 — 가상 주행에 실시간 교통은 무의미한 노이즈 | `rtwMapConfig.ts` applyRtwLayerStyle |
| 2026-07-17 | `[Map]` `[UI]` | **RTW Dark 베이스 dark-v11 → navigation-night-v1** — 도로 유령화·건물 숨김은 오버뷰(비주행) 한정으로 전환, 주행 중엔 원본 대비 복원. POI 라벨은 임시 토글로 비교 후 결정 예정 | dark-v11+유령화가 주행 화면에도 적용돼 도로·주변 식별 불가 — 내비 전용 고대비 다크가 라이딩 판독성 우위 | `rtwMapConfig.ts` · MapView `rideActive` |
| 2026-07-17 | `[Map]` `[UI]` | **맵 스타일 프리셋 5 → 3** — Streets·Light 제거, RTW Dark / Outdoors / Satellite만 유지 | Streets는 Outdoors와 중복이며 사이클 앱 적합도는 Outdoors가 우위, Light는 지나치게 단조로워 라이딩 배경으로 부적합 | `appSessionKeys.ts` MAP_STYLE_OPTIONS |
| 2026-07-17 | `[Route]` `[Infra]` | **퍼블릭 등록 자동화 — 수동 검수·완주 조건 폐기** — 조건(거리 5~120km·금칙어·개인정보·중복·유사도 90%·bbox 500m 등) 통과 시 CF `autoReviewPublicRouteRequest`(신뢰 경계=서버)가 즉시 publication 생성, 탈락은 rejected+사유 기록. 선점 방지는 완주 대신 **미완주 출판 상한(free 3/paid 5, ride-to-unlock)**. 수동 큐·리뷰어는 안전망으로 유지. 노출 품질은 후속 커뮤니티 평가로 | 관리자 일일 검수는 무의미(운영 부담·확장성 0). 완주 조건의 원목적은 선점 방지였고 직접 장치로 대체 — 지도만 보고 설계한 좋은 코스도 가치 있음. "등록은 자동, 노출은 사용자 평가" | [정책 SoT](260717-퍼블릭-경로-자동등록-정책.md) |
| 2026-07-17 | `[Route]` `[Infra]` | **경로 생성 거리 상한 120km** — 실제 Mapbox 경로 거리 기준. 클라 사전 차단(직선거리 합 초과 시 호출·토큰 소진 전 중단) + 서버 사후 강제(응답 거리 초과 시 토큰 환불 후 `invalid-argument`). 포인트 수 상한은 이번 범위 제외 | 거리 무제한 시 Routing/Elevation/DB/렌더 비용이 선형 증가, 300km+ 입력은 대부분 테스트·실수·장난. 짧은 Trail 여러 개를 잇는 RTW 게임성과도 일치 | [routeLimits.ts] · [functions/src/index.ts] `getMapboxDirections` |
| 2026-07-17 | `[UI]` `[Ontology]` | **주행 중 「진행 / 주행경로 전체거리」 병기** — HUD 거리 셀을 주행 중 `달린 거리 / 전체거리`로 표시(경로 확정 시에만, ad-hoc은 단일값). 전체거리 데이터는 원래 있었으나 주행 시작 시 화면에서 사라졌음 | 목표 대비 진행을 한눈에 — 얼마 남았는지 알 수 없던 문제. 히어로는 달린 거리(운동=본체) 유지, 전체는 흐리게 종속 | [Ontology §2.3](260714-RTW-Ontology.md) · MapHud/App 거리 셀 |
| 2026-07-16 | `[Product]` `[UI]` | **표면 이중 축 — 운동=본체, 정복=보너스** — 운동 축(오늘·누적 거리=마일리지)은 항상 표시·반복 주행도 전액 인정, 정복 축(새 도로 🏴)은 발생 시에만 표시(**0km 미표시** — 반복 주행에 벌점 없음). 챌린지 층의 탐험 중심(07-07)과는 층 분리로 양립 | RTW는 운동 앱에 게임 요소를 얹은 것이지 그 역이 아님 — 같은 10km 반복도 "오늘의 운동"으로 온전히 축하받아야 리텐션이 성립 | [Ontology §2.5](260714-RTW-Ontology.md)(마일리지) · 상태보드 §3.5 |
| 2026-07-14 | `[Ontology]` `[Docs]` | **RTW Ontology SoT 신설** — 용어 정의·관계·금지어의 단일 진실을 [Ontology](260714-RTW-Ontology.md)로 분리, [260517](260517-제품-용어-Trailhead-Trail.md)은 Trail 도메인 상세로 축소, 용어 변경 절차([지침 §6.1](260509-BOXCYCLE-문서-생성-및-수정-지침.md))·루트 CLAUDE.md 신설, 본 로그에 태그 도입 | 정의가 여러 SoT에 흩어져 사람·AI 모두 참조점 불명. 자문의 폴더 재편(01_~05_)은 링크 파손 비용으로 기각 — README 라우팅 표로 대체 | [Ontology](260714-RTW-Ontology.md) · [README](README.md) |
| 2026-07-13 | `[Product]` `[Trail]` | **"홀로 vs 함께" 역할 분담** — 획득=Ride=Claim(싱글플레이 코어), 유지=함께(Trail)·**비동기 우선**(Pioneer·흔적·통산 카운트). 실시간 동행은 유동성 확보 후 확장 | 초기 유동성 0에서 실시간 동행을 코어에 걸면 "빈 선술집" + peer 싱크 🔶 미해결. 비동기 사회성은 1명부터 작동 | [자문 요약](archive/260712-자문-문답-홀로-함께-역할분담-이어달리기.md) · 상태보드 §3.3 |
| 2026-07-07 | `[Conquest]` `[Route]` | **주행 인정 분리** — 운동·Claim·Pioneer 즉시 인정 유지, Route 완주만 ≥98% 게이트. 미완료 쿼터 무료5/유료10, TTL 90일, 최소 인정 거리 임계 폐기 | "달린 건 모두 축적" 철학 유지 — 악용(미완료 양산)은 개수 쿼터로만 차단 | [Conquest §9.5](260703-Conquest-정복-레이어-설계.md) · [자문 문답](archive/260707-자문-문답-주행거리-인정-진행률-미완료쿼터.md) |
| 2026-07-07 | `[Economy]` | **회원 체계 2단계(Free/Supporter)** — 프리미엄 3단계 안 함 | 3단계는 경계가 애매해지고 전환율 저하. 코어 루프는 전부 무료, 유료=편의·표현·분석 | [자문 문답](archive/260707-자문-문답-지식구조-랭킹-챌린지-회원구분.md) |
| 2026-07-07 | `[Map]` `[Economy]` | **구글 스트리트뷰 프리미엄 도입 안 함** | pre-version이 거리뷰 비용으로 사망한 전례 + Mapillary 무료 내장과 중복 + 지속 운영비 | 자문 문답 · [마스터 비전](260511-RTW-마스터-비전-및-종합계획.md) |
| 2026-07-07 | `[Product]` | **챌린지는 속도 경쟁 배제, 탐험 중심**(신규 도로·완주·정복률) | 센서·장비 격차로 속도 공정성이 성립하지 않음 — 차별화 축은 "오늘 어디를 탐험했는가" | 자문 문답 · [Conquest §3.2](260703-Conquest-정복-레이어-설계.md) |
| 2026-07-07 | `[Conquest]` `[Infra]` | **랭킹: 표시=행정구역, 계산=타일** — 행정코드는 달린 z12청크/z20셀에 lazy 1회 판정 후 캐시 | 런타임 역지오코딩 비용 회피. 자문의 "도로에 미리 부여"는 도로 ID 레지스트리 부재(OQ-13)로 셀 기반 수정 채택 | 자문 문답 + Claude 수정안 |
| 2026-07-07 | `[Docs]` | **document/ 아카이브 분리** — 톱레벨은 살아있는 문서만, 보고서류는 태어날 때부터 archive/ | 52편 평면 나열로 탐색 불능 — 상태보드를 단일 진입점으로 | [README](README.md) |
| 2026-07-06 | `[Economy]` | **free 월 경로 저장 한도 15 → 5** | 무료 체험 범위 조정(총 보유 30은 유지) | `tierQuotaCore.ts` |
| 2026-07-06 | `[Route]` | **동일 경로 판정 = S/E좌표+거리버킷+수단** — 중복 저장은 확인 후 기존 문서 갱신(quota 미소모) | 좌표열 전체 해시는 Directions 재라우팅 노이즈로 중복을 못 잡음. 같은 길 반복은 새 자산이 아님 | `routeFingerprint.ts` |
| 2026-07-03 | `[Conquest]` | **정복 단위 z16 타일(면) → z20 도로 셀(선)** | 사이클리스트의 자산은 면적이 아니라 달린 도로 — 노력 비례. 시뮬레이터라 경로가 곧 맵매칭 | [Conquest §3.1](260703-Conquest-정복-레이어-설계.md) |
| 2026-07-03 | `[Product]` `[Ontology]` | **핵심 판타지 「Ride = Claim」 확정, 명칭 RTW** | 그래픽 전장 회피, 해자=지구 전체(벡터 지도, 한계비용 ~0). 흔적이 영구 자산으로 축적 | [마스터 비전 §1.4·§7](260511-RTW-마스터-비전-및-종합계획.md) |
| 2026-07-03 | `[Conquest]` `[Economy]` | **정복 화폐 = 검증된 페달링(Trust Tier)** — 속도 슬라이더는 체험(T0) 티어 | 슬라이더는 개발 편의 장치. cheat 차단=중독 방지 동일 장치 | [Conquest §3.2](260703-Conquest-정복-레이어-설계.md) |
| ~2026-05 | `[Map]` | **지도 플랫폼 Mapbox** (구글 대비) | 위성 해상도는 구글 우위지만 3D·표현 자유도 우선. 위성지도는 정사영상 아님(누운 건물)이라 비노출 방침 | 마스터 비전 |
| ~2026-05 | `[Map]` | **구글 거리뷰 폐기 → Mapillary** | pre-version이 거리뷰 API 비용으로 사망 — 거리뷰는 더 이상 핵심 매력 아님 | 마스터 비전 §1 |

---

## 개정 이력

| 날짜 | 내용 |
|------|------|
| 2026-07-07 | 최초 작성 |
| 2026-07-14 | **태그 열 도입 + 이유 열 다이어트**(상세는 링크로) — 기존 결정 내용은 불변, 표기만 재정리 |
