/** `public/rider/pedal-sprite.png` 가로 스트립 메타(셀 120px 기준). 스프라이트 교체 시 너비로 프레임 수 맞출 것. */
export const RIDER_PEDAL_FRAME_COUNT = 20;
export const RIDER_PEDAL_CELL_PX = 120;

/** 스프라이트 URL 캐시 무력화 — 이미지 교체 시 1 증가 */
export const RIDER_PEDAL_SPRITE_REVISION = 1;
