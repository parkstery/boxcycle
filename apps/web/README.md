# BOXCYCLE 웹 (`boxcycle-web`)

Vite + TypeScript + React 본 개발 앱입니다. 실행·Firebase 설정은 저장소 루트 [`README.md`](../../README.md) 의 **본 개발 웹 앱** 절을 본다.

- 개발: `npm run dev`
- 빌드: `npm run build`
- 환경 변수: `.env.example` → `.env` (Firebase 웹 SDK 값)
