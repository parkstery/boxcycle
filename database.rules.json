{
  "rules": {
    "trails": {
      "$trailId": {
        "motion": {
          ".read": "auth != null",
          "$uid": {
            ".write": "auth != null && auth.uid == $uid",
            ".validate": "newData.hasChildren(['p', 'd', 'v', 'ph', 't']) && newData.child('p').isString() && newData.child('p').val().length > 0 && newData.child('p').val().length <= 128 && newData.child('d').isNumber() && newData.child('d').val() >= 0 && newData.child('v').isNumber() && newData.child('v').val() >= 0 && newData.child('ph').isString() && (newData.child('ph').val() == 'live' || newData.child('ph').val() == 'paused' || newData.child('ph').val() == 'completed') && newData.child('t').isNumber() && newData.child('t').val() > 0"
          }
        }
      }
    }
  }
}
